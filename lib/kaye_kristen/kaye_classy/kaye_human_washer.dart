class KayeHumanWasher {
  bool ohMonroeAs_ = false;
  bool mmLizardExpression_ = true;
  String isAwfulDucky_ = "";
  bool moPlimptonGlitter_ = false;
  void joCatChocolate() {
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();

    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
    if (ohMonroeAs_ || mmLizardExpression_ || moPlimptonGlitter_) {
      ohMonroeAs_ = !mmLizardExpression_;
      mmLizardExpression_ = !moPlimptonGlitter_;
      moPlimptonGlitter_ = !ohMonroeAs_;
    }

    if (ohMonroeAs_ || mmLizardExpression_ || moPlimptonGlitter_) {
      ohMonroeAs_ = !mmLizardExpression_;
      mmLizardExpression_ = !moPlimptonGlitter_;
      moPlimptonGlitter_ = !ohMonroeAs_;
    }
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
  }

  void opZipBeaker() {
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
    ohMonroeAs_ = moPlimptonGlitter_ || mmLizardExpression_;
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
    if (ohMonroeAs_ || mmLizardExpression_ || moPlimptonGlitter_) {
      ohMonroeAs_ = !mmLizardExpression_;
      mmLizardExpression_ = !moPlimptonGlitter_;
      moPlimptonGlitter_ = !ohMonroeAs_;
    }

    mmLizardExpression_ = moPlimptonGlitter_ && ohMonroeAs_;
    moPlimptonGlitter_ = mmLizardExpression_ && ohMonroeAs_;
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
  }

  void taSleazyWarlock() {
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();

    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
  }

  void beBreachSmoothie() {
    if (moPlimptonGlitter_ && mmLizardExpression_ && ohMonroeAs_) {
      moPlimptonGlitter_ = !moPlimptonGlitter_;
      mmLizardExpression_ = moPlimptonGlitter_;
      ohMonroeAs_ = moPlimptonGlitter_;
    }

    if (mmLizardExpression_ || ohMonroeAs_ || moPlimptonGlitter_) {
      mmLizardExpression_ = !ohMonroeAs_;
      ohMonroeAs_ = !moPlimptonGlitter_;
      moPlimptonGlitter_ = !mmLizardExpression_;
    }

    if (mmLizardExpression_ && ohMonroeAs_) {
      moPlimptonGlitter_ = !moPlimptonGlitter_;
    }
  }

  void faAahSwell() {
    mmLizardExpression_ = ohMonroeAs_ || moPlimptonGlitter_;
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
    if (mmLizardExpression_ && ohMonroeAs_) {
      moPlimptonGlitter_ = !moPlimptonGlitter_;
    }
    isAwfulDucky_ = isAwfulDucky_.toUpperCase();
  }
}
