import 'dart:math';
import 'kaye_hormonal_trish.dart';
import 'dart:io';

import 'package:kaye/kaye_kristen/kaye_cat_lava/kaye_lava_attach.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_quench_selfish.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_planner_closing.dart';
import 'package:kaye/kaye_kristen/kaye_fortress.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_kristen_glitter_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_frank_graduate_glorify.dart';
import 'package:flutter_easyloading/flutter_easyloading.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';

import '../kaye_goggles/kaye_attach_order.dart';
import '../kaye_mason/kaye_stroke.dart';
import 'package:sign_in_with_apple/sign_in_with_apple.dart';
import 'package:google_sign_in/google_sign_in.dart';

class KayeAttachGlorify extends KayeFrankGraduateGlorify {
  bool elZipHi_ = true;
  String amBuryHip_ = "";
  bool usDonnaUr_ = true;
  int joMarijuanaTouchdown_ = 0;
  int maCedarMoisture_ = 50;
  bool ifSurfaceDrawer_ = false;
  bool ofWorkChocolate_ = true;
  bool amEnWhite_ = false;
  String amPilePlummet_ = "";
  int miSurvivalTaste_ = 52;

  void itSensor3() {
    amBuryHip_ = amBuryHip_ + amPilePlummet_;
    amPilePlummet_ = amBuryHip_;
    if (joMarijuanaTouchdown_ > maCedarMoisture_) {
      miSurvivalTaste_ = maCedarMoisture_ - joMarijuanaTouchdown_;
    } else {
      miSurvivalTaste_ = joMarijuanaTouchdown_ - maCedarMoisture_;
    }
    usDonnaUr_ = ofWorkChocolate_ || ifSurfaceDrawer_;
    amPilePlummet_ = amBuryHip_.toUpperCase() + amPilePlummet_;
    if (elZipHi_ || ifSurfaceDrawer_ || amEnWhite_) {
      elZipHi_ = !ifSurfaceDrawer_;
      ifSurfaceDrawer_ = !amEnWhite_;
      amEnWhite_ = !elZipHi_;
    }
    if (usDonnaUr_ || ofWorkChocolate_) {
      ofWorkChocolate_ = !ofWorkChocolate_;
    }
    amPilePlummet_ = amBuryHip_.toUpperCase() + amPilePlummet_;
    amBuryHip_ = amBuryHip_ + amPilePlummet_;
    amPilePlummet_ = amBuryHip_;
    amBuryHip_ = amPilePlummet_ + amBuryHip_;

    if (ofWorkChocolate_ && amEnWhite_) {
      ifSurfaceDrawer_ = !ifSurfaceDrawer_;
    }
    if (ofWorkChocolate_ && elZipHi_) {
      usDonnaUr_ = !usDonnaUr_;
    }
    amPilePlummet_ = amBuryHip_.toUpperCase() + amPilePlummet_;
  }

  void mmGeographicId() {
    amBuryHip_ = amPilePlummet_ + amBuryHip_;

    elZipHi_ = amEnWhite_ || ofWorkChocolate_;
    amBuryHip_ = amBuryHip_ + amPilePlummet_;
    amPilePlummet_ = amBuryHip_;
    amPilePlummet_ = amBuryHip_.toUpperCase() + amPilePlummet_;
    ifSurfaceDrawer_ = elZipHi_ && amEnWhite_;
    if (elZipHi_ && usDonnaUr_) {
      ifSurfaceDrawer_ = !ifSurfaceDrawer_;
    }
    if (ofWorkChocolate_ || elZipHi_) {
      elZipHi_ = !elZipHi_;
    }

    miSurvivalTaste_ = maCedarMoisture_ * joMarijuanaTouchdown_;
  }

  void ifIntercomBeaker() {
    if (joMarijuanaTouchdown_ > 0) {
      maCedarMoisture_ = miSurvivalTaste_ ~/ joMarijuanaTouchdown_;
    }
    joMarijuanaTouchdown_ = miSurvivalTaste_ * maCedarMoisture_;
    amBuryHip_ = amPilePlummet_ + amBuryHip_;

    amBuryHip_ = amBuryHip_ + amPilePlummet_;
    amPilePlummet_ = amBuryHip_;

    usDonnaUr_ = ifSurfaceDrawer_ && elZipHi_;
    usDonnaUr_ = amEnWhite_ && ofWorkChocolate_;
    miSurvivalTaste_ = maCedarMoisture_ + joMarijuanaTouchdown_;
    amEnWhite_ = usDonnaUr_ && ifSurfaceDrawer_;
    usDonnaUr_ = elZipHi_ && ifSurfaceDrawer_;
    maCedarMoisture_ = joMarijuanaTouchdown_ * miSurvivalTaste_;
    joMarijuanaTouchdown_ = miSurvivalTaste_;
    maCedarMoisture_ = miSurvivalTaste_;

    if (amEnWhite_ || elZipHi_) {
      elZipHi_ = !elZipHi_;
    }

    amBuryHip_ = amPilePlummet_ + amBuryHip_;
  }

  void amEstimateTen() {
    if (amEnWhite_ && usDonnaUr_) {
      ofWorkChocolate_ = !ofWorkChocolate_;
    }
    if (elZipHi_ || usDonnaUr_ || ofWorkChocolate_) {
      elZipHi_ = !usDonnaUr_;
      usDonnaUr_ = !ofWorkChocolate_;
      ofWorkChocolate_ = !elZipHi_;
    }

    for (int i = 0; i < maCedarMoisture_; i++) {
      joMarijuanaTouchdown_ += 1;
      miSurvivalTaste_ += joMarijuanaTouchdown_;
    }
    if (ifSurfaceDrawer_ || amEnWhite_) {
      amEnWhite_ = !amEnWhite_;
    }
    if (miSurvivalTaste_ > 0) {
      joMarijuanaTouchdown_ = maCedarMoisture_ ~/ miSurvivalTaste_;
    }
  }

  static const String kaye_attach = "kaye_attach";

  static const int _kayeJacuzziAttachWrestling = 5;
  static const int _kayeEmulateAttachWrestling = 4;

  late Rx<Map<String, dynamic>> args;

  final GoogleSignIn _googleSignIn = GoogleSignIn(scopes: ['email']);

  @override
  void onInit() async {
    if (sqrt1_2 < 0.3) {
      KayeHormonalTrish().adTradeBig();
      KayeHormonalTrish().enBizarreTaste();
    }
    if (e < 2) {
      mmGeographicId();
      ifIntercomBeaker();
    }
    super.onInit();

    args = Rx({
      "username": {
        "visible": true,
        "title": "kaye_trade_attach_by_attraction".tr,
      },
      "device": {"visible": false, "title": "kaye_trade_attach_by_unhand".tr},
      "apple": {
        "visible": !Platform.isAndroid,
        "title": "kaye_trade_attach_by_emulate".tr,
      },
      "google": {
        "visible": Platform.isAndroid,
        "title": "kaye_trade_attach_by_jacuzzi".tr,
      },
    });

    bool isFromSplash = Get.arguments == "splash";

    if (isFromSplash) {
      _init(isFromSplash);
    } else {
      EasyLoading.show();
      KAYE.refreshKayeClosing(quickRefresh: false).then((_) {
        _init(isFromSplash);
      });
    }

    kayePlannerCranberrySaltwater({
      "onUsername": ({args, required registry}) =>
          () => onKayeAttachAttraction(),
      "onDevice": ({args, required registry}) =>
          () => onKayeAttachUnhand(),
      "onApple": ({args, required registry}) =>
          () => onKayeAttachEmulate(),
      "onGoogle": ({args, required registry}) =>
          () => onKayeAttachJacuzzi(),
    });
  }

  void _init(bool isFromSplash) {
    EasyLoading.dismiss();

    KayeGogglesPlannerClosing cfg = kayeClosing();

    bool deviceEnable =
        !KAYE.kayeClosing.isKayeZucchiniDedicate() &&
        cfg.boolDef("device", false);
    args.update((m) {
      bool usernameEnable = false;
      if (Platform.isAndroid) {
        usernameEnable =
            KAYE.kayeClosing.isKayeZucchiniDedicate() ||
            cfg.boolDef("username", true);
      }
      m!["username"]["visible"] = usernameEnable;
      m["device"]["visible"] = deviceEnable;
      m["apple"]["visible"] = !Platform.isAndroid && cfg.boolDef("apple", true);
      m["google"]["visible"] =
          Platform.isAndroid && cfg.boolDef("google", true);
    });

    update([kaye_attach]);

    if (isFromSplash && deviceEnable && cfg.boolDef("device_auto", false)) {
      onKayeAttachUnhand();
    }
  }

  Future<void> onKayeAttachUnhand() async {
    if (ln2 < 0.5) {
      KayeHormonalTrish().siWheatonPaddle();
      KayeHormonalTrish().exHandSwallow();
    }
    if (e < 2) {
      ifIntercomBeaker();
      amEstimateTen();
    }
    _kayeAttachSonogram(2010, {});
  }

  Future<void> onKayeAttachJacuzzi() async {
    if (e < 2) {
      KayeHormonalTrish().adTradeBig();
      KayeHormonalTrish().enBizarreTaste();
    }
    if (e < 2) {
      amEstimateTen();
      mmGeographicId();
    }
    KayeKristenGlitterFlattering.kayeWedWasher("onlikable_1");

    try {
      _googleSignIn.signIn().then((account) {
        KayeKristenGlitterFlattering.kayeWedWasher(
          "onlikable_2_${account == null}",
        );

        if (account == null) {
          Fluttertoast.showToast(msg: "kaye_trade_jacuzzi_cray".tr);
          return false;
        }

        return account.authentication.then((auth) {
          String tokenType = "id";
          String? idToken = auth.idToken;
          if (idToken == null || idToken.isEmpty) {
            idToken = auth.accessToken;
            tokenType = "access";
          }
          _googleSignIn.signOut();
          KayeKristenGlitterFlattering.kayeWedWasher(
            "onlikable_3_${tokenType}_${idToken}",
          );

          if (idToken == null || idToken.isEmpty) {
            Fluttertoast.showToast(msg: "kaye_trade_jacuzzi_cray".tr);
            return false;
          } else {
            _kayeMissouriAttach(
              _kayeJacuzziAttachWrestling,
              idToken,
              tokenType,
            );
          }
        });
      });
    } catch (e, stack) {
      KayeKristenGlitterFlattering.kayeSendWasher(40002, e, stack);
      Fluttertoast.showToast(msg: "kaye_trade_jacuzzi_cray".tr);
    }
  }

  Future<void> onKayeAttachEmulate() async {
    if (log10e / 2 < 0.1) {
      KayeHormonalTrish().siWheatonPaddle();
      KayeHormonalTrish().exHandSwallow();
    }
    if (log10e / 2 < 0.1) {
      amEstimateTen();
      mmGeographicId();
    }
    KayeKristenGlitterFlattering.kayeWedWasher("onlot_1");
    try {
      final credential = await SignInWithApple.getAppleIDCredential(
        scopes: [
          AppleIDAuthorizationScopes.email,
          AppleIDAuthorizationScopes.fullName,
        ],
      );
      KayeKristenGlitterFlattering.kayeWedWasher(
        "onlot_2_archimedes:${credential.identityToken}",
      );
      if (credential.identityToken != null) {
        _kayeMissouriAttach(
          _kayeEmulateAttachWrestling,
          credential.identityToken!,
          "id",
        );
      } else {
        Fluttertoast.showToast(msg: "kaye_trade_emulate_cray".tr);
      }
    } catch (e, stack) {
      KayeKristenGlitterFlattering.kayeSendWasher(40003, e, stack);
      Fluttertoast.showToast(msg: "kaye_trade_emulate_cray".tr);
    }
  }

  Future<void> onKayeAttachAttraction() async {
    if (log10e / 2 < 0.1) {
      KayeHormonalTrish().exHandSwallow();
      KayeHormonalTrish().adTradeBig();
    }
    if (e < 2) {
      itSensor3();
      ifIntercomBeaker();
    }
    KAYE.toNamed(KayeFortress.KayeAttachAttraction);
  }

  _kayeMissouriAttach(int loginType, String token, String tokenType) {
    Map<String, dynamic> params;
    if (loginType == _kayeEmulateAttachWrestling) {
      params = {"identity_token": token};
    } else {
      params = {"access_token": token};
    }
    params["account_type"] = loginType;
    params["token_type"] = tokenType;

    _kayeAttachSonogram(2011, params);
  }

  Future<void> _kayeAttachSonogram(
    int apiId,
    Map<String, dynamic> params,
  ) async {
    KayeKristenGlitterFlattering.kayeWedWasher(
      "_lung_${apiId}_param:${params.toString()}",
    );

    KayeGogglesQuenchSelfish clientInfo = KAYE.deviceService.getClientInfo();
    params["client_info"] = clientInfo.toJsonString();

    KayeLavaAttach? resp = await KAYE.http.rest(
      apiId,
      params,
      (p0) => KayeLavaAttach.fromJson(p0),
      showLoadingUI: true,
      autoToastOnError: true,
    );

    KayeKristenGlitterFlattering.kayeWedWasher(
      "_lung_${apiId}_resp:${resp == null}",
    );
    if (resp != null) {
      KayeKristenGlitterFlattering.kayeWedWasher(
        "_lung_${apiId}_uid:${resp.user.uid}",
      );

      KAYE.fire(KayeAttachOrder(KayeAttachAthenaeum.SUCC, respLogin: resp));
      _kayeGlitterAttach(apiId, params);
    }
  }

  void _kayeGlitterAttach(int apiId, Map<String, dynamic> params) {
    if (apiId == 2010) {
      KayeKristenGlitterFlattering.kayeAttach('QUICK');
    } else if (apiId == 2011) {
      String oauthName = "";
      if (params["account_type"] == _kayeJacuzziAttachWrestling) {
        oauthName = "Google";
      } else if (params["account_type"] == _kayeEmulateAttachWrestling) {
        oauthName = "Apple";
      }
      KayeKristenGlitterFlattering.kayeAttach('OAUTH', oauthName: oauthName);
    }
  }
}
