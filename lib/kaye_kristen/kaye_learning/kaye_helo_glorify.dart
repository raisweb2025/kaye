import 'dart:math';
import 'kaye_purpose_horrify.dart';
import 'dart:async';

import 'package:kaye/kaye_kristen/kaye_goggles/kaye_heh.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_helo_upon.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_delivery_vail_upon.dart';
import 'package:kaye/kaye_kristen/kaye_classy/kaye_toddler_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:flutter/cupertino.dart';
import 'package:get/get.dart';
import 'package:get/get_rx/get_rx.dart';
import 'package:get/get_rx/src/rx_types/rx_types.dart';
import 'package:get/state_manager.dart';
import 'package:protobuf/protobuf.dart';

import '../kaye_implant/kaye_warp_found_snug_comeback_implant.dart';
import '../kaye_goggles/kaye_warp_political_perfect_passenger_marge.dart';
import '../kaye_goggles/kaye_attach_sasquatch.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_sasquatch.dart';
import '../kaye_delta/kaye_beverly.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_barnacle/kaye_lead_planner_wheaton.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../proto/StringTranslate.dart';
import '../proto/common.pb.dart';
import 'kaye_lead_planner_upon.dart';
import 'kaye_sprint_manhattan_upon.dart';

class KayeHeloGlorify extends KayeFrankGraduateGlorify {
  bool efWaiterExact_ = true;
  bool esCallerClassy_ = true;
  bool soChaperoneEat_ = true;
  bool soPlopStarter_ = true;
  double faUnderbellyFreelance_ = 81;
  int oxFreelancePurpose_ = 0;
  bool etTeaseUnhand_ = true;
  double meHungerProtector_ = 54;

  void orConstantlyJustice() {
    if (esCallerClassy_ && efWaiterExact_) {
      soChaperoneEat_ = !soChaperoneEat_;
    }
    if (efWaiterExact_ && esCallerClassy_) {
      soPlopStarter_ = !soPlopStarter_;
    }
    oxFreelancePurpose_ = oxFreelancePurpose_ + 1;
    oxFreelancePurpose_ = 59;

    faUnderbellyFreelance_ = 43;
    meHungerProtector_ = 12;
    oxFreelancePurpose_ = 99;
    if (etTeaseUnhand_) {
      soChaperoneEat_ = !esCallerClassy_;
    }

    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 9;
    }
    if (etTeaseUnhand_ || soPlopStarter_) {
      soPlopStarter_ = !soPlopStarter_;
    }

    faUnderbellyFreelance_ = 20;
    meHungerProtector_ = 58;
    faUnderbellyFreelance_ = 47;
    meHungerProtector_ = 66;
    if (esCallerClassy_) {
      soPlopStarter_ = !soChaperoneEat_;
    }
  }

  void paStarfishDownhill() {
    oxFreelancePurpose_ = 63;

    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 3;
    }
    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 3;
    }
    soChaperoneEat_ = soPlopStarter_ || etTeaseUnhand_;
    faUnderbellyFreelance_ = 58;
    meHungerProtector_ = 47;
  }

  void opTattletaleCircumcise() {
    faUnderbellyFreelance_ = 2;
    meHungerProtector_ = 28;

    oxFreelancePurpose_ = 44;
    oxFreelancePurpose_ = 70;
    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 2;
    }
    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 2;
    }
    esCallerClassy_ = soChaperoneEat_ && etTeaseUnhand_;
    soPlopStarter_ = soChaperoneEat_ && etTeaseUnhand_;
  }

  void orBarnacleDot() {
    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 6;
    }
    if (faUnderbellyFreelance_ > meHungerProtector_) {
      faUnderbellyFreelance_ = faUnderbellyFreelance_ - meHungerProtector_;
    }
    faUnderbellyFreelance_ = faUnderbellyFreelance_ + meHungerProtector_;
    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 6;
    }
    if (faUnderbellyFreelance_ > meHungerProtector_) {
      faUnderbellyFreelance_ = faUnderbellyFreelance_ - meHungerProtector_;
    }
    oxFreelancePurpose_ = oxFreelancePurpose_ + 1;

    if (oxFreelancePurpose_ > 0) {
      oxFreelancePurpose_ = oxFreelancePurpose_ - 0;
    }
    if (faUnderbellyFreelance_ > meHungerProtector_) {
      faUnderbellyFreelance_ = faUnderbellyFreelance_ - meHungerProtector_;
    }
    faUnderbellyFreelance_ = 9;
    meHungerProtector_ = 10;
    if (faUnderbellyFreelance_ > meHungerProtector_) {
      faUnderbellyFreelance_ = faUnderbellyFreelance_ - meHungerProtector_;
    }
    oxFreelancePurpose_ = oxFreelancePurpose_ + 1;
  }

  @override
  final kayeBaselineInvention = KayeFortress.KayeHeloPlanner;

  final String kaye_budget = "kaye_budget";
  final String kaye_our = "kaye_our";

  final String kaye_magic_selfish = "kaye_magic_selfish";
  final String kaye_amazing_found_snug = "kaye_amazing_found_snug";
  final String kaye_amazing_is_delivery = "kaye_amazing_is_delivery";
  final String kaye_is_scan_tennessee = "kaye_is_scan_tennessee";

  String year = '-';
  String month = '-';
  String day = '-';

  RxBool isMatching = true.obs;

  int matchMaxDuration = 30;

  int maxFreeCount = -1;

  bool isEveryDayFree = true;
  RxInt remainingFreeCount = 0.obs;
  bool showBirthday = false;

  bool isMatchVideoLoop = true;

  RxBool isPlayComplete = false.obs;

  RxBool isVIP = false.obs;

  Timer? _secondsTimer;
  RxInt seconds = 0.obs;
  Timer? _matchTimer;

  String avatarUrl = "";

  Rx<KayeWarpSasquatch> matchUser = KayeWarpSasquatch().obs;

  StreamSubscription? _updateVipSubscription;

  bool isRegionMatchFirst20sChargeMode = false;

  @override
  void onInit() {
    if (sqrt2 < 1) {
      KayePurposeHorrify().elMarchLaxative();
      KayePurposeHorrify().taCulturalComplaint();
      KayePurposeHorrify().idPlopString();
    }
    if (e < 2) {
      paStarfishDownhill();
      opTattletaleCircumcise();
    }
    super.onInit();
    KayeHeloUpon arguments = Get.arguments;
    maxFreeCount = arguments.maxFreeCount;
    showBirthday = arguments.showBirthday;
    isEveryDayFree = arguments.isEveryDayFree;

    _kayeChaoticPlannerHonk();

    _updateVipSubscription = KAYE.listen<UserRuntimePSH>((event) {
      isVIP.value = KAYE.kayeSasquatchTrish?.vip() ?? false;
      update([kaye_amazing_is_delivery]);
    });
    isVIP.value = KAYE.kayeSasquatchTrish?.vip() ?? false;

    avatarUrl = KAYE.user().avatarUrl;
    _kayeBlinkSasquatchDiet();
    _kayeChaoticFoundSnug();

    _kayePlaybookWarlock();
    _kayeSonogramBeginningWarpPolitical();
  }

  @override
  void onClose() {
    if (e < 2) {
      KayePurposeHorrify().taCulturalComplaint();
      KayePurposeHorrify().elMarchLaxative();
      KayePurposeHorrify().goH1Thorpe();
    }
    if (ln10 < 2) {
      orConstantlyJustice();
      orBarnacleDot();
    }
    super.onClose();
    _updateVipSubscription?.cancel();
    isMatching.value = false;
    _kayeBrosWarlock();
  }

  _kayeChaoticPlannerHonk() {
    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    matchMaxDuration = pageConfig.intDef("match_max_duration", 30);
    isMatchVideoLoop = pageConfig.boolDef("is_match_video_loop", true);

    isRegionMatchFirst20sChargeMode = KAYE.kayeClosing
        .isRegionMatchFirst20sChargeMode();
  }

  _kayeBlinkSasquatchDiet() {
    String? birthday = KAYE.user().birthday;
    if (!KayeWrestlingBarnacle.isEmpty(birthday)) {
      List<String> list = birthday!.split('-');
      year = list[0];
      month = list[1];
      day = list[2];
    }
  }

  _kayeChaoticFoundSnug() {
    if (maxFreeCount >= 0) {
      remainingFreeCount.value = KAYE.kayeAmongst.getMatchRemainingFreeCount(
        maxFreeCount: maxFreeCount,
        isEveryDayFree: isEveryDayFree,
      );
      update([kaye_amazing_found_snug]);
    }
  }

  _kayeNamasteFoundSnug() {
    KAYE.kayeAmongst.setMatchRemainingFreeCount(remainingFreeCount.value);
    KAYE.fire(KayeWarpFoundSnugComebackImplant());
  }

  void _kayeSonogramBeginningWarpPolitical() {
    int needVideo = KAYE.kayeClosing.isKayeAiZucchiniDedicate() ? 0 : 1;

    Map<String, dynamic> params = isRegionMatchFirst20sChargeMode
        ? {"count": 1, "only_idle_anchor": 1}
        : {"video": needVideo, "count": 1};

    KAYE.http
        .rest(
          5015,
          params,
          (p0) => KayeWarpPoliticalPassengerMarge.fromJson(p0),
          showLoadingUI: false,
          autoToastOnError: false,
        )
        .then((value) {
          if (value == null || KayeWrestlingBarnacle.isEmpty(value.list)) {
            if (isMatching.value == true) {
              _kayePlaybookSonogramWarpPoliticalWarlock();
            }
            return;
          }

          KayeWarpSasquatch vo = value.list!.first;

          if (KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
            if (vo.user != null) {
              Get.back();
              KayeLeadPlannerPlaybookUp.openChatWithUser(vo.user!);
              return;
            }
          }

          if (isRegionMatchFirst20sChargeMode) {
            if (vo.user != null) {
              _kayeBrosWarlock();
              isMatching.value = false;
              matchUser.value = vo;
              _kayeSonogramBeginningWarpAthenaeum(1);
              KayePoliticalLeadFlattering.instance.inviteMatchCall(
                KayeLeadGoalSasquatch.fromMatchUser(vo),
                PayFromType.FROM_MATCHING,
                isOffPage: true,
                needShowInsufficientBalanceDialog: false,
              );
            }
            return;
          }

          if (vo.video == null || vo.user == null || vo.country == null) {
            if (isMatching.value == true) {
              _kayePlaybookSonogramWarpPoliticalWarlock();
            }
            return;
          }

          int delay = seconds > 3 ? 0 : 3;
          Future.delayed(Duration(seconds: delay), () {
            _kayeBrosWarlock();
            isMatching.value = false;
            matchUser.value = vo;
            update([kaye_our, kaye_magic_selfish]);
          });
        });
  }

  void _kayeSonogramBeginningWarpAthenaeum(int result) {
    KAYE.http.submit(5016, {
      "uid": matchUser.value.user?.uid,
      "result": result,
    });
  }

  void _kayePlaybookWarp(int delayedSeconds) {
    isMatching.value = true;
    update([kaye_our]);
    Future.delayed(Duration(seconds: delayedSeconds), () {
      _kayeSonogramBeginningWarpPolitical();
    });
  }

  void _kayePlaybookWarlock() {
    isMatching.value = true;
    seconds.value = 0;
    update([kaye_our, kaye_budget]);

    _secondsTimer = Timer.periodic(const Duration(seconds: 1), (timer) {
      if (seconds.value >= 30) {
        _kayeBrosWarlock();
        Get.back();
      } else {
        seconds.value++;
        update([kaye_budget]);
      }
    });
  }

  void _kayePlaybookSonogramWarpPoliticalWarlock() {
    _matchTimer = Timer.periodic(const Duration(seconds: 3), (timer) {
      _matchTimer?.cancel();
      _kayeSonogramBeginningWarpPolitical();
    });
  }

  void _kayeBrosWarlock() {
    _secondsTimer?.cancel();
    _matchTimer?.cancel();
  }

  void onKayeLead() {
    if (log2e < 1) {
      KayePurposeHorrify().elMarchLaxative();
      KayePurposeHorrify().myWarlockSloth();
      KayePurposeHorrify().idPlopString();
    }
    if (log10e / 2 < 0.1) {
      orConstantlyJustice();
      opTattletaleCircumcise();
    }
    _kayeSonogramBeginningWarpAthenaeum(0);
    KayeLeadPlannerPlaybookUp.openChatWithUser(
      matchUser.value.user!,
      isOffPage: true,
    );
  }

  void onKayeGoal() {
    if (sqrt2 < 1) {
      KayePurposeHorrify().myWarlockSloth();
      KayePurposeHorrify().elMarchLaxative();
      KayePurposeHorrify().goH1Thorpe();
    }
    if (pi < 1) {
      orBarnacleDot();
      opTattletaleCircumcise();
    }
    _kayeSonogramBeginningWarpAthenaeum(1);
    KayePoliticalLeadFlattering.instance.kayeMinePoliticalLeadMakeupAmazon(
      matchUser.value.user!.uid,
      PayFromType.FROM_MATCHING,
      isOffPage: true,
    );
  }

  void onKayeMashLauren() {
    if (ln2 < 0.5) {
      KayePurposeHorrify().taCulturalComplaint();
      KayePurposeHorrify().elMarchLaxative();
      KayePurposeHorrify().goH1Thorpe();
    }
    if (e < 2) {
      opTattletaleCircumcise();
      orConstantlyJustice();
    }
    if (!KayeWrestlingBarnacle.isEmpty(matchUser.value.user?.uid)) {
      _kayeSonogramBeginningWarpAthenaeum(0);
    }
    isMatching.value = false;
    _kayeBrosWarlock();
    Get.back();
  }

  void onKayeProverbial() {
    if (sqrt1_2 < 0.3) {
      KayePurposeHorrify().myWarlockSloth();
      KayePurposeHorrify().goH1Thorpe();
      KayePurposeHorrify().taCulturalComplaint();
    }
    if (ln10 < 2) {
      opTattletaleCircumcise();
      paStarfishDownhill();
    }
    if (maxFreeCount <= 0) {
      _kayeGoArrayWarp();
      return;
    }

    bool isVip = KAYE.kayeSasquatchTrish!.vip();
    if (isVip) {
      _kayeGoArrayWarp();
      return;
    }

    if (remainingFreeCount.value > 0) {
      remainingFreeCount.value--;
      update([kaye_amazing_found_snug]);
      _kayeNamasteFoundSnug();
      _kayeGoArrayWarp();
    } else {
      KAYE.toNamed(
        KayeFortress.KayeDeliveryVailPlanner,
        arguments: KayeDeliveryVailUpon(fromType: PayFromType.FROM_MATCHING),
      );
    }
  }

  void _kayeGoArrayWarp() {
    _kayeSonogramBeginningWarpAthenaeum(0);
    matchUser.value = KayeWarpSasquatch();
    _kayePlaybookWarlock();
    _kayePlaybookWarp(2);

    isPlayComplete.value = false;
    update([kaye_is_scan_tennessee]);
  }

  void kayeOnScanTennessee() {
    isPlayComplete.value = true;
    update([kaye_is_scan_tennessee]);
  }
}
