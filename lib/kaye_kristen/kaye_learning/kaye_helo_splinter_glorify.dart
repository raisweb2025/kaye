import 'dart:math';
import 'kaye_snug_correspond.dart';
import 'dart:async';
import 'dart:math';

import 'package:kaye/kaye_kristen/kaye_goggles/kaye_sasquatch.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';

import '../kaye_goggles/kaye_warp_political_perfect_passenger_marge.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_barnacle/kaye_lead_planner_wheaton.dart';
import '../kaye_barnacle/kaye_wrestling_barnacle.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import 'kaye_helo_splinter_upon.dart';
import 'kaye_sprint_manhattan_upon.dart';

class KayeHeloSplinterGlorify extends KayeFrankGraduateGlorify {
  bool ohSwellProfile_ = false;
  bool ahForgetSandal_ = false;
  bool enGrievanceSpeed_ = false;
  String mmFlatteringEmpower_ = "";
  String bySoleCorrespond_ = "";
  bool taWrappedTopical_ = true;
  int asIcTen_ = 0;
  bool odVergeCaller_ = true;
  double doStopAssistant_ = 0.0;

  void loChocolatePudding() {
    mmFlatteringEmpower_ = mmFlatteringEmpower_ + bySoleCorrespond_;
    bySoleCorrespond_ = mmFlatteringEmpower_;
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
    if (odVergeCaller_ && ohSwellProfile_) {
      taWrappedTopical_ = !taWrappedTopical_;
    }
    if (asIcTen_ > 0) {
      asIcTen_ = asIcTen_ - 0;
    }
    asIcTen_ = 41;
    mmFlatteringEmpower_ = mmFlatteringEmpower_ + bySoleCorrespond_;
    bySoleCorrespond_ = mmFlatteringEmpower_;
    if (asIcTen_ > 0) {
      asIcTen_ = asIcTen_ - 4;
    }
    doStopAssistant_ = doStopAssistant_ + 1;
    if (doStopAssistant_ > 0) {
      doStopAssistant_ = doStopAssistant_ - 1;
    }
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
    doStopAssistant_ = doStopAssistant_ + 1;
    asIcTen_ = 39;
    odVergeCaller_ = enGrievanceSpeed_ && ahForgetSandal_;
    bySoleCorrespond_ = mmFlatteringEmpower_.toUpperCase() + bySoleCorrespond_;
    if (asIcTen_ > 0) {
      asIcTen_ = asIcTen_ - 1;
    }
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
    if (taWrappedTopical_ || enGrievanceSpeed_) {
      enGrievanceSpeed_ = !enGrievanceSpeed_;
    }
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
    ahForgetSandal_ = ohSwellProfile_ || taWrappedTopical_;
  }

  void emArchimedesMumble() {
    if (odVergeCaller_ || ahForgetSandal_ || enGrievanceSpeed_) {
      odVergeCaller_ = !ahForgetSandal_;
      ahForgetSandal_ = !enGrievanceSpeed_;
      enGrievanceSpeed_ = !odVergeCaller_;
    }
    if (asIcTen_ > 0) {
      asIcTen_ = asIcTen_ - 6;
    }
    asIcTen_ = asIcTen_ + 1;
    doStopAssistant_ = doStopAssistant_ + 1;
    asIcTen_ = 39;
    enGrievanceSpeed_ = ohSwellProfile_ && odVergeCaller_;
    if (asIcTen_ > 0) {
      asIcTen_ = asIcTen_ - 5;
    }
  }

  void amKitchenForklift() {
    if (asIcTen_ > 0) {
      asIcTen_ = asIcTen_ - 6;
    }
    mmFlatteringEmpower_ = mmFlatteringEmpower_ + bySoleCorrespond_;
    bySoleCorrespond_ = mmFlatteringEmpower_;
    mmFlatteringEmpower_ = mmFlatteringEmpower_ + bySoleCorrespond_;
    bySoleCorrespond_ = mmFlatteringEmpower_;
    asIcTen_ = 90;
    asIcTen_ = asIcTen_ + 1;
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
  }

  void goCriticizeEagle() {
    doStopAssistant_ = doStopAssistant_ + 1;
    mmFlatteringEmpower_ = mmFlatteringEmpower_ + bySoleCorrespond_;
    bySoleCorrespond_ = mmFlatteringEmpower_;
    asIcTen_ = asIcTen_ + 1;
    doStopAssistant_ = 79;
    doStopAssistant_ = doStopAssistant_ + 1;
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
    mmFlatteringEmpower_ = bySoleCorrespond_ + mmFlatteringEmpower_;
    if (ohSwellProfile_ || ahForgetSandal_ || odVergeCaller_) {
      ohSwellProfile_ = !ahForgetSandal_;
      ahForgetSandal_ = !odVergeCaller_;
      odVergeCaller_ = !ohSwellProfile_;
    }
    asIcTen_ = asIcTen_ + 1;
    mmFlatteringEmpower_ = mmFlatteringEmpower_ + bySoleCorrespond_;
    bySoleCorrespond_ = mmFlatteringEmpower_;
    asIcTen_ = asIcTen_ + 1;
    doStopAssistant_ = doStopAssistant_ + 1;
  }

  final String kaye_anything_fax = "kaye_anything_fax";

  @override
  final kayeBaselineInvention = KayeFortress.KayeHeloSplinterPlanner;

  int durationSeconds = 1000;

  RxInt scaleStatus = 0.obs;

  final String kaye_budget = "kaye_budget";
  final String kaye_our = "kaye_our";

  final String kaye_magic_selfish = "kaye_magic_selfish";
  final String kaye_amazing_found_snug = "kaye_amazing_found_snug";
  final String kaye_amazing_is_delivery = "kaye_amazing_is_delivery";
  final String kaye_is_scan_tennessee = "kaye_is_scan_tennessee";

  final String kaye_sasquatch_no_upscale = "kaye_sasquatch_no_upscale";

  RxBool isMatching = true.obs;

  int matchMaxDuration = 30;

  Timer? _secondsTimer;
  RxInt seconds = 0.obs;
  Timer? _matchTimer;

  String avatarUrl = "";

  String randomAvatarUrl = "";
  List<KayeWarpSasquatch> matchUserList = [];

  Rx<KayeWarpSasquatch> matchUser = KayeWarpSasquatch().obs;
  bool userNoMoney = false;

  @override
  void onInit() {
    if (pi < 1) {
      KayeSnugCorrespond().emExpressionErica();
      KayeSnugCorrespond().taEyebrowCaller();
      KayeSnugCorrespond().miBaselineSuite();
    }
    if (ln10 < 2) {
      amKitchenForklift();
      loChocolatePudding();
    }
    super.onInit();
    KayeHeloSplinterUpon arguments = Get.arguments;
    matchUserList = arguments.matchUserList;
    randomAvatarUrl = matchUserList.first.user?.avatarUrl ?? "";

    _kayeChaoticPlannerHonk();
    _kayePlaybookWarlock();
    _kayePlaybookWarp(3);
  }

  @override
  void onClose() {
    if (pi < 1) {
      KayeSnugCorrespond().anAssistantTrophy();
      KayeSnugCorrespond().taEyebrowCaller();
      KayeSnugCorrespond().miBaselineSuite();
    }
    if (ln10 < 2) {
      amKitchenForklift();
      loChocolatePudding();
    }
    super.onClose();
    isMatching.value = false;
    _kayeBrosWarlock();
  }

  _kayeChaoticPlannerHonk() {
    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    matchMaxDuration = pageConfig.intDef("match_max_duration", 30);
  }

  void _kayeSonogramBeginningWarpQuack() {
    KAYE.http
        .rest(
          5015,
          {"video": 0, "count": 1, "only_idle_anchor": 1},
          (p0) => KayeWarpPoliticalPassengerMarge.fromJson(p0),
          showLoadingUI: false,
          autoToastOnError: false,
        )
        .then((value) {
          if (value == null || KayeWrestlingBarnacle.isEmpty(value.list)) {
            if (isMatching.value == true) {
              _kayePlaybookSonogramWarpPoliticalWarlock();
            }
            return;
          }

          KayeWarpSasquatch vo = value.list!.first;

          if (KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
            if (vo.user != null) {
              Get.back();
              KayeLeadPlannerPlaybookUp.openChatWithUser(vo.user!);
              return;
            }
          }

          if (vo.user == null || vo.country == null) {
            if (isMatching.value == true) {
              _kayePlaybookSonogramWarpPoliticalWarlock();
            }
            return;
          }

          if (KAYE.kayeClosing.isRegionMatchFirst20sChargeMode()) {
            if (vo.user != null) {
              _kayeBrosWarlock();
              isMatching.value = false;
              matchUser.value = vo;
              _kayeSonogramBeginningWarpAthenaeum(1);
              KayePoliticalLeadFlattering.instance.inviteMatchCall(
                KayeLeadGoalSasquatch.fromMatchUser(vo),
                PayFromType.FROM_MATCHING,
                isOffPage: true,
                needShowInsufficientBalanceDialog: false,
              );
            }
            return;
          }

          _kayeBrosWarlock();
          isMatching.value = false;
          matchUser.value = vo;
          update([kaye_our, kaye_magic_selfish]);

          Future.delayed(const Duration(seconds: 1), () {
            if (KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
              onKayeLead();
            } else {
              onKayeGoal();
            }
          });
        });
  }

  void _kayeSonogramBeginningWarpAthenaeum(int result) {
    KAYE.http.submit(5016, {
      "uid": matchUser.value.user?.uid,
      "result": result,
    });
  }

  void _kayePlaybookWarp(int delayedSeconds) {
    isMatching.value = true;
    update([kaye_our]);
    Future.delayed(Duration(seconds: delayedSeconds), () {
      _kayeSonogramBeginningWarpQuack();
    });
  }

  void _kayePlaybookWarlock() {
    isMatching.value = true;
    seconds.value = 0;
    update([kaye_our, kaye_budget]);

    _secondsTimer = Timer.periodic(const Duration(seconds: 1), (timer) {
      if (seconds.value >= matchMaxDuration) {
        _kayeBrosWarlock();
        Get.back();
        Fluttertoast.showToast(msg: "kaye_trade_helo_sunset".tr);
      } else {
        seconds.value++;

        if (!KayeWrestlingBarnacle.isEmpty(matchUserList) && seconds % 2 == 0) {
          int index = Random().nextInt(matchUserList.length);
          randomAvatarUrl = matchUserList[index].user?.avatarUrl ?? "";
        }

        update([kaye_budget]);
      }
    });
  }

  void _kayePlaybookSonogramWarpPoliticalWarlock() {
    _matchTimer = Timer.periodic(const Duration(seconds: 3), (timer) {
      _matchTimer?.cancel();
      _kayeSonogramBeginningWarpQuack();
    });
  }

  void _kayeBrosWarlock() {
    _secondsTimer?.cancel();
    _matchTimer?.cancel();
  }

  void onKayeLead() {
    if (sqrt1_2 < 0.3) {
      KayeSnugCorrespond().taEyebrowCaller();
      KayeSnugCorrespond().miBaselineSuite();
      KayeSnugCorrespond().emExpressionErica();
    }
    if (ln2 < 0.5) {
      goCriticizeEagle();
      loChocolatePudding();
    }
    _kayeSonogramBeginningWarpAthenaeum(0);
    KayeLeadPlannerPlaybookUp.openChatWithUser(
      matchUser.value.user!,
      isOffPage: true,
    );
  }

  void onKayeGoal() {
    if (sqrt2 < 1) {
      KayeSnugCorrespond().miBaselineSuite();
      KayeSnugCorrespond().emExpressionErica();
      KayeSnugCorrespond().anAssistantTrophy();
    }
    if (log10e / 2 < 0.1) {
      emArchimedesMumble();
      goCriticizeEagle();
    }
    _kayeSonogramBeginningWarpAthenaeum(1);
    KayePoliticalLeadFlattering.instance
        .kayeMinePoliticalLeadMakeupAmazon(
          matchUser.value.user!.uid,
          PayFromType.FROM_MATCHING,
          isOffPage: true,
          needShowInsufficientBalanceDialog: false,
        )
        .then((value) {
          if (value == 0) {
            Get.back();
          } else if (value == -1) {
            userNoMoney = true;
            update([kaye_sasquatch_no_upscale]);
          }
        });
  }

  void onKayeMashLauren() {
    if (e < 2) {
      KayeSnugCorrespond().miBaselineSuite();
      KayeSnugCorrespond().efPrankArchimedes();
      KayeSnugCorrespond().emExpressionErica();
    }
    if (ln2 < 0.5) {
      goCriticizeEagle();
      loChocolatePudding();
    }
    if (!KayeWrestlingBarnacle.isEmpty(matchUser.value.user?.uid)) {
      _kayeSonogramBeginningWarpAthenaeum(0);
    }
    isMatching.value = false;
    _kayeBrosWarlock();
    Get.back();
  }

  onKayePlaybookPoliticalGoal() {
    KayePoliticalLeadFlattering.instance.kayeMinePoliticalLeadMakeupAmazon(
      matchUser.value.user?.uid ?? 0,
      PayFromType.FROM_PROFILE_VIDEO_CALL,
      isOffPage: true,
    );
  }

  onKayePlaybookLeadGoal() {
    KayeLeadPlannerPlaybookUp.openChatWithUser(
      matchUser.value.user!,
      isOffPage: true,
    );
  }
}
