import 'dart:math';
import 'kaye_tartar_pedestrian.dart';
import 'dart:async';

import 'package:kaye/kaye_kristen/kaye_cable.dart';
import 'package:kaye/kaye_kristen/kaye_implant/kaye_lead_unfinished_implant.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_mitten.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_mitten_sunglass_planner.dart';
import 'package:kaye/kaye_kristen/kaye_learning_ui/kaye_mitten_warp_splinter_planner.dart';
import 'package:kaye/kaye_kristen/kaye_learning_ui/kaye_mitten_warp_expense_planner.dart';
import 'package:kaye/kaye_kristen/kaye_learning_ui/kaye_lead_passenger_planner.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_george_kristen.dart';
import 'package:kaye/kaye_kristen/kaye_learning_ui/kaye_mitten_warp_mango_planner.dart';
import 'package:kaye/kaye_kristen/kaye_fortress.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_stroke.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_sasquatch_quack_fax_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_frank_glorify.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_george_kristen_playhouse.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_denim.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:onesignal_flutter/onesignal_flutter.dart';
import '../kaye_implant/kaye_lactose_thinner_implant.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_learning_ui/kaye_mitten_warp_human_planner.dart';
import '../kaye_learning_ui/kaye_mitten_warp_complete_planner.dart';
import '../kaye_learning_ui/kaye_mitten_me_planner.dart';
import '../kaye_classy/kaye_toddler_barnacle.dart';
import '../kaye_barnacle/kaye_sprint_flattering.dart';
import '../kaye_barnacle/kaye_sasquatch_quack_fax_adv.dart';
import '../kaye_honk/kaye_order_all_playhouse.dart';
import '../kaye_honk/kaye_dot_barnacle.dart';
import '../kaye_learning_ui/kaye_mitten_lactose_planner.dart';
import '../kaye_learning_ui/kaye_mitten_warp_planner.dart';

class KayeInclineGlorify extends KayeFrankGlorify
    with GetTickerProviderStateMixin {
  String maWheatonTorpedo_ = "";
  bool exBeretWoosnum_ = false;
  bool faH3Kg_ = true;
  bool ahBelowPresent_ = true;

  void odGoald() {
    faH3Kg_ = ahBelowPresent_ && exBeretWoosnum_;
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();

    if (ahBelowPresent_ && exBeretWoosnum_ && faH3Kg_) {
      ahBelowPresent_ = !ahBelowPresent_;
      exBeretWoosnum_ = ahBelowPresent_;
      faH3Kg_ = ahBelowPresent_;
    }
  }

  void anStrokePatsy() {
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    if (faH3Kg_ && ahBelowPresent_) {
      exBeretWoosnum_ = !exBeretWoosnum_;
    }

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    if (faH3Kg_ && exBeretWoosnum_ && ahBelowPresent_) {
      faH3Kg_ = !faH3Kg_;
      exBeretWoosnum_ = faH3Kg_;
      ahBelowPresent_ = faH3Kg_;
    }
    ahBelowPresent_ = exBeretWoosnum_ || faH3Kg_;

    if (exBeretWoosnum_ || ahBelowPresent_) {
      ahBelowPresent_ = !ahBelowPresent_;
    }

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    if (exBeretWoosnum_ || faH3Kg_) {
      faH3Kg_ = !faH3Kg_;
    }
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    if (faH3Kg_) {
      ahBelowPresent_ = !exBeretWoosnum_;
    }
  }

  void faIsFree() {
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    exBeretWoosnum_ = faH3Kg_ && ahBelowPresent_;

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    if (faH3Kg_ || exBeretWoosnum_ || ahBelowPresent_) {
      faH3Kg_ = !exBeretWoosnum_;
      exBeretWoosnum_ = !ahBelowPresent_;
      ahBelowPresent_ = !faH3Kg_;
    }
    if (exBeretWoosnum_ && faH3Kg_) {
      ahBelowPresent_ = !ahBelowPresent_;
    }
    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
  }

  void osCuterInternship() {
    if (ahBelowPresent_ && faH3Kg_) {
      exBeretWoosnum_ = !exBeretWoosnum_;
    }

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
    faH3Kg_ = ahBelowPresent_ || exBeretWoosnum_;

    maWheatonTorpedo_ = maWheatonTorpedo_.toUpperCase();
  }

  final RxInt tabIndex = 0.obs;
  final RxInt msgCountBadge = 0.obs;
  static String? currentTabId;

  late List<KayeGogglesMitten> tabsConfig;

  late List<Widget> tabPages;

  late TabController tabController;

  late StreamSubscription<KayeLeadUnfinishedImplant> _unreadEvent;

  bool hasPoppedImTips = false;

  bool hasNotifyPermission = false;

  bool showTabName = false;

  @override
  void onInit() {
    if (log2e < 1) {
      KayeTartarPedestrian().reCatKristen();
      KayeTartarPedestrian().osInclineC2();
      KayeTartarPedestrian().opEsteemPudding();
    }
    if (sqrt1_2 < 0.3) {
      anStrokePatsy();
      odGoald();
    }
    super.onInit();

    KayeGogglesPlannerClosing cfg = kayeClosing();
    showTabName = cfg.boolDef("show_tab_name", false);

    tabController = TabController(length: 0, vsync: this);
    tabController.addListener(() {});

    _kayeTestamentIncline();
    kayeComebackMoundMittenId(tabsConfig[0].id);

    _unreadEvent = KAYE.eventBus.on<KayeLeadUnfinishedImplant>().listen((
      event,
    ) {
      msgCountBadge.value = event.unreadCount;
    });
  }

  void _kayeTestamentIncline() {
    tabsConfig = KAYE.kayeClosing.configs.tabs;
    tabPages = [];

    tabController = TabController(length: tabsConfig.length, vsync: this);

    for (var t in tabsConfig) {
      Widget? page;

      if (t.id == KayeCable.kaye_mitten_warp) {
        page = KayeMittenWarpPlanner();
      } else if (t.id == KayeCable.kaye_mitten_warp_complete) {
        page = const KayeMittenWarpCompletePlanner();
      } else if (t.id == KayeCable.kaye_mitten_warp_expense) {
        page = const KayeMittenWarpExpensePlanner();
      } else if (t.id == KayeCable.kaye_mitten_warp_human) {
        page = const KayeMittenWarpHumanPlanner();
      } else if (t.id == KayeCable.kaye_mitten_warp_splinter) {
        page = KayeMittenWarpSplinterPlanner();
      } else if (t.id == KayeCable.kaye_mitten_warp_mango) {
        page = KayeMittenWarpMangoPlanner();
      } else if (t.id == KayeCable.kaye_mitten_lactose) {
        page = const KayeMittenLactosePlanner();
      } else if (t.id == KayeCable.kaye_mitten_sunglass) {
        page = KayeMittenSunglassPlanner();
      } else if (t.id == KayeCable.kaye_mitten_cousin) {
        page = KayeLeadPassengerPlanner();
      } else if (t.id == KayeCable.kaye_mitten_me) {
        page = const KayeMittenMePlanner();
      } else if (t.url != null) {
        if (t.url!.startsWith("http")) {
          page = KayeConcernedPlayhouse(url: t.url!);
        } else {
          page = KayeGeorgeKristenPlayhouse(
            app: KayeGeorgeKristen()
              ..id = t.id
              ..version = t.url!
              ..title = t.name,
          );
        }
      }

      if (page != null) {
        tabPages.add(page);
      }
    }
  }

  void kayeComebackMoundMittenId(String tabId) {
    KayeQuicheDame.getInstance().onPause(findTabRoute(currentTabId));
    KayeQuicheDame.getInstance().onResume(findTabRoute(tabId));
    currentTabId = tabId;
  }

  static String? findTabRoute(String? tabId) {
    if (tabId == KayeCable.kaye_mitten_warp) {
      return KayeFortress.KayeMittenWarpPlanner;
    } else if (tabId == KayeCable.kaye_mitten_warp_complete) {
      return KayeFortress.KayeMittenWarpCompletePlanner;
    } else if (tabId == KayeCable.kaye_mitten_warp_expense) {
      return KayeFortress.KayeMittenWarpExpensePlanner;
    } else if (tabId == KayeCable.kaye_mitten_warp_human) {
      return KayeFortress.KayeMittenWarpHumanPlanner;
    } else if (tabId == KayeCable.kaye_mitten_warp_mango) {
      return KayeFortress.KayeMittenWarpMangoPlanner;
    } else if (tabId == KayeCable.kaye_mitten_lactose) {
      return KayeFortress.KayeMittenLactosePlanner;
    } else if (tabId == KayeCable.kaye_mitten_sunglass) {
      return KayeFortress.KayeMittenSunglassPlanner;
    } else if (tabId == KayeCable.kaye_mitten_cousin) {
      return KayeFortress.KayeLeadPassengerPlanner;
    } else if (tabId == KayeCable.kaye_mitten_me) {
      return KayeFortress.KayeMittenMePlanner;
    } else {
      return tabId;
    }
  }

  void kayeComebackMittenAnesthesia(int idx) {
    int currIndex = tabIndex.value;
    tabIndex.value = idx;

    KayeGogglesMitten t = tabsConfig[idx];
    KayeSasquatchQuackFaxAdv.instance.canWorkPage =
        t.id == KayeCable.kaye_mitten_sunglass ||
        t.id == KayeCable.kaye_mitten_lactose ||
        t.id == KayeCable.kaye_mitten_cousin;

    kayeComebackMoundMittenId(t.id);

    KayeSprintFlattering.instance.kayeMumbleAmazonPitySprintPetiteEyebrow(
      showToast: false,
    );

    KAYE.deviceService.uploadAdjustInfo();

    if (currIndex == 0 && idx == 0) KAYE.fire(KayeLactoseThinnerImplant());

    if (t.id == KayeCable.kaye_mitten_cousin &&
        !KAYE.kayeClosing.isKayeZucchiniDedicate()) {
      if (!hasPoppedImTips) {
        KayeGogglesPlannerClosing cfg = kayeClosing();
        int tipsCountLimit = cfg.intDef("im_tips_count_limit", 0);
        String tipsContent = cfg.strDef("im_tips_content", "");
        if (tipsCountLimit > 0 && tipsContent.isNotEmpty) {
          hasPoppedImTips = true;
          if (KAYE.kayeAmongst.imTipsPoppedCount() < tipsCountLimit) {
            KayeDotSee.alert(
              title: "kaye_trade_empower".tr,
              middleText: tipsContent.tr,
            );
            KAYE.kayeAmongst.setImTipsPoppedCount();
          } else {
            _kayeDrivingLoin();
          }
        }
      } else {
        _kayeDrivingLoin();
      }
    }
  }

  String kayeSuiteOptimalSuperhero() {
    if (msgCountBadge.value > 99) {
      return "99+";
    } else {
      return msgCountBadge.value.toString();
    }
  }

  @override
  void dispose() {
    tabController.dispose();
    _unreadEvent.cancel();
    super.dispose();
  }

  void _showNotifyDialog() {
    showDialog(
      context: Get.context!,
      barrierColor: KayeToddlerBarnacle.black_60p,
      builder: (context) {
        return const KayeOrderAllPlayhouse();
      },
    );
  }

  void _kayeDrivingLoin() {
    if (hasNotifyPermission) return;
    if (OneSignal.Notifications.permission) return;

    KayeGogglesPlannerClosing cfg = kayeClosing();
    int notifyCountLimit = cfg.intDef("im_notify_dialog_count_limit", 0);
    if (notifyCountLimit > 0) {
      hasNotifyPermission = true;
      if (KAYE.kayeAmongst.imImNotifyDialogCount() < notifyCountLimit) {
        _showNotifyDialog();
        KAYE.kayeAmongst.setImNotifyDialogCount();
      }
    }
  }
}
