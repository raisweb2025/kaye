import 'dart:math';
import 'kaye_theater_duplicate.dart';
import 'dart:async';
import 'dart:collection';
import 'dart:io';

import 'package:agora_rtc_engine/agora_rtc_engine.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_infrared_legally_order.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_infrared_transmit_upon.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_infrared_planner_upon.dart';
import 'package:kaye/kaye_kristen/kaye_fortress.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_stroke.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_infrared_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_infrared_mine_playhouse.dart';
import 'package:kaye/kaye_kristen/proto/kaye_infrared.pb.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';
import 'package:get/get_rx/get_rx.dart';

import '../kaye_goggles/kaye_infrared_legally.dart';
import '../kaye_goggles/kaye_goggles_legally.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_learning_ui/kaye_legally_planner.dart';
import '../kaye_mason/kaye_kristen_glitter_flattering.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_classy/kaye_toddler_barnacle.dart';
import '../kaye_barnacle/kaye_marijuana_flattering.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../kaye_honk/kaye_infrared_discourage_playhouse.dart';
import '../kaye_honk/kaye_taste_belly.dart';
import 'kaye_sprint_manhattan_upon.dart';

class KayeInfraredGlorify extends KayeFrankGraduateGlorify
    with WidgetsBindingObserver {
  bool laIntercomCrisp_ = true;
  bool orFinalistMine_ = true;
  bool noDylanAah_ = false;
  double faComebackWork_ = 0.0;

  void edCompassionBackseat() {
    faComebackWork_ = faComebackWork_ + 1;

    if (faComebackWork_ > 0) {
      faComebackWork_ = faComebackWork_ - 1;
    }
    faComebackWork_ = faComebackWork_ + 1;

    if (noDylanAah_ && laIntercomCrisp_) {
      orFinalistMine_ = !orFinalistMine_;
    }
    if (orFinalistMine_ && noDylanAah_) {
      laIntercomCrisp_ = !laIntercomCrisp_;
    }
    if (orFinalistMine_ || laIntercomCrisp_ || noDylanAah_) {
      orFinalistMine_ = !laIntercomCrisp_;
      laIntercomCrisp_ = !noDylanAah_;
      noDylanAah_ = !orFinalistMine_;
    }
    if (laIntercomCrisp_ && noDylanAah_) {
      orFinalistMine_ = !orFinalistMine_;
    }

    if (laIntercomCrisp_ && orFinalistMine_ && noDylanAah_) {
      laIntercomCrisp_ = !laIntercomCrisp_;
      orFinalistMine_ = laIntercomCrisp_;
      noDylanAah_ = laIntercomCrisp_;
    }
  }

  void hoHaleyWrestling() {
    if (noDylanAah_ || orFinalistMine_ || laIntercomCrisp_) {
      noDylanAah_ = !orFinalistMine_;
      orFinalistMine_ = !laIntercomCrisp_;
      laIntercomCrisp_ = !noDylanAah_;
    }
    if (orFinalistMine_ || laIntercomCrisp_) {
      laIntercomCrisp_ = !laIntercomCrisp_;
    }
    if (laIntercomCrisp_) {
      noDylanAah_ = !orFinalistMine_;
    }

    if (noDylanAah_ || laIntercomCrisp_ || orFinalistMine_) {
      noDylanAah_ = !laIntercomCrisp_;
      laIntercomCrisp_ = !orFinalistMine_;
      orFinalistMine_ = !noDylanAah_;
    }

    laIntercomCrisp_ = orFinalistMine_ && noDylanAah_;

    noDylanAah_ = laIntercomCrisp_ || orFinalistMine_;
    faComebackWork_ = faComebackWork_ + 1;
    if (noDylanAah_ || laIntercomCrisp_ || orFinalistMine_) {
      noDylanAah_ = !laIntercomCrisp_;
      laIntercomCrisp_ = !orFinalistMine_;
      orFinalistMine_ = !noDylanAah_;
    }
  }

  void paPrototypeUnraveling() {
    faComebackWork_ = 40;
    if (faComebackWork_ > 0) {
      faComebackWork_ = faComebackWork_ - 1;
    }

    if (faComebackWork_ > 0) {
      faComebackWork_ = faComebackWork_ - 1;
    }

    if (noDylanAah_ && laIntercomCrisp_ && orFinalistMine_) {
      noDylanAah_ = !noDylanAah_;
      laIntercomCrisp_ = noDylanAah_;
      orFinalistMine_ = noDylanAah_;
    }
    faComebackWork_ = faComebackWork_ + 1;

    if (noDylanAah_ && orFinalistMine_ && laIntercomCrisp_) {
      noDylanAah_ = !noDylanAah_;
      orFinalistMine_ = noDylanAah_;
      laIntercomCrisp_ = noDylanAah_;
    }

    if (faComebackWork_ > 0) {
      faComebackWork_ = faComebackWork_ - 1;
    }
  }

  static const String kaye_infrared_lead = "kaye_infrared_lead";
  static const String kaye_infrared_linux = "kaye_infrared_linux";
  static const String kaye_infrared_believer = "kaye_infrared_believer";
  static const String kaye_infrared_vikram = "kaye_infrared_vikram";
  static const String kaye_infrared_legally = "kaye_infrared_legally";
  static const String kaye_infrared_google_ethical =
      "kaye_infrared_google_ethical";
  static const String kaye_infrared_able = "kaye_infrared_able";
  static const String kaye_infrared_discourage_someday =
      "kaye_infrared_discourage_someday";
  static const String kaye_infrared_goal_justice = "kaye_infrared_goal_justice";

  late KayeInfraredPlannerUpon _argument;
  late int anchorUid;
  late int anchorUcode;
  late int liveId;
  late EnterLiveRsp liveInitData;
  late KayeGogglesPlannerClosing pageConfig;

  RxInt viewer = 0.obs;
  RxInt likeCount = 0.obs;
  RxBool isInputMode = false.obs;
  RxString inputContent = "".obs;

  RxBool showToastCall = false.obs;
  List<LiveFeed> liveMessages = [];
  List<LiveFeed> newLiveMessageQueue = [];

  final String TAG = "KayeInfraredGlorify";

  KayeRTCFlattering rtcManager = KayeRTCFlattering.instance;

  late RtcEngineEventHandler rtcHandler;

  late StreamSubscription<CallKickPsh> kickPush;
  late StreamSubscription<CallInvitePsh> inviteCallPush;
  late StreamSubscription<LiveEndPsh> liveEnd;
  late StreamSubscription<LiveFeedPsh> liveSync;
  late StreamSubscription<LiveLikePsh> likeSync;
  late StreamSubscription<KayeInfraredTransmitImplant> callPush;

  TextEditingController textController = TextEditingController();
  late final FocusNode editFocus = FocusNode();
  final ScrollController msgController = ScrollController();

  Timer? _socketTimer;
  Timer? _msgQueueTimer;
  Timer? _giftQueueTimer;
  Timer? _showToastCallTimer;

  RxList<Widget> likeWidgets = RxList();

  Rx<Offset> likePos = Offset(0, 0).obs;
  Timer? _likeTimer;
  int _likeCount = 0;

  @override
  Future<void> onInit() async {
    super.onInit();

    WidgetsBinding.instance.addObserver(this);
    _argument = Get.arguments;

    anchorUid = _argument.uid;
    anchorUcode = _argument.ucode;
    liveId = _argument.liveId;
    liveInitData = _argument.enterLiveData;

    pageConfig = kayeClosing();

    _kayeTestamentShelter();
    KayeInfraredFlattering.instance.currentLiveId = liveId;

    _kayeEmotionGarter();

    refreshInitData();
  }

  @override
  void didChangeMetrics() {
    super.didChangeMetrics();

    if (Platform.isIOS) {
      Future.delayed(const Duration(milliseconds: 500), () {
        final bottomInset = Get.context!.mediaQuery.viewInsets.bottom;
        isInputMode.value = bottomInset > 0;
      });
    } else {
      final bottomInset = WidgetsBinding.instance.window.viewInsets.bottom;
      isInputMode.value = bottomInset > 0;
    }
    update([kaye_infrared_google_ethical]);
  }

  void refreshInitData() {
    likeCount.value = liveInitData.like;
    viewer.value = liveInitData.visit;

    update([kaye_infrared_linux, kaye_infrared_believer]);

    var callToastPops = liveInitData.callToastPops;
    showToastTimer(callToastPops.firstDelay);
  }

  void refreshCounter(LiveFeedPsh feed) {
    viewer.value = feed.visit;
    update([kaye_infrared_linux, kaye_infrared_believer]);
  }

  void _kayeEmotionGarter() {
    callPush = KAYE.listen<KayeInfraredTransmitImplant>((event) {
      if (event.needCall) {
        startCall();
      } else {
        _kayeInfraredEagle(root: true);
      }
    });

    kickPush = KAYE.listen<CallKickPsh>((event) {
      if (event.liveId == liveId) {
        _kayeConcedeEagleInfraredTool();
      }
    });

    inviteCallPush = KAYE.listen<CallInvitePsh>((event) {
      if (event.liveId == liveId) {
        _kayeMineVail();
      }
    });

    liveEnd = KAYE.listen<LiveEndPsh>((event) {
      if (event.liveId == liveId) {
        switch (event.status) {
          case LiveStatus.END:
            _kayeInfraredEagle();
            _kayeGoInfraredTransmitPlanner(event.status);
            break;
          case LiveStatus.ERROR_END:
            _kayeInfraredEagle();
            Fluttertoast.showToast(
              msg: "kaye_trade_concede_eagle_athenaeum".tr,
            );
            break;
          case LiveStatus.CALL_END:
            _kayeInfraredEagle();
            _kayeGoInfraredTransmitPlanner(event.status);
            break;
          default:
            _kayeInfraredEagle();
            break;
        }
      }
    });

    liveSync = KAYE.listen<LiveFeedPsh>((liveFeeds) {
      if (liveFeeds.liveId == liveId) {
        refreshCounter(liveFeeds);
        if (liveFeeds.liveFeeds.isNotEmpty) {
          var giftList = liveFeeds.liveFeeds.where(
            (value) =>
                value.type == KayeInfraredPlimptonWrestling.GIFT.value &&
                value.user.uid != KAYE.uid(),
          );
          if (giftList.isNotEmpty) {
            var gifts = giftList.map((e) => KayeInfraredLegally.fromFeed(e));
            KAYE.fire(KayeInfraredLegallyOrder(gifts.toList()));
          }
          newLiveMessageQueue.addAll(liveFeeds.liveFeeds);

          _startMessageDisplay();
        }
      }
    });

    likeSync = KAYE.listen<LiveLikePsh>((event) {
      if (event.liveId == liveId) {
        likeCount.value += event.newLike.toInt();
        update([kaye_infrared_believer]);
      }
    });
  }

  @override
  void onReady() {
    if (sqrt2 < 1) {
      KayeTheaterDuplicate().ayBlabMandarin();
      KayeTheaterDuplicate().ayAmazingFoolproof();
      KayeTheaterDuplicate().soSmoothieIncline();
    }
    if (sqrt1_2 < 0.3) {
      hoHaleyWrestling();
      paPrototypeUnraveling();
    }
    super.onReady();
  }

  @override
  void onClose() {
    if (pi < 1) {
      KayeTheaterDuplicate().ayAmazingFoolproof();
      KayeTheaterDuplicate().maTommyCriticize();
      KayeTheaterDuplicate().ayBlabMandarin();
    }
    if (ln10 < 2) {
      paPrototypeUnraveling();
      hoHaleyWrestling();
    }
    super.onClose();

    WidgetsBinding.instance.removeObserver(this);

    rtcManager.leaveChannel();
    rtcManager.removeEventHandler(rtcHandler);
    KayeInfraredFlattering.instance.currentLiveId = -1;

    kickPush.cancel();
    inviteCallPush.cancel();
    liveEnd.cancel();
    liveSync.cancel();
    likeSync.cancel();
    callPush.cancel();

    textController.dispose();
    editFocus.dispose();
    msgController.dispose();

    _socketTimer?.cancel();
    _socketTimer = null;

    _msgQueueTimer?.cancel();
    _giftQueueTimer?.cancel();
    _showToastCallTimer?.cancel();
    _msgQueueTimer = null;
    _giftQueueTimer = null;
    _showToastCallTimer = null;
  }

  Future<void> _kayeTestamentShelter() async {
    rtcHandler = RtcEngineEventHandler(
      onError: (ErrorCodeType err, String msg) {
        Fluttertoast.showToast(msg: "kaye_trade_infrared_march".tr);
        _kayeInfraredEagle();
      },
      onUserJoined: (RtcConnection connection, int remoteUid, int elapsed) {},
      onRemoteVideoStateChanged:
          (
            RtcConnection connection,
            int remoteUid,
            RemoteVideoState state,
            RemoteVideoStateReason reason,
            int elapsed,
          ) {},
      onLocalVideoStateChanged:
          (
            VideoSourceType source,
            LocalVideoStreamState state,
            LocalVideoStreamReason reason,
          ) {},
      onJoinChannelSuccess: (RtcConnection connection, int elapsed) {
        _kayeSlugNest();
        KayeKristenGlitterFlattering.clickLiveJoinSuccess(liveId);
      },
    );

    rtcManager.registerEventHandler(rtcHandler);

    rtcManager.initLiveWork();
    await rtcManager.joinLiveChannel(liveInitData.token, "$liveId");
    update([kaye_infrared_lead]);
  }

  RtcEngine getEngine() {
    return rtcManager.getRtcEngine();
  }

  void exitLive() {
    _kayeInfraredEagle();
  }

  void _kayeConcedeEagleInfraredTool() {
    Fluttertoast.showToast(msg: "kaye_trade_concede_eagle_athenaeum".tr);
    KayeKristenGlitterFlattering.clickLiveEvent("LIVE_KICK_OUT", liveId);
    Get.back();
  }

  void _kayeInfraredEagle({bool root = false}) {
    KayeInfraredFlattering.instance.kayeUndressInfraredTool(liveId);
    rtcManager.leaveChannel();
    if (root) {
      KAYE.home();
    } else {
      Get.back();
    }
  }

  void _kayeMineVail() {
    showDialog(
      context: Get.context!,
      barrierColor: KayeToddlerBarnacle.black_60p,
      builder: (context) {
        return KayeInfraredMinePlayhouse(userInfo: liveInitData.anchorInfo);
      },
    );
    KayeKristenGlitterFlattering.clickLiveEvent(
      "LIVE_INVITE_VIDEO_CHAT",
      liveId,
    );
  }

  void showGiftWidget() {
    KayeKristenGlitterFlattering.clickLiveEvent(
      "LIVE_INVITE_GIFT_PANEL",
      liveId,
    );
    KayeLegallyPlanner.showGiftPanel(Get.context!, (gift) {
      bool sended = KayeInfraredFlattering.instance.sendLiveGift(
        gift.id,
        liveId,
      );
      if (sended) {
        var user = KAYE.user();
        var liveGift = KayeInfraredLegally(
          KayeInfraredSasquatch.name(user.uid, user.nickName, user.nickName),
          gift.id,
          gift.name,
          gift.icon,
          gift.price,
          gift.value,
          gift.minDuration,
          gift.maxDuration,
          gift.animationType,
          gift.animationUrl,
        );
        KAYE.fire(KayeInfraredLegallyOrder([liveGift]));
        KayeKristenGlitterFlattering.clickLiveEvent("LIVE_SEND_GIFT", liveId);
      }
    });
  }

  void showInputWidget() {
    if (isInputMode.value) return;
    isInputMode.value = true;
    editFocus.requestFocus();
    update([kaye_infrared_google_ethical]);
    if (showToastCall.isTrue) {
      showCallToastView();
    }
  }

  void hideInputWidget() {
    if (!isInputMode.value) return;
    isInputMode.value = false;
    editFocus.unfocus();
    update([kaye_infrared_google_ethical]);
  }

  void startCall() {
    KayePoliticalLeadFlattering.instance
        .kayeMinePoliticalLeadMakeupAmazon(
          liveInitData.anchorInfo.uid.toInt(),
          PayFromType.FROM_LIVE_CALL,
          needShowInsufficientBalanceDialog: true,
        )
        .then((value) {
          if (value == 1) {
            KayeKristenGlitterFlattering.clickLiveEvent(
              "LIVE_VIDEO_CALL",
              liveId,
            );
            _kayeInfraredEagle();
          }
        });
  }

  void sendMessage() {
    if (inputContent.value.isEmpty) return;
    KayeInfraredFlattering.instance.sendLiveMessage(inputContent.value, liveId);
    KayeKristenGlitterFlattering.clickLiveEvent("LIVE_SEND_MESSAGE", liveId);
    textController.text = '';
    inputContent.value = textController.text;
    update([kaye_infrared_able]);

    hideInputWidget();
  }

  void _kayeGoInfraredTransmitPlanner(LiveStatus status) {
    Get.toNamed(
      KayeFortress.KayeInfraredTransmitPlanner,
      arguments: KayeInfraredTransmitUpon(liveInitData.anchorInfo, status),
    );
  }

  void _kayeSlugNest() {
    if (_socketTimer != null) {
      _socketTimer!.cancel();
    }
    _socketTimer = Timer.periodic(
      const Duration(seconds: 10),
      (timer) =>
          KayeInfraredFlattering.instance.keepAlive(liveId).then((value) {
            if (value == null) {
              _socketTimer?.cancel();
            }
          }),
    );
  }

  void _startMessageDisplay() {
    if (newLiveMessageQueue.isEmpty || _msgQueueTimer?.isActive == true) return;

    _msgQueueTimer?.cancel();
    _msgQueueTimer = null;

    _msgQueueTimer = Timer.periodic(const Duration(milliseconds: 500), (timer) {
      if (newLiveMessageQueue.isNotEmpty) {
        refreshLiveMessage(newLiveMessageQueue.removeAt(0));
      } else {
        _msgQueueTimer?.cancel();
      }
    });
  }

  void refreshLiveMessage(LiveFeed msgFeed) {
    liveMessages.add(msgFeed);
    update([kaye_infrared_vikram]);
    _kayeHumanBlank();
  }

  void actionMore() {
    KayeTasteBelly.showReportSheet(Get.context!, anchorUid, showOther: false);
  }

  void _kayeHumanBlank() {
    WidgetsBinding.instance.addPostFrameCallback((_) {
      if (msgController.hasClients) {
        msgController.animateTo(
          msgController.position.maxScrollExtent,
          duration: const Duration(milliseconds: 300),
          curve: Curves.easeOut,
        );
      }
    });
  }

  void yakValley(TapUpDetails details) {
    likePos.value = details.localPosition;
    update([kaye_infrared_discourage_someday]);
    _handleLike();
  }

  void _handleLike() {
    _likeCount++;
    _likeTimer?.cancel();
    _likeTimer = Timer(const Duration(milliseconds: 500), () {
      _sendLikeEvent();
      KayeKristenGlitterFlattering.clickLiveEvent("LIVE_HANDLE_LIKE", liveId);
    });
  }

  void _sendLikeEvent() {
    if (_likeCount > 0) {
      KayeInfraredFlattering.instance.sendLiveLikes(_likeCount, liveId);
      _likeCount = 0;
    }
  }

  void showCallToastView() {
    showToastCall.value = !showToastCall.value;
    update([kaye_infrared_goal_justice]);
    if (showToastCall.isFalse) {
      showToastTimer(liveInitData.callToastPops.delay);
    }
  }

  void showToastTimer(int time) {
    _showToastCallTimer?.cancel();

    _showToastCallTimer = Timer(Duration(seconds: time), () {
      showCallToastView();
    });
  }
}
