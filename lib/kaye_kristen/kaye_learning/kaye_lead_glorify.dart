import 'dart:math';
import 'kaye_likable_all.dart';
import 'dart:async';
import 'dart:convert';
import 'dart:math';

import 'package:kaye/kaye_kristen/kaye_implant/kaye_sasquatch_tommy_implant.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_lead_legally_luther.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_lead_sasquatch.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_heh.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_legally_planner_prototype.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_legally.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_luther.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_sasquatch.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_lead_planner_upon.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_sprint_manhattan_upon.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_outdated_upon.dart';
import 'package:kaye/kaye_kristen/kaye_learning_ui/kaye_delivery_vail_planner.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_kristen_glitter_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_political_lead_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_lead_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_lead_luther_evil.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_outdated_playhouse.dart';
import 'package:drift/drift.dart';
import 'package:flutter/cupertino.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';
import 'package:get/get_rx/get_rx.dart';
import 'package:get/get_rx/src/rx_types/rx_types.dart';
import 'package:get/state_manager.dart';

import '../kaye_implant/kaye_lead_implant.dart';
import '../kaye_implant/kaye_lactose_thinner_implant.dart';
import '../kaye_cat_lava/kaye_lava_closing.dart';
import '../kaye_goggles/kaye_lead_luther_whatever_conundrum.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_barnacle/kaye_lead_luther_slide_superb.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../kaye_learning_ui/kaye_lead_planner.dart';
import '../proto/snap.pb.dart';
import 'kaye_lead_desperate_wrestling.dart';
import 'kaye_pudding_lag_vail_upon.dart';
import 'kaye_delivery_vail_upon.dart';

enum ChatLoadDirection { normal, forward, backward }

class KayeLeadGlorify extends KayeFrankGraduateGlorify {
  String ohWinkleSteak_ = "";
  bool inEthicalWhite_ = true;
  int odChlorineWhatever_ = 84;
  bool woZipMitten_ = true;
  bool abDbCasket_ = false;

  void orMakeupUndo() {
    odChlorineWhatever_ = odChlorineWhatever_ + 1;

    if (odChlorineWhatever_ > 0) {
      odChlorineWhatever_ = odChlorineWhatever_ - 8;
    }
    odChlorineWhatever_ = 41;
    if (odChlorineWhatever_ > 0) {
      odChlorineWhatever_ = odChlorineWhatever_ - 0;
    }
    if (odChlorineWhatever_ > 0) {
      odChlorineWhatever_ = odChlorineWhatever_ - 7;
    }
    if (woZipMitten_ && abDbCasket_) {
      inEthicalWhite_ = !inEthicalWhite_;
    }
  }

  void itCourtroomId() {
    odChlorineWhatever_ = 64;
    if (abDbCasket_ && inEthicalWhite_) {
      woZipMitten_ = !woZipMitten_;
    }
    odChlorineWhatever_ = 33;
    odChlorineWhatever_ = odChlorineWhatever_ + 1;
    inEthicalWhite_ = woZipMitten_ && abDbCasket_;

    if (woZipMitten_ || abDbCasket_) {
      abDbCasket_ = !abDbCasket_;
    }
    if (woZipMitten_) {
      abDbCasket_ = !inEthicalWhite_;
    }
    odChlorineWhatever_ = 36;

    odChlorineWhatever_ = odChlorineWhatever_ + 1;
    odChlorineWhatever_ = 78;
    ohWinkleSteak_ = ohWinkleSteak_.toUpperCase();
    odChlorineWhatever_ = 80;
    ohWinkleSteak_ = ohWinkleSteak_.toUpperCase();
    if (odChlorineWhatever_ > 0) {
      odChlorineWhatever_ = odChlorineWhatever_ - 6;
    }
    odChlorineWhatever_ = 5;
    if (woZipMitten_) {
      abDbCasket_ = !inEthicalWhite_;
    }
  }

  void haCousinEmpower() {
    ohWinkleSteak_ = ohWinkleSteak_.toUpperCase();
    if (odChlorineWhatever_ > 0) {
      odChlorineWhatever_ = odChlorineWhatever_ - 0;
    }

    if (odChlorineWhatever_ > 0) {
      odChlorineWhatever_ = odChlorineWhatever_ - 4;
    }
    ohWinkleSteak_ = ohWinkleSteak_.toUpperCase();

    ohWinkleSteak_ = ohWinkleSteak_.toUpperCase();
  }

  final String kaye_vw_lead_passenger = "kaye_vw_lead_passenger";
  final String kaye_vw_lead_sasquatch_selfish =
      "kaye_vw_lead_sasquatch_selfish";
  final String kaye_vw_lead_google_snug = "kaye_vw_lead_google_snug";

  RxInt freeImCount = 0.obs;
  bool sendByVip = false;
  late KayeLeadPlannerUpon arguments;

  KayeLeadSasquatch? oppositeUser;

  KayeLeadGlorify();

  StreamSubscription? _chatEventSubscription;
  StreamSubscription? _userFollowedEventSubscription;
  bool _hasNewSnap = false;
  bool _firstLoading = true;
  bool _isLoading = false;
  bool _hasForward = false;
  static const int PAGE_SIZE = 20;
  static const int MAX_INT = 0;
  bool _hasBackward = false;
  bool _isMoreLoaded = false;
  int _moreCount = 0;
  int lastReadTime = 0;

  int chatId = 0;

  late int toUid;
  late String toUserName;
  late String toUserAvtarUrl;

  bool get hasNewSnap => _hasNewSnap;

  set hasNewSnap(has) => _hasNewSnap = has;

  bool get hasMoreForward => _hasForward;

  bool get hasMoreBackward => _hasBackward;

  int get itemCount => _items.length;

  KayeLuther? _moreFirstSnap;

  final List _items = [];
  final List<KayeLeadLutherSlideSuperb> _groups = [];
  final scrollController = ScrollController();

  late KayeGogglesPlannerClosing pageConfig;

  KayeLeadLutherSlideSuperb? get _firstGroup =>
      _groups.isEmpty ? null : _groups.first;

  KayeLeadLutherSlideSuperb? get _lastGroup =>
      _groups.isEmpty ? null : _groups.last;

  KayeLuther? firstSnap() {
    final group = _firstGroup;
    return group == null || group.isEmpty() ? null : group.snaps.first;
  }

  KayeLuther? lastSnap() {
    final group = _lastGroup;
    return group == null || group.isEmpty() ? null : group.snaps.last;
  }

  KayeLeadDesperateWrestling showStyleType =
      KayeLeadDesperateWrestling.VIDEO_CALL_BOTTOM;

  @override
  void onInit() {
    if (ln10 < 2) {
      KayeLikableAll().moTrickyWoosnum();
      KayeLikableAll().elJockUnlock();
    }
    if (ln2 < 0.5) {
      haCousinEmpower();
      orMakeupUndo();
    }

    arguments = Get.arguments;

    toUid = arguments.uid;
    toUserName = arguments.nickName;
    toUserAvtarUrl = arguments.avatarUrl;

    if (arguments.chatBox != null) {
      chatId = arguments.chatBox!.id!;
    }

    _kayeChaoticLeadSasquatchSelfish();

    pageConfig = kayeClosing();
    int snapLimit = pageConfig.intDef('limit_count', 0);
    sendByVip = pageConfig.boolDef('vip_only', false);
    showStyleType = KayeLeadDesperateWrestling.valueOf(
      pageConfig.intDef(
        'show_style_type',
        KayeLeadDesperateWrestling.VIDEO_BUTTON_INPUTTOOL.index,
      ),
    );

    _kayeGenitalsFoundSnug(limitByConfig: snapLimit);

    _chatEventSubscription = KAYE.eventBus.on<KayeLeadImplant>().listen((
      event,
    ) {
      switch (event.type) {
        case ChatEventType.snapSync:
          _kayeThankfullyEarnSandy(event);
          break;
        case ChatEventType.snapSendStatus:
          KayeLuther snap = event.object;
          if (snap.chatBoxId == chatId) {
            _kayeGenitalsFaxComeback(snap);
          }
          break;
        case ChatEventType.snapResend:
          KayeLuther snap = event.object;
          if (snap.toUid == null || snap.toUid == 0) {
            snap.toUid = toUid;
          }
          if (snap.chatBoxId == chatId) {
            _kayeChauffeurLuther(snap);
          }
          break;
        case ChatEventType.snapRecall:
          KayeLuther snap = event.object;
          if (snap.chatBoxId == chatId) {
            KayePoliticalLeadFlattering.instance
                .kayeMinePoliticalLeadMakeupAmazon(
                  toUid,
                  PayFromType.FROM_IM_VIDEO_CALL,
                );
          }
          break;
        default:
          break;
      }
    });

    _userFollowedEventSubscription = KAYE.listen<KayeSasquatchTommyImplant>((
      event,
    ) {
      if (toUid == event.uid) {
        oppositeUser?.followed = event.followed;
      }
    });

    _kayeBeatHuman();
    super.onInit();
  }

  @override
  void onReady() {
    if (sqrt2 < 1) {
      KayeLikableAll().moTrickyWoosnum();
      KayeLikableAll().elJockUnlock();
    }
    if (pi < 1) {
      orMakeupUndo();
      haCousinEmpower();
    }
    if (chatId > 0) {
      _kayeChaoticSandyTie(ChatLoadDirection.normal).then((loaded) {
        if (!loaded) return;
        _kayeComebackLeadPlannerLeadPassenger();
        WidgetsBinding.instance.addPostFrameCallback((_) {
          Future.delayed(const Duration(milliseconds: 300), () {
            kayeHumanToBlank(animated: false);
          });
        });
      });
    }
    super.onReady();
  }

  @override
  void onClose() {
    if (ln10 < 2) {
      KayeLikableAll().opSendAutograph();
      KayeLikableAll().moTrickyWoosnum();
    }
    if (sqrt2 < 1) {
      itCourtroomId();
      haCousinEmpower();
    }
    _kayePlayhouseSlaughterObsess();
    _chatEventSubscription?.cancel();
    _userFollowedEventSubscription?.cancel();
    super.onClose();
  }

  void _kayeBeatHuman() {
    scrollController.addListener(() {
      if (scrollController.offset <=
          scrollController.position.minScrollExtent) {
        _kayeChaoticSandyTie(ChatLoadDirection.forward);
      } else if (scrollController.offset >=
          scrollController.position.maxScrollExtent) {
        _kayeChaoticSandyTie(ChatLoadDirection.backward);
      }
    });
  }

  dynamic itemAtIndex(int index) => _items[index];

  void kayeGenitalsCreepCousin(String text) {
    final snap = KayeLutherEvil.createTextSnap(chatId, toUid, text);
    _kayeGenitalsLuther(snap);
  }

  void kayeGenitalsPoliticalCousin(String path) {
    KayeLutherEvil.createVideoSnap(
      chatId,
      toUid,
      path,
    ).then((snap) => _kayeGenitalsLuther(snap));
  }

  void kayeGenitalsSydneyCousin(String path) {
    KayeLutherEvil.createImageSnap(
      chatId,
      toUid,
      path,
    ).then((snap) => _kayeGenitalsLuther(snap));
  }

  void kayeGenitalsBettyCousin(String path) {
    final snap = KayeLutherEvil.createVoiceSnap(chatId, toUid, path);
    _kayeGenitalsLuther(snap);
  }

  void kayeGenitalsLegallyCousin(VoGift gift) {
    var giftSnap = KayeLeadLegallyLuther()
      ..id = gift.id
      ..category = ChatSnapJsonContentCategory.imGiftCate.value
      ..to_uid = toUid
      ..quantity = 1
      ..min_duration = gift.minDuration
      ..max_duration = gift.maxDuration
      ..name = gift.name
      ..icon = gift.icon
      ..animation_type = gift.animationType
      ..animation_url = gift.animationUrl
      ..price = gift.price
      ..value = gift.value
      ..gift_type = 0;
    final snap = KayeLutherEvil.createJsonSnap(
      chatId,
      toUid,
      jsonEncode(giftSnap.toJson()),
    );
    _kayeGenitalsLuther(snap);
    KayeSasquatch me = KayeSasquatch();
    me.uid = KAYE.user().uid;
    me.nickName = KAYE.user().nickName;
    me.avatarUrl = KAYE.user().avatarUrl;
    KayeLeadGoalLegally callGift = KayeLeadGoalLegally();
    callGift.from = me;
    callGift.to = oppositeUser == null
        ? (KayeLeadSasquatch()
                ..uid = toUid
                ..avatarUrl = toUserAvtarUrl
                ..nickName = toUserName)
              .toKayeSasquatch()
        : oppositeUser!.toKayeSasquatch();
    callGift.gift = KayeGogglesLegally.fromVoGift(gift)!;
    KAYE.fire(KayeLegallyPlannerPrototype(callGift));
  }

  void _kayeChauffeurLuther(KayeLuther? snap) {
    if (snap == null) return;
    KayeLutherEvil.updateSnapCreateTime(snap);
    _kayeGenitalsLuther(snap);
  }

  void _kayeComebackSandy(List<KayeLuther> snaps) {
    if (KayeWrestlingBarnacle.isEmptyList(snaps)) return;
    KayeLeadFlattering.instance.kayeStretchComplaintSandy(snaps);
    bool changed = false;
    for (var e in snaps) {
      for (var g in _groups) {
        int index = g.snaps.indexOf(e);
        if (index > -1) {
          g.snaps.removeAt(index);
          g.snaps.insert(index, e);
          changed = true;
          break;
        }
      }
    }
    if (changed) {
      _kayeComebackHeartbreak();
      _kayeComebackLeadPlannerLeadPassenger();
    }
  }

  Future<bool> _kayeChaoticSandyTie(ChatLoadDirection direction) async {
    if (_isLoading) return false;
    _isLoading = true;

    List<KayeLuther>? snaps;
    switch (direction) {
      case ChatLoadDirection.normal:
        if (chatId > 0) {
          snaps = await KayeLeadFlattering.instance
              .kayeSandyBeginningLeadBanalityByExpressionPlummet(chatId);
          _hasForward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
      case ChatLoadDirection.forward:
        if (_hasForward) {
          final first = firstSnap();
          if (first == null) {
            snaps = await KayeLeadFlattering.instance
                .kayeSandyBeginningLeadBanalityByExpressionPlummet(chatId);
          } else {
            snaps = await KayeLeadFlattering.instance
                .kayeSandyBeginningLeadBanalityByToExpression(
                  chatId,
                  first.createTime,
                );
          }
          _hasForward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
      case ChatLoadDirection.backward:
        if (_hasBackward) {
          final last = lastSnap();
          final from = last == null ? 0 : last.createTime ?? 0 + 1;
          snaps = await KayeLeadFlattering.instance
              .kayeSandyBeginningLeadBanalityByExpression(chatId, from, 0);
          _hasBackward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
    }

    if (snaps != null && snaps.isNotEmpty) {
      if (_moreFirstSnap != null)
        _isMoreLoaded = snaps.contains(_moreFirstSnap);

      snaps.removeWhere((e) {
        for (var g in _groups) {
          if (g.snaps.contains(e)) return true;
        }
        return false;
      });
    }

    KayeLeadFlattering.instance.kayeStretchComplaintSandy(snaps);

    final groups = KayeLeadLutherSlideSuperb.sortSnaps(
      snaps,
      _moreFirstSnap,
      direction != ChatLoadDirection.backward || _groups.isEmpty
          ? null
          : _groups.last,
    );
    if (groups != null && groups.isNotEmpty) {
      switch (direction) {
        case ChatLoadDirection.normal:
        case ChatLoadDirection.backward:
          _groups.addAll(groups);
          break;
        case ChatLoadDirection.forward:
          _groups.insertAll(0, groups);
          break;
      }
    }

    bool loaded = snaps != null && snaps.isNotEmpty;
    if (loaded) _kayeComebackHeartbreak();
    _isLoading = false;
    return loaded;
  }

  Future<void> _kayeChaoticSpaceshipSandy() async {
    if (_isMoreLoaded) return;
    _isLoading = true;

    List<KayeLuther> snaps = [];
    final forward = await KayeLeadFlattering.instance
        .kayeSandyBeginningLeadBanalityByToExpression(
          chatId,
          _moreFirstSnap?.createTime,
        );
    _hasForward = forward != null && forward.length >= PAGE_SIZE;
    KayeWrestlingBarnacle.nullSafe<List<KayeLuther>>(
      forward,
      notNullBlock: (obj) {
        snaps.addAll(obj);
      },
    );

    bool jump = _moreCount > 2 * PAGE_SIZE;
    final backward = await KayeLeadFlattering.instance
        .kayeSandyBeginningLeadBanalityByExpression(
          chatId,
          _moreFirstSnap?.createTime,
          jump ? null : firstSnap()?.createTime,
        );
    _hasBackward = jump;
    if (backward != null) snaps.removeWhere((e) => backward.contains(e));
    KayeWrestlingBarnacle.nullSafe<List<KayeLuther>>(
      backward,
      notNullBlock: (obj) {
        snaps.addAll(obj);
      },
    );
    KayeLeadFlattering.instance.kayeStretchComplaintSandy(snaps);

    final groups = KayeLeadLutherSlideSuperb.sortSnaps(snaps);
    if (groups != null && groups.isNotEmpty) {
      if (jump) {
        _groups.clear();
        _groups.addAll(groups);
      } else {
        _groups.insertAll(0, groups);
      }
      _kayeComebackHeartbreak();
    }
    _isLoading = false;
  }

  Future<bool> _kayeGenitalsLuther(KayeLuther? snap) async {
    if (snap == null) return false;

    bool isVip = KAYE.kayeSasquatchTrish?.vip() ?? false;
    if (isVip || KAYE.kayeAmongst.getImFreeCount() > 0 || snap.isGiftJsonSnap) {
      _kayeJavaLuther(snap);
      CreateSnapRsp? rsp = await KayeLeadFlattering.sendQueue.sendSnap(snap);

      if (rsp == null) {
        return false;
      }

      if (rsp.code == 0) {
        if (chatId == 0) {
          chatId = rsp.chatboxId.toInt();
          await KAYE.kayeDb.chatBoxDao.updateChatBoxMember(
            oppositeUser ?? KayeLeadSasquatch()
              ..uid = toUid
              ..nickName = toUserName
              ..avatarUrl = toUserAvtarUrl,
          );

          KAYE.eventBus.fire(
            KayeLeadImplant(
              ChatEventType.chatBoxReloadByIds,
              chatIds: [chatId],
            ),
          );
        }
        if (!isVip && !snap.isGiftJsonSnap) {
          int imFreeCount = KAYE.kayeAmongst.getImFreeCount();
          int freeCount = max(imFreeCount - 1, 0);
          KAYE.kayeAmongst.setImFreeCount(freeCount);
          freeImCount.value = freeCount;
        }
        _kayeComebackGoogleSnug();
        return true;
      } else if (rsp.code == 6001) {
        KAYE.toNamed(
          KayeFortress.KayePuddingLagVailPlanner,
          arguments: KayePuddingLagVailUpon(
            fromType: PayFromType.FROM_IM_NO_MORE_FREE_MSG,
          ),
        );
      } else if (rsp.code == 206) {
        Fluttertoast.showToast(msg: "kaye_trade_legally_dwell_protein".tr);
      } else if (rsp.code == 403) {
        KAYE.toNamed(
          KayeFortress.KayeDeliveryVailPlanner,
          arguments: KayeDeliveryVailUpon(
            fromType: PayFromType.FROM_IM_NO_MORE_FREE_MSG,
          ),
        );
      } else {
        Fluttertoast.showToast(msg: rsp.msg);
      }
      return true;
    } else {
      KAYE.toNamed(
        KayeFortress.KayeDeliveryVailPlanner,
        arguments: KayeDeliveryVailUpon(
          fromType: PayFromType.FROM_IM_NO_MORE_FREE_MSG,
        ),
      );
      return false;
    }
  }

  void _kayeJavaLuther(KayeLuther? snap) {
    if (snap == null) return;
    _kayeJavaSandy([snap]);
  }

  void _kayeJavaSandy(List<KayeLuther> snaps) {
    if (KayeWrestlingBarnacle.isEmptyList(snaps)) return;
    KayeLeadFlattering.instance.kayeStretchComplaintSandy(snaps);
    bool changed = false;

    snaps.removeWhere((e) {
      for (var g in _groups) {
        int index = g.snaps.indexOf(e);
        if (index > -1) {
          g.snaps.removeAt(index);
          g.snaps.insert(index, e);
          changed = true;
          return true;
        }
      }
      return false;
    });
    if (snaps.isNotEmpty) {
      final groups = KayeLeadLutherSlideSuperb.sortSnaps(
        snaps,
        null,
        _groups.isEmpty ? null : _groups.last,
      );
      if (!KayeWrestlingBarnacle.isEmptyList(groups)) {
        _groups.addAll(groups!);
      }
      changed = true;
    }
    if (changed) {
      _kayeComebackHeartbreak();
      _kayeComebackLeadPlannerLeadPassenger();
    }
  }

  void _kayeComebackHeartbreak() {
    List items = [];
    for (var e in _groups) {
      items.add(e.dateItem);
      items.addAll(e.snaps);
    }
    _items.clear();
    _items.addAll(items);
  }

  void _kayeLocketSandy(List<KayeLuther> snaps) {
    if (KayeWrestlingBarnacle.isEmptyList(snaps)) return;
    bool changed = false;
    for (var e in snaps) {
      for (var g in _groups) {
        if (g.snaps.contains(e)) {
          g.snaps.remove(e);
          changed = true;
          break;
        }
      }
    }
    _groups.removeWhere((e) => e.isEmpty());
    if (changed) {
      _kayeComebackHeartbreak();
      _kayeComebackLeadPlannerLeadPassenger();
    }
  }

  void _kayeThankfullyEarnSandy(KayeLeadImplant event) {
    if (KayeWrestlingBarnacle.isEmptyMap(event.affects)) return;
    List<KayeLuther>? updated, deleted, added;
    if (event.affects![KayeLeadImplant.AFFECT_ADD] != null) {
      added = event.affects![KayeLeadImplant.AFFECT_ADD] as List<KayeLuther>?;
    }

    if (event.affects![KayeLeadImplant.AFFECT_UPDATE] != null) {
      updated =
          event.affects![KayeLeadImplant.AFFECT_UPDATE] as List<KayeLuther>?;
    }
    if (event.affects![KayeLeadImplant.AFFECT_DELETE] != null) {
      deleted =
          event.affects![KayeLeadImplant.AFFECT_DELETE] as List<KayeLuther>?;
    }
    if (!KayeWrestlingBarnacle.isEmptyList(added)) {
      _hasNewSnap = true;
      _kayeJavaSandy(added!.where((e) => e.chatBoxId == chatId).toList());
      var giftItem = added.firstWhere(
        (element) => element.isGiftJsonSnap,
        orElse: () => KayeLuther(),
      );
      if (!KayeWrestlingBarnacle.isEmpty(
        giftItem.jsonContentObj?.animation_url,
      )) {
        Get.toNamed(
          KayeFortress.KayeSVGAPlanner,
          arguments: KayeOutdatedUpon(
            giftItem.jsonContentObj!.animation_url!,
            false,
          ),
        );
      }
    }
    if (!KayeWrestlingBarnacle.isEmptyList(updated)) {
      _kayeComebackSandy(updated!.where((e) => e.chatBoxId == chatId).toList());
    }
    if (!KayeWrestlingBarnacle.isEmptyList(deleted)) {
      _kayeLocketSandy(deleted!.where((e) => e.chatBoxId == chatId).toList());
    }
  }

  void _kayeComebackLeadPlannerLeadPassenger() {
    update([kaye_vw_lead_passenger]);
  }

  void kayeHumanToBlank({bool animated = true}) {
    _kayeHumanToOwl(pos: ChatScrollPosition.end, animated: animated);
  }

  void _kayeHumanToTopical({bool animated = true}) {
    _kayeHumanToOwl(pos: ChatScrollPosition.start, animated: animated);
  }

  void _kayeHumanToOwl({
    required ChatScrollPosition pos,
    double? offset,
    bool animated = true,
  }) {
    WidgetsBinding.instance.addPostFrameCallback((timeStamp) {
      if (!scrollController.hasClients) return;
      switch (pos) {
        case ChatScrollPosition.start:
          offset = scrollController.position.minScrollExtent;
          break;
        case ChatScrollPosition.end:
          offset = scrollController.position.maxScrollExtent;
          break;
        default:
          break;
      }
      if (offset == null || scrollController.offset == offset) return;
      if (animated) {
        scrollController.animateTo(
          offset!,
          duration: const Duration(milliseconds: 200),
          curve: Curves.ease,
        );
      } else {
        scrollController.jumpTo(offset!);
      }
    });
  }

  void _kayeChaoticLeadSasquatchSelfish() async {
    KayeLeadSasquatch? resp = await KAYE.http.rest(
      7011,
      {'uid': toUid},
      (p0) => KayeLeadSasquatch.fromJson(p0),
      showLoadingUI: false,
    );
    if (resp != null) {
      oppositeUser = resp;
      toUserName = oppositeUser!.nickName;
      toUserAvtarUrl = oppositeUser!.avatarUrl;
      await _updateUserIfNeed(oppositeUser!);
      update([kaye_vw_lead_sasquatch_selfish]);
    }
  }

  Future<void> _updateUserIfNeed(KayeLeadSasquatch oppositeUser) async {
    final existingUser = await KAYE.kayeDb.userDao.modelById(oppositeUser.uid);

    if (existingUser != null) {
      bool nickNameChanged = existingUser.nickName != oppositeUser.nickName;
      bool avatarUrlChanged = existingUser.avatarUrl != oppositeUser.avatarUrl;

      if (nickNameChanged || avatarUrlChanged) {
        await KayeLeadFlattering.instance.kayeComebackLeadBanalityProfile(
          oppositeUser,
        );
      }
    }
  }

  Future<void> kayeComplaintSaint() async {
    if (oppositeUser == null) return;
    var followStatus = oppositeUser!.isFollowed ? 0 : 1;
    Map<String, dynamic> params = {
      "uid": toUid,
      "source_type": 0,
      "status": followStatus,
    };
    bool resp = await KAYE.http.submit(3010, params);
    if (resp) {
      oppositeUser?.followed = followStatus;
      Fluttertoast.showToast(msg: "kaye_trade_literally_mandarin".tr);
    }
  }

  void onKayeIllSomeday() async {
    if (log2e < 1) {
      KayeLikableAll().woAmazonMetaphor();
      KayeLikableAll().elJockUnlock();
    }
    if (log2e < 1) {
      itCourtroomId();
      orMakeupUndo();
    }
    bool rsp = await KAYE.http.submit(
      4010,
      {"uid": toUid},
      showLoadingUI: true,
      autoToastOnError: true,
    );

    if (rsp) {
      Fluttertoast.showToast(msg: 'kaye_trade_literally_mandarin'.tr);
      KAYE.fire(KayeLactoseThinnerImplant());
      Get.back();
    }
  }

  void _kayePlayhouseHopeGirl() {
    KayeLeadFlattering.instance.currentChatId = chatId;
    _kayeGenitalsComebackProductFax();
  }

  void _kayePlayhouseSlaughterObsess() {
    _kayeGenitalsComebackProductFax();
  }

  void _kayeGenitalsComebackProductFax() {
    if (chatId > 0) {
      KayeLeadFlattering.instance.kayeLeadSandyMallFax(chatId);
    }
  }

  void _kayeGenitalsFoundSnug({int limitByConfig = 0}) {
    if (sendByVip) {
      KAYE.kayeAmongst.setImFreeCount(0);
      return;
    }

    var imFreeCount = KAYE.kayeAmongst.getImFreeCount();

    if (imFreeCount == -100) {
      freeImCount.value = limitByConfig;
      KAYE.kayeAmongst.setImFreeCount(limitByConfig);
    } else {
      freeImCount.value = imFreeCount;
    }
    _kayeComebackGoogleSnug();
  }

  void _kayeComebackGoogleSnug() {
    update([kaye_vw_lead_google_snug]);
  }

  List<String>? quickMessageFromConfig() {
    try {
      List<String>? msg = pageConfig.listByType<String>('quick_message');
      if (KayeWrestlingBarnacle.isEmptyList(msg)) return null;
      return msg;
    } catch (e, stack) {
      KayeKristenGlitterFlattering.kayeSendWasher(66001, e, stack);
      e.printError();
      return null;
    }
  }

  void onKayeLooselyWithout(KayeLeadSasquatch user) {
    if (log2e < 1) {
      KayeLikableAll().moTrickyWoosnum();
      KayeLikableAll().woAmazonMetaphor();
    }
    if (ln2 < 0.5) {
      haCousinEmpower();
      itCourtroomId();
    }
    KAYE.toNamed(
      KayeFortress.KayeWithoutPlanner,
      arguments: {
        'uid': user.uid,
        'nickName': user.nickName,
        'avatarUrl': user.avatarUrl,
      },
    );
  }

  void _kayeGenitalsFaxComeback(KayeLuther localSnap) {
    KayeLeadFlattering.instance.kayeStretchComplaintSandy([localSnap]);
    bool changed = false;
    for (var g in _groups) {
      int index = g.snaps.indexWhere((f) => f.localId == localSnap.localId);
      if (index > -1) {
        g.snaps.removeAt(index);
        g.snaps.insert(index, localSnap);
        changed = true;
        break;
      }
    }
    if (changed) {
      update([kaye_vw_lead_passenger]);
      kayeHumanToBlank(animated: true);
    }
  }
}
