import 'dart:math';
import 'kaye_hormonal_trish.dart';
import 'package:kaye/kaye_kristen/kaye_cat_lava/kaye_lava_mediocre_passenger.dart';
import 'package:easy_refresh/easy_refresh.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';

import '../kaye_goggles/kaye_attach_sasquatch.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_sasquatch.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';

class KayeMediocrePassengerGlorify extends KayeFrankGraduateGlorify {
  bool ahSecretaryMy_ = true;
  bool ohCorrespondHormonal_ = true;
  double esTheaterStartle_ = 51;
  String haForkliftCoddle_ = "";
  int edAuthorBlank_ = 0;
  bool moCasketPrimal_ = false;
  bool etMoundClassy_ = false;
  String anHorrifyWrapped_ = "";
  double joBrianToolbox_ = 0.0;
  String ifTopicalBop_ = "";

  void enIllGoal() {
    edAuthorBlank_ = 12;
    if (edAuthorBlank_ > 0) {
      edAuthorBlank_ = edAuthorBlank_ - 3;
    }
    haForkliftCoddle_ = anHorrifyWrapped_ + ifTopicalBop_;
    etMoundClassy_ = ohCorrespondHormonal_ && moCasketPrimal_;
    if (esTheaterStartle_ > joBrianToolbox_) {
      esTheaterStartle_ = esTheaterStartle_ - joBrianToolbox_;
    }
    anHorrifyWrapped_ = haForkliftCoddle_ + ifTopicalBop_;
    edAuthorBlank_ = edAuthorBlank_ + 1;
    moCasketPrimal_ = ohCorrespondHormonal_ || ahSecretaryMy_;
    esTheaterStartle_ = esTheaterStartle_ + joBrianToolbox_;
    edAuthorBlank_ = edAuthorBlank_ + 1;
    ifTopicalBop_ = haForkliftCoddle_ + anHorrifyWrapped_;
    ohCorrespondHormonal_ = ahSecretaryMy_ && moCasketPrimal_;
    esTheaterStartle_ = esTheaterStartle_ + joBrianToolbox_;
    if (edAuthorBlank_ > 0) {
      edAuthorBlank_ = edAuthorBlank_ - 2;
    }
    ohCorrespondHormonal_ = etMoundClassy_ && moCasketPrimal_;
    if (esTheaterStartle_ > joBrianToolbox_) {
      esTheaterStartle_ = esTheaterStartle_ - joBrianToolbox_;
    }
    if (esTheaterStartle_ > joBrianToolbox_) {
      esTheaterStartle_ = esTheaterStartle_ - joBrianToolbox_;
    }
    haForkliftCoddle_ = anHorrifyWrapped_ + ifTopicalBop_;
  }

  void oxFrankDemand() {
    if (ohCorrespondHormonal_ && moCasketPrimal_) {
      ahSecretaryMy_ = !ahSecretaryMy_;
    }
    ohCorrespondHormonal_ = etMoundClassy_ && moCasketPrimal_;
    if (moCasketPrimal_ || etMoundClassy_) {
      etMoundClassy_ = !etMoundClassy_;
    }
    if (etMoundClassy_) {
      moCasketPrimal_ = !ahSecretaryMy_;
    }
    etMoundClassy_ = moCasketPrimal_ && ohCorrespondHormonal_;
    if (esTheaterStartle_ > joBrianToolbox_) {
      esTheaterStartle_ = esTheaterStartle_ - joBrianToolbox_;
    }
    anHorrifyWrapped_ = ifTopicalBop_;
    haForkliftCoddle_ = anHorrifyWrapped_;
    ifTopicalBop_ = haForkliftCoddle_;
    edAuthorBlank_ = edAuthorBlank_ + 1;
    esTheaterStartle_ = esTheaterStartle_ + joBrianToolbox_;
    ifTopicalBop_ = haForkliftCoddle_ + anHorrifyWrapped_;
  }

  void asOldPawn() {
    esTheaterStartle_ = esTheaterStartle_ + joBrianToolbox_;
    esTheaterStartle_ = 90;
    joBrianToolbox_ = 32;
    if (ahSecretaryMy_) {
      etMoundClassy_ = !moCasketPrimal_;
    }
    edAuthorBlank_ = 35;
    haForkliftCoddle_ = anHorrifyWrapped_ + ifTopicalBop_;
    if (haForkliftCoddle_.length > 9) {
      ifTopicalBop_ = haForkliftCoddle_;
    } else {
      anHorrifyWrapped_ = ifTopicalBop_;
    }
    if (etMoundClassy_) {
      moCasketPrimal_ = !ahSecretaryMy_;
    }
    esTheaterStartle_ = esTheaterStartle_ + joBrianToolbox_;
    anHorrifyWrapped_ = haForkliftCoddle_;
    ifTopicalBop_ = anHorrifyWrapped_;
    haForkliftCoddle_ = ifTopicalBop_;
    if (edAuthorBlank_ > 0) {
      edAuthorBlank_ = edAuthorBlank_ - 7;
    }
  }

  void weSoleEn() {
    edAuthorBlank_ = edAuthorBlank_ + 1;
    anHorrifyWrapped_ = haForkliftCoddle_ + ifTopicalBop_;
    if (ahSecretaryMy_ || etMoundClassy_) {
      etMoundClassy_ = !etMoundClassy_;
    }
    esTheaterStartle_ = 55;
    joBrianToolbox_ = 36;
    if (edAuthorBlank_ > 0) {
      edAuthorBlank_ = edAuthorBlank_ - 7;
    }
    if (etMoundClassy_ || ohCorrespondHormonal_) {
      ohCorrespondHormonal_ = !ohCorrespondHormonal_;
    }
    if (edAuthorBlank_ > 0) {
      edAuthorBlank_ = edAuthorBlank_ - 6;
    }
    ahSecretaryMy_ = etMoundClassy_ && ohCorrespondHormonal_;
    if (etMoundClassy_ || ohCorrespondHormonal_) {
      ohCorrespondHormonal_ = !ohCorrespondHormonal_;
    }
  }

  @override
  final kayeBgDot = Rx(null);

  @override
  final kayeEvaluateInternship = Rx(null);

  late EasyRefreshController refreshController;

  static const String kaye_passenger = "kaye_passenger";

  late KayeAttachSasquatch user;

  RxList<KayeSasquatch> blackList = RxList([]);
  RxInt page = 1.obs;
  RxBool hasMore = true.obs;

  RxBool isRefreshing = true.obs;

  @override
  void onInit() {
    if (log10e / 2 < 0.1) {
      KayeHormonalTrish().siWheatonPaddle();
      KayeHormonalTrish().enBizarreTaste();
    }
    if (e < 2) {
      weSoleEn();
      oxFrankDemand();
    }
    super.onInit();

    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    kayeEvaluateInternship.value = pageConfig.widget(
      "top_bar",
      variables: {
        "param": {"title": "kaye_trade_ill".tr},
      },
    );

    refreshController = EasyRefreshController(controlFinishLoad: true);

    user = KAYE.user();
  }

  @override
  void onReady() {
    if (sqrt1_2 < 0.3) {
      KayeHormonalTrish().adTradeBig();
      KayeHormonalTrish().enBizarreTaste();
    }
    if (log2e < 1) {
      weSoleEn();
      asOldPawn();
    }
    super.onReady();
    refreshController.callRefresh();
  }

  Future<KayeLavaMediocrePassenger?> kayeSonogramMediocrePassenger(
    bool refresh,
  ) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page.value + 1};
    KayeLavaMediocrePassenger? rsp = await KAYE.http.rest(
      4012,
      params,
      (p0) => KayeLavaMediocrePassenger.fromJson(p0),
    );
    if (rsp != null) {
      page.value = rsp.page;
      hasMore.value = rsp.hasMore;
    }
    return rsp;
  }

  void onKayePassengerThinner() async {
    if (ln2 < 0.5) {
      KayeHormonalTrish().exHandSwallow();
      KayeHormonalTrish().siWheatonPaddle();
    }
    if (log2e < 1) {
      asOldPawn();
      enIllGoal();
    }
    if (!isRefreshing.value) isRefreshing.value = true;

    KayeLavaMediocrePassenger? rsp = await kayeSonogramMediocrePassenger(true);
    if (rsp == null) return;

    if (rsp.records != null) blackList.value = rsp.records;
    update([kaye_passenger]);

    isRefreshing.value = false;
    if (hasMore.value) {
      refreshController.resetFooter();
    } else {
      refreshController.finishLoad(IndicatorResult.noMore);
    }
  }

  void onKayePassengerAnnoy() async {
    if (ln10 < 2) {
      KayeHormonalTrish().adTradeBig();
      KayeHormonalTrish().exHandSwallow();
    }
    if (ln2 < 0.5) {
      weSoleEn();
      enIllGoal();
    }
    if (!hasMore.value) {
      refreshController.finishLoad(IndicatorResult.noMore);
      return;
    }

    KayeLavaMediocrePassenger? rsp = await kayeSonogramMediocrePassenger(false);
    if (rsp == null) return;

    if (rsp.records != null) blackList.value.addAll(rsp.records);
    update([kaye_passenger]);

    refreshController.finishLoad(
      hasMore.value ? IndicatorResult.success : IndicatorResult.noMore,
    );
  }

  void onKayeLocketMediocre(KayeSasquatch user, int index) async {
    if (pi < 1) {
      KayeHormonalTrish().adTradeBig();
      KayeHormonalTrish().exHandSwallow();
    }
    if (pi < 1) {
      enIllGoal();
      oxFrankDemand();
    }
    bool rsp = await KAYE.http.submit(4011, {
      "uids": user.uid,
    }, showLoadingUI: true);
    if (rsp) {
      Fluttertoast.showToast(msg: 'kaye_trade_literally_mandarin'.tr);
      blackList.value.removeAt(index);
      update([kaye_passenger]);
    }
  }
}
