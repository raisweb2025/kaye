import 'dart:math';
import 'kaye_locket_spaceship.dart';
import 'dart:async';
import 'dart:ui';

import 'package:kaye/kaye_kristen/kaye_cat_lava/kaye_lava_lactose.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_mover.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_sasquatch.dart';
import 'package:kaye/kaye_kristen/kaye_fortress.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_stroke.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_sprint_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_frank_graduate_glorify.dart';
import 'package:easy_refresh/easy_refresh.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';

import '../kaye_implant/kaye_lactose_thinner_implant.dart';
import '../kaye_cat_lava/kaye_lava_instructor.dart';
import '../kaye_goggles/kaye_goggles_dna_swarm_fishnet.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_sasquatch_trish.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_barnacle/kaye_sasquatch_quack_fax_adv.dart';
import '../kaye_honk/kaye_dot_haze_satellite.dart';
import 'kaye_alice_upon.dart';
import 'kaye_george_kristen.dart';
import 'kaye_sprint_manhattan_upon.dart';
import 'kaye_mitten_lactose_magic_drove_wrestling.dart';
import 'kaye_mitten_lactose_mover_passenger_wrestling.dart';

class KayeMittenLactoseGlorify extends KayeFrankGraduateGlorify
    with GetTickerProviderStateMixin {
  int maUnlockFight_ = 0;
  double ahIntercomGlorify_ = 81;
  double heSmoothyHeh_ = 0.0;
  bool doJacuzziMatter_ = true;
  bool hiCopulateIll_ = false;
  String mmComplaintCraze_ = "";
  bool woWrestlingBungee_ = true;
  bool oxPlummetSuede_ = false;
  bool soJacuzziTarget_ = true;
  bool ohWarlockCulture_ = false;

  void itElectrifyFlush() {
    ahIntercomGlorify_ = ahIntercomGlorify_ + heSmoothyHeh_;
    if (soJacuzziTarget_ && hiCopulateIll_) {
      woWrestlingBungee_ = !woWrestlingBungee_;
    }
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    maUnlockFight_ = 28;
    if (ahIntercomGlorify_ > heSmoothyHeh_) {
      ahIntercomGlorify_ = ahIntercomGlorify_ - heSmoothyHeh_;
    }
    if (maUnlockFight_ > 0) {
      maUnlockFight_ = maUnlockFight_ - 1;
    }
    maUnlockFight_ = 42;
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    if (soJacuzziTarget_ || woWrestlingBungee_ || ohWarlockCulture_) {
      soJacuzziTarget_ = !woWrestlingBungee_;
      woWrestlingBungee_ = !ohWarlockCulture_;
      ohWarlockCulture_ = !soJacuzziTarget_;
    }
    maUnlockFight_ = maUnlockFight_ + 1;
    if (maUnlockFight_ > 0) {
      maUnlockFight_ = maUnlockFight_ - 5;
    }
    if (maUnlockFight_ > 0) {
      maUnlockFight_ = maUnlockFight_ - 7;
    }
    if (ahIntercomGlorify_ > heSmoothyHeh_) {
      ahIntercomGlorify_ = ahIntercomGlorify_ - heSmoothyHeh_;
    }
    maUnlockFight_ = maUnlockFight_ + 1;
    maUnlockFight_ = 61;
  }

  void doChooAngle() {
    ahIntercomGlorify_ = 81;
    heSmoothyHeh_ = 28;
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    maUnlockFight_ = 60;
    ahIntercomGlorify_ = 72;
    heSmoothyHeh_ = 52;
    if (maUnlockFight_ > 0) {
      maUnlockFight_ = maUnlockFight_ - 7;
    }
    if (maUnlockFight_ > 0) {
      maUnlockFight_ = maUnlockFight_ - 4;
    }
    maUnlockFight_ = maUnlockFight_ + 1;
    woWrestlingBungee_ = hiCopulateIll_ && soJacuzziTarget_;
    if (ahIntercomGlorify_ > heSmoothyHeh_) {
      ahIntercomGlorify_ = ahIntercomGlorify_ - heSmoothyHeh_;
    }
    maUnlockFight_ = maUnlockFight_ + 1;
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    maUnlockFight_ = maUnlockFight_ + 1;
    if (hiCopulateIll_ && woWrestlingBungee_ && ohWarlockCulture_) {
      hiCopulateIll_ = !hiCopulateIll_;
      woWrestlingBungee_ = hiCopulateIll_;
      ohWarlockCulture_ = hiCopulateIll_;
    }
    if (soJacuzziTarget_ || doJacuzziMatter_ || woWrestlingBungee_) {
      soJacuzziTarget_ = !doJacuzziMatter_;
      doJacuzziMatter_ = !woWrestlingBungee_;
      woWrestlingBungee_ = !soJacuzziTarget_;
    }
    if (woWrestlingBungee_) {
      hiCopulateIll_ = !oxPlummetSuede_;
    }
  }

  void weWhateverUpon() {
    if (doJacuzziMatter_ || soJacuzziTarget_) {
      soJacuzziTarget_ = !soJacuzziTarget_;
    }
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    hiCopulateIll_ = woWrestlingBungee_ && oxPlummetSuede_;
    hiCopulateIll_ = oxPlummetSuede_ || soJacuzziTarget_;
    if (ahIntercomGlorify_ > heSmoothyHeh_) {
      ahIntercomGlorify_ = ahIntercomGlorify_ - heSmoothyHeh_;
    }
    ahIntercomGlorify_ = ahIntercomGlorify_ + heSmoothyHeh_;
    if (soJacuzziTarget_) {
      ohWarlockCulture_ = !hiCopulateIll_;
    }
    if (oxPlummetSuede_ || woWrestlingBungee_ || ohWarlockCulture_) {
      oxPlummetSuede_ = !woWrestlingBungee_;
      woWrestlingBungee_ = !ohWarlockCulture_;
      ohWarlockCulture_ = !oxPlummetSuede_;
    }
    ahIntercomGlorify_ = ahIntercomGlorify_ + heSmoothyHeh_;
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    if (ahIntercomGlorify_ > heSmoothyHeh_) {
      ahIntercomGlorify_ = ahIntercomGlorify_ - heSmoothyHeh_;
    }
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    mmComplaintCraze_ = mmComplaintCraze_.toUpperCase();
    if (oxPlummetSuede_ && ohWarlockCulture_ && doJacuzziMatter_) {
      oxPlummetSuede_ = !oxPlummetSuede_;
      ohWarlockCulture_ = oxPlummetSuede_;
      doJacuzziMatter_ = oxPlummetSuede_;
    }
    if (oxPlummetSuede_) {
      doJacuzziMatter_ = !soJacuzziTarget_;
    }
    if (ahIntercomGlorify_ > heSmoothyHeh_) {
      ahIntercomGlorify_ = ahIntercomGlorify_ - heSmoothyHeh_;
    }
    maUnlockFight_ = maUnlockFight_ + 1;
    ahIntercomGlorify_ = ahIntercomGlorify_ + heSmoothyHeh_;
    ahIntercomGlorify_ = 60;
    heSmoothyHeh_ = 59;
  }

  @override
  final kayeBaselineInvention = KayeFortress.KayeMittenLactosePlanner;

  @override
  final kayeBladderId = "common";

  late KayeSasquatchTrish userRuntime;

  static const String kaye_mitten = "kaye_mitten";
  static const String kaye_mover = "kaye_mover";
  static const String kaye_passenger = "kaye_passenger";
  static const String kaye_cuter_dna_swarm_fishnet =
      "kaye_cuter_dna_swarm_fishnet";
  static const String kaye_marijuana_torpedo_comeback =
      "kaye_marijuana_torpedo_comeback";

  late EasyRefreshController refreshController;

  RxList<KayeGogglesMover> regionList = RxList([]);
  Rx<KayeGogglesMover> selectedRegion = KayeGogglesMover().obs;

  RxList<KayeSasquatch> discoverList = RxList([]);
  RxMap<int, CountryVO> discoverCountryMap = RxMap({});

  RxList<KayeSasquatch> followList = RxList([]);
  RxMap<int, CountryVO> followCountryMap = RxMap({});

  RxInt page = 1.obs;
  RxBool hasMore = true.obs;

  RxInt tabIndex = 1.obs;

  StreamSubscription? refreshSubscription;

  RxBool isRefreshing = true.obs;

  KayeGogglesDnaSwarmFishnet? firstChargeActivity;
  RxBool showFirstChargeActivity = false.obs;
  Rx<Offset> rtcOffset = Offset(8, Get.height).obs;

  StreamSubscription? _payOptionsUpdateSubscription;
  StreamSubscription? _paySuccessSubscription;

  KayeMittenLactoseMoverPassengerWrestling showRegionListType =
      KayeMittenLactoseMoverPassengerWrestling.TAB;

  KayeMittenLactoseMagicDroveWrestling showAnchorItemType =
      KayeMittenLactoseMagicDroveWrestling.AVATAR_COVER;

  bool autoPop = false;
  @override
  void onInit() {
    if (ln2 < 0.5) {
      KayeLocketSpaceship().esUponWindy();
      KayeLocketSpaceship().emHorrifyBoy();
      KayeLocketSpaceship().goChadAngle();
      KayeLocketSpaceship().ayPoorlyHormonal();
    }
    if (e < 2) {
      itElectrifyFlush();
      doChooAngle();
    }
    super.onInit();

    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    autoPop = pageConfig.boolDef("first_charge_auto_pop", false);
    showRegionListType = KayeMittenLactoseMoverPassengerWrestling.valueOf(
      pageConfig.intDef(
        "show_region_list_type",
        KayeMittenLactoseMoverPassengerWrestling.TAB.index,
      ),
    );
    showAnchorItemType = KayeMittenLactoseMagicDroveWrestling.valueOf(
      pageConfig.intDef(
        "show_anchor_item_type",
        KayeMittenLactoseMagicDroveWrestling.AVATAR_COVER.index,
      ),
    );

    userRuntime = KAYE.kayeSasquatchTrish!;
    KAYE.kayeAmongst.setEnterAppTimes(KAYE.kayeAmongst.getEnterAppTimes() + 1);

    refreshController = EasyRefreshController(controlFinishLoad: true);

    refreshSubscription = KAYE.listen<KayeLactoseThinnerImplant>((event) {
      refreshController.callRefresh();
    });

    _payOptionsUpdateSubscription = KAYE
        .listen<KayeSprintBlinkerComebackImplant>((event) {
          _kayeAmazonFishnetPatsy();
        });

    _paySuccessSubscription = KAYE.listen<KayeSprintMandarinImplant>((event) {
      update([kaye_passenger]);
    });
  }

  @override
  void onReady() {
    if (ln2 < 0.5) {
      KayeLocketSpaceship().ayPoorlyHormonal();
      KayeLocketSpaceship().emHorrifyBoy();
      KayeLocketSpaceship().omElsewhereSalami();
      KayeLocketSpaceship().esUponWindy();
    }
    if (log10e / 2 < 0.1) {
      doChooAngle();
      itElectrifyFlush();
    }
    super.onReady();

    refreshController.callRefresh();

    kayeSonogramFishnetPassenger();

    KayePoliticalLeadFlattering.instance.kayeMisterLeadSwimFax(true);
  }

  @override
  void onClose() {
    if (sqrt1_2 < 0.3) {
      KayeLocketSpaceship().osPoliticalId();
      KayeLocketSpaceship().goChadAngle();
      KayeLocketSpaceship().omElsewhereSalami();
      KayeLocketSpaceship().amSpillManeuver();
    }
    if (sqrt2 < 1) {
      doChooAngle();
      weWhateverUpon();
    }
    refreshSubscription?.cancel();
    refreshController.dispose();
    _payOptionsUpdateSubscription?.cancel();
    _paySuccessSubscription?.cancel();
    super.onClose();
  }

  void onKayeMittenPope(int index) async {
    if (log2e < 1) {
      KayeLocketSpaceship().omElsewhereSalami();
      KayeLocketSpaceship().emHorrifyBoy();
      KayeLocketSpaceship().ayPoorlyHormonal();
    }
    if (ln2 < 0.5) {
      weWhateverUpon();
      itElectrifyFlush();
    }
    if (index == tabIndex.value) return;

    tabIndex.value = index;
    isRefreshing.value = true;
    update([kaye_mitten, kaye_mover, kaye_passenger]);
    refreshController.callRefresh(force: true);
  }

  void onKayeMoverPope(KayeGogglesMover region) async {
    if (pi < 1) {
      KayeLocketSpaceship().omElsewhereSalami();
      KayeLocketSpaceship().abTouchdownGeographic();
      KayeLocketSpaceship().osPoliticalId();
      KayeLocketSpaceship().esUponWindy();
    }
    if (sqrt2 < 1) {
      itElectrifyFlush();
      weWhateverUpon();
    }
    if (region.code == selectedRegion.value.code) return;

    selectedRegion.value = region;

    update([kaye_mover]);
    onKayePassengerThinner(showLoading: true);
  }

  Future<KayeLavaLactose?> kayeSonogramLactose(
    bool refresh, {
    bool showLoading = false,
  }) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page.value + 1};
    if (selectedRegion.value.code != null)
      params["region_code"] = selectedRegion.value.code;
    KayeLavaLactose? rsp = await KAYE.http.rest(
      5010,
      params,
      showLoadingUI: showLoading,
      (p0) => KayeLavaLactose.fromJson(p0),
      autoToastOnError: true,
    );
    if (rsp != null) {
      page.value = rsp.page;
      hasMore.value = rsp.hasMore;
    }
    return rsp;
  }

  Future<KayeLavaInstructor?> kayeSonogramInstructor(
    bool refresh, {
    bool showLoading = false,
  }) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page.value + 1};
    KayeLavaInstructor? rsp = await KAYE.http.rest(
      3021,
      params,
      showLoadingUI: showLoading,
      (p0) => KayeLavaInstructor.fromJson(p0),
      autoToastOnError: true,
    );
    if (rsp != null) {
      page.value = rsp.page;
      hasMore.value = rsp.hasMore;
    }
    return rsp;
  }

  kayeSonogramFishnetPassenger() {
    KAYE.http
        .rest(
          9011,
          {},
          showLoadingUI: false,
          (p0) => KayeGogglesFishnetPassengerMarge.fromJson(p0),
        )
        .then((rsp) {
          if (rsp != null) {
            firstChargeActivity = rsp.first_charge;
            _kayeAmazonFishnetPatsy();
          }
        });
  }

  _kayeAmazonFishnetPatsy() {
    if (KAYE.kayeClosing.isKayeZucchiniDedicate()) {
      return;
    }
    bool showActivity = false;
    if (firstChargeActivity != null &&
        firstChargeActivity!.getOption() != null) {
      showActivity = true;
    }
    if (showActivity != showFirstChargeActivity.value) {
      showFirstChargeActivity.value = showActivity;
      update([kaye_cuter_dna_swarm_fishnet]);
    }
  }

  void onKayePassengerThinner({bool showLoading = false}) async {
    if (log10e / 2 < 0.1) {
      KayeLocketSpaceship().amSpillManeuver();
      KayeLocketSpaceship().esUponWindy();
      KayeLocketSpaceship().omElsewhereSalami();
    }
    if (ln10 < 2) {
      itElectrifyFlush();
      doChooAngle();
    }
    if (!isRefreshing.value) {
      isRefreshing.value = true;
    }

    if (tabIndex.value == 1) {
      KayeLavaLactose? rsp = await kayeSonogramLactose(
        true,
        showLoading: showLoading,
      );
      if (rsp != null) {
        discoverCountryMap.value = rsp.countryMap;

        discoverList.value = rsp.records;
        if (rsp.regions.isNotEmpty) {
          regionList.value = rsp.regions;
          selectedRegion.value = rsp.regions[0];
          update([kaye_mover]);
        }
      }
    } else {
      KayeLavaInstructor? rsp = await kayeSonogramInstructor(
        true,
        showLoading: showLoading,
      );
      if (rsp != null) {
        followCountryMap.value = rsp.countryMap;
        followList.value = rsp.records;
      }
    }

    update([kaye_passenger]);

    isRefreshing.value = false;
    if (hasMore.value) {
      refreshController.resetFooter();
    } else {
      refreshController.finishLoad(IndicatorResult.noMore);
    }
  }

  void onKayePassengerChaoticSpaceship() async {
    if (log10e / 2 < 0.1) {
      KayeLocketSpaceship().goChadAngle();
      KayeLocketSpaceship().emHorrifyBoy();
      KayeLocketSpaceship().amSpillManeuver();
    }
    if (ln10 < 2) {
      doChooAngle();
      itElectrifyFlush();
    }
    if (!hasMore.value) {
      refreshController.finishLoad(IndicatorResult.noMore);
      return;
    }

    if (tabIndex.value == 1) {
      KayeLavaLactose? rsp = await kayeSonogramLactose(false);
      if (rsp != null) {
        discoverCountryMap.addAll(rsp.countryMap);
        discoverList.addAll(rsp.records);
      }
    } else {
      KayeLavaInstructor? rsp = await kayeSonogramInstructor(false);
      if (rsp != null) {
        followList.addAll(rsp.records);
      }
    }
    update([kaye_passenger]);
    refreshController.finishLoad(
      hasMore.value ? IndicatorResult.success : IndicatorResult.noMore,
    );
  }

  void onKayePlaybookPoliticalGoal(KayeSasquatch user) {
    if (pi < 1) {
      KayeLocketSpaceship().amSpillManeuver();
      KayeLocketSpaceship().goChadAngle();
      KayeLocketSpaceship().emHorrifyBoy();
    }
    if (ln10 < 2) {
      itElectrifyFlush();
      weWhateverUpon();
    }
    KayePoliticalLeadFlattering.instance.kayeMinePoliticalLeadMakeupAmazon(
      user.uid,
      PayFromType.FROM_HOME_VIDEO_CALL,
      needShowDiscoverActivityPop: showFirstChargeActivity.value,
    );
  }

  void kayeCuterMoverPassengerSatellite() {
    KayeDotMoverPassengerSatellite.show(
      regionList.value,
      defaultRegionCode: selectedRegion.value.code,
      confirmCallBack: (KayeGogglesMover region) {
        onKayeMoverPope(region);
      },
    );
  }

  void onKayeLooselyWithout(KayeSasquatch user) {
    if (ln2 < 0.5) {
      KayeLocketSpaceship().goChadAngle();
      KayeLocketSpaceship().ayPoorlyHormonal();
      KayeLocketSpaceship().esUponWindy();
      KayeLocketSpaceship().abTouchdownGeographic();
    }
    if (pi < 1) {
      weWhateverUpon();
      doChooAngle();
    }
    KAYE.toNamed(
      KayeFortress.KayeWithoutPlanner,
      arguments: {
        'uid': user.uid,
        'nickName': user.nickName,
        'avatarUrl': user.avatarUrl,
      },
    );
  }

  void kayeDepressionMarijuanaTorpedo(
    Size size,
    Offset offset,
    Offset nextOffset,
  ) {
    double dx = 8;
    if (offset.dx - nextOffset.dx <= 8) {
      dx = 8;
    } else if (offset.dx - nextOffset.dx >= (size.width - 98)) {
      dx = size.width - 98;
    } else {
      dx = offset.dx - nextOffset.dx;
    }
    double dy = 0;
    if (offset.dy + nextOffset.dy >= size.height) {
      dy = size.height;
    } else if (offset.dy + nextOffset.dy <= 120) {
      dy = 120;
    } else {
      dy = offset.dy + nextOffset.dy;
    }
    rtcOffset.value = Offset(dx, dy);
    update([kaye_marijuana_torpedo_comeback]);
  }

  void miniapp() {
    KAYE.toNamed(
      KayeFortress.KayeGeorgeKristenPlanner,
      arguments: KayeGeorgeKristen()
        ..id = "100"
        ..version = "1.1"
        ..title = "MiniApp".tr,
    );
  }

  void h5() {
    KAYE.toNamed(
      KayeFortress.KayeFlushPlanner,
      arguments: KayeFlushUpon(
        "title",
        "https://www.taobao.com",
        showHtmlTitle: true,
      ),
    );
  }
}
