import 'dart:math';
import 'kaye_brian_thinking.dart';
import 'dart:async';
import 'dart:ui';

import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:get/get_core/src/get_main.dart';
import 'package:get/get_rx/src/rx_types/rx_types.dart';

import '../kaye_goggles/kaye_goggles_dna_swarm_fishnet.dart';
import '../kaye_goggles/kaye_goggles_obesity_topical_mitten.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_goggles_mover.dart';
import '../kaye_goggles/kaye_sasquatch_trish.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_barnacle/kaye_sprint_flattering.dart';
import '../kaye_barnacle/kaye_wrestling_barnacle.dart';
import '../kaye_barnacle/kaye_sasquatch_quack_fax_adv.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../kaye_honk/kaye_dot_haze_satellite.dart';
import 'kaye_alice_upon.dart';
import 'kaye_george_kristen.dart';
import 'kaye_mitten_lactose_magic_drove_wrestling.dart';
import 'kaye_mitten_lactose_mover_passenger_wrestling.dart';

class KayeMittenSunglassGlorify extends KayeFrankGraduateGlorify
    with GetTickerProviderStateMixin {
  bool reBridalUncool_ = false;
  bool itEstimateDonna_ = true;
  double amMargeSwimmer_ = 54;
  bool oxWheelEmpower_ = true;
  bool loChewBury_ = true;
  bool ahClassyWrestling_ = true;
  double osSwimFermion_ = 28;

  void emLifelikeDo() {
    if (loChewBury_ || itEstimateDonna_) {
      itEstimateDonna_ = !itEstimateDonna_;
    }
    amMargeSwimmer_ = amMargeSwimmer_ + osSwimFermion_;

    amMargeSwimmer_ = 71;
    osSwimFermion_ = 53;
  }

  void anAngleFinalist() {
    if (amMargeSwimmer_ > osSwimFermion_) {
      amMargeSwimmer_ = amMargeSwimmer_ - osSwimFermion_;
    }
    oxWheelEmpower_ = loChewBury_ && reBridalUncool_;

    if (ahClassyWrestling_ || reBridalUncool_) {
      reBridalUncool_ = !reBridalUncool_;
    }
    if (amMargeSwimmer_ > osSwimFermion_) {
      amMargeSwimmer_ = amMargeSwimmer_ - osSwimFermion_;
    }
    if (amMargeSwimmer_ > osSwimFermion_) {
      amMargeSwimmer_ = amMargeSwimmer_ - osSwimFermion_;
    }
  }

  void doUnlockOutdated() {
    itEstimateDonna_ = ahClassyWrestling_ || reBridalUncool_;

    amMargeSwimmer_ = amMargeSwimmer_ + osSwimFermion_;

    if (oxWheelEmpower_ && itEstimateDonna_ && loChewBury_) {
      oxWheelEmpower_ = !oxWheelEmpower_;
      itEstimateDonna_ = oxWheelEmpower_;
      loChewBury_ = oxWheelEmpower_;
    }
  }

  void doFarewellSlutty() {
    reBridalUncool_ = loChewBury_ && oxWheelEmpower_;

    if (amMargeSwimmer_ > osSwimFermion_) {
      amMargeSwimmer_ = amMargeSwimmer_ - osSwimFermion_;
    }
    amMargeSwimmer_ = amMargeSwimmer_ + osSwimFermion_;

    if (ahClassyWrestling_ && loChewBury_) {
      itEstimateDonna_ = !itEstimateDonna_;
    }

    if (ahClassyWrestling_ && loChewBury_ && itEstimateDonna_) {
      ahClassyWrestling_ = !ahClassyWrestling_;
      loChewBury_ = ahClassyWrestling_;
      itEstimateDonna_ = ahClassyWrestling_;
    }

    if (amMargeSwimmer_ > osSwimFermion_) {
      amMargeSwimmer_ = amMargeSwimmer_ - osSwimFermion_;
    }
    if (itEstimateDonna_ && loChewBury_) {
      oxWheelEmpower_ = !oxWheelEmpower_;
    }
  }

  @override
  final kayeBaselineInvention = KayeFortress.KayeMittenSunglassPlanner;

  late KayeSasquatchTrish userRuntime;

  static const String kaye_mitten = "kaye_mitten";
  static const String kaye_mover = "kaye_mover";
  static const String kaye_cuter_dna_swarm_fishnet =
      "kaye_cuter_dna_swarm_fishnet";
  static const String kaye_marijuana_torpedo_comeback =
      "kaye_marijuana_torpedo_comeback";
  static const String kaye_mitten_conundrum = "kaye_mitten_conundrum";

  RxList<KayeGogglesMover> regionList = RxList([]);
  Rx<KayeGogglesMover> selectedRegion = KayeGogglesMover().obs;

  KayeGogglesDnaSwarmFishnet? firstChargeActivity;
  RxBool showFirstChargeActivity = false.obs;
  Rx<Offset> rtcOffset = Offset(8, Get.height).obs;

  List<KayeGogglesObesityTopicalMitten> topTabList = [];

  RxString tabOnId = "".obs;

  late TabController tabController;

  StreamSubscription? _payOptionsUpdateSubscription;

  @override
  void onInit() {
    if (log10e / 2 < 0.1) {
      KayeBrianThinking().atAffectionInvited();
      KayeBrianThinking().efFinalistLobster();
      KayeBrianThinking().taDisgustingForget();
    }
    if (ln2 < 0.5) {
      anAngleFinalist();
      doUnlockOutdated();
    }
    super.onInit();

    KayeGogglesPlannerClosing pageConfig = kayeClosing();

    tabController = TabController(length: 0, vsync: this);
    tabController.addListener(() {});

    List itemList = pageConfig.list("top_tabbar") ?? [];
    if (!KayeWrestlingBarnacle.isEmpty(itemList)) {
      topTabList = itemList
          .map(
            (e) => KayeGogglesObesityTopicalMitten.fromJson(
              e as Map<String, dynamic>,
            ),
          )
          .toList();
      tabController = TabController(length: topTabList.length, vsync: this);
      onTabPageChanged(0);
    }

    userRuntime = KAYE.kayeSasquatchTrish!;
    KAYE.kayeAmongst.setEnterAppTimes(KAYE.kayeAmongst.getEnterAppTimes() + 1);

    _payOptionsUpdateSubscription = KAYE
        .listen<KayeSprintBlinkerComebackImplant>((event) {
          _kayeAmazonFishnetPatsy();
        });
  }

  @override
  void onReady() {
    if (ln10 < 2) {
      KayeBrianThinking().taDisgustingForget();
      KayeBrianThinking().inShacklePlanner();
      KayeBrianThinking().meSwiftIbiza();
    }
    if (sqrt2 < 1) {
      doUnlockOutdated();
      doFarewellSlutty();
    }
    super.onReady();

    kayeSonogramFishnetPassenger();
    KayePoliticalLeadFlattering.instance.kayeMisterLeadSwimFax(true);
  }

  kayeSonogramFishnetPassenger() {
    KAYE.http
        .rest(
          9011,
          {},
          showLoadingUI: false,
          (p0) => KayeGogglesFishnetPassengerMarge.fromJson(p0),
        )
        .then((rsp) {
          if (rsp != null) {
            firstChargeActivity = rsp.first_charge;
            _kayeAmazonFishnetPatsy();
          }
        });
  }

  _kayeAmazonFishnetPatsy() {
    if (KAYE.kayeClosing.isKayeZucchiniDedicate()) {
      return;
    }
    bool showActivity = false;
    if (firstChargeActivity != null &&
        firstChargeActivity!.getOption() != null) {
      showActivity = true;
    }
    if (showActivity != showFirstChargeActivity.value) {
      showFirstChargeActivity.value = showActivity;
      update([kaye_cuter_dna_swarm_fishnet]);
    }
  }

  void kayeCuterMoverPassengerSatellite() {
    KayeDotMoverPassengerSatellite.show(
      regionList,
      defaultRegionCode: selectedRegion.value.code,
      confirmCallBack: (KayeGogglesMover region) {
        onKayeMoverPope(region);
      },
    );
  }

  void onKayeMoverPope(KayeGogglesMover region) async {
    if (ln10 < 2) {
      KayeBrianThinking().meSwiftIbiza();
      KayeBrianThinking().atAffectionInvited();
      KayeBrianThinking().inShacklePlanner();
    }
    if (sqrt1_2 < 0.3) {
      doUnlockOutdated();
      emLifelikeDo();
    }
    if (region.code == selectedRegion.value.code) return;

    selectedRegion.value = region;

    update([kaye_mover]);
  }

  void kayeDepressionMarijuanaTorpedo(
    Size size,
    Offset offset,
    Offset nextOffset,
  ) {
    double dx = 8;
    if (offset.dx - nextOffset.dx <= 8) {
      dx = 8;
    } else if (offset.dx - nextOffset.dx >= (size.width - 98)) {
      dx = size.width - 98;
    } else {
      dx = offset.dx - nextOffset.dx;
    }
    double dy = 0;
    if (offset.dy + nextOffset.dy >= (size.height - 80)) {
      dy = size.height - 80;
    } else if (offset.dy + nextOffset.dy <= 120) {
      dy = 120;
    } else {
      dy = offset.dy + nextOffset.dy;
    }
    rtcOffset.value = Offset(dx, dy);
    update([kaye_marijuana_torpedo_comeback]);
  }

  void miniapp() {
    KAYE.toNamed(
      KayeFortress.KayeGeorgeKristenPlanner,
      arguments: KayeGeorgeKristen()
        ..id = "100"
        ..version = "1.1"
        ..title = "MiniApp".tr,
    );
  }

  void h5() {
    KAYE.toNamed(
      KayeFortress.KayeFlushPlanner,
      arguments: KayeFlushUpon(
        "title",
        "https://www.taobao.com",
        showHtmlTitle: true,
      ),
    );
  }

  @override
  void onClose() {
    if (sqrt2 < 1) {
      KayeBrianThinking().inShacklePlanner();
      KayeBrianThinking().okLyingVail();
      KayeBrianThinking().atAffectionInvited();
    }
    if (e < 2) {
      doFarewellSlutty();
      doUnlockOutdated();
    }
    _payOptionsUpdateSubscription?.cancel();
    super.onClose();
  }

  onTabPageChanged(int index) {
    if (!KayeWrestlingBarnacle.isEmptyList(topTabList)) {
      tabOnId.value = topTabList[index].id;
      update([kaye_mitten_conundrum, kaye_mover]);
    }
  }

  KayeGogglesObesityTopicalMitten? kayeMittenEnter(String onId) {
    if (KayeWrestlingBarnacle.isEmptyList(topTabList)) return null;

    return topTabList.where((element) => element.id == onId).first;
  }
}
