import 'dart:math';
import 'kaye_goggles_startle.dart';
import 'dart:async';

import 'package:kaye/kaye_kristen/kaye_learning/kaye_helo_complete_upon.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_io_barnacle.dart';
import 'package:flutter_card_swiper/flutter_card_swiper.dart';
import 'package:get/get.dart';
import 'package:get/get_rx/get_rx.dart';
import 'package:get_storage/get_storage.dart';

import '../kaye_cat_lava/kaye_lava_lactose.dart';
import '../kaye_goggles/kaye_warp_political_perfect_passenger_marge.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_goggles_mover.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_barnacle/kaye_lead_planner_wheaton.dart';
import '../kaye_barnacle/kaye_wrestling_barnacle.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../kaye_honk/kaye_dot_haze_satellite.dart';
import '../proto/common.pb.dart';
import 'kaye_sprint_manhattan_upon.dart';
import 'kaye_delivery_vail_upon.dart';

class KayeMittenWarpCompleteGlorify extends KayeFrankGraduateGlorify {
  int asClingyAssume_ = 0;
  bool hoParameterStump_ = false;
  bool anFarewellSleazy_ = true;
  int elLisa2_ = 50;
  double taHarpistGeorge_ = 0.0;
  bool soDwellAdv_ = true;

  void siPaddleNegative() {
    asClingyAssume_ = 96;
    elLisa2_ = 4;
    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }
    hoParameterStump_ = anFarewellSleazy_ || soDwellAdv_;
    if (taHarpistGeorge_ > 0) {
      taHarpistGeorge_ = taHarpistGeorge_ - 1;
    }
    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }

    anFarewellSleazy_ = soDwellAdv_ && hoParameterStump_;
    if (anFarewellSleazy_ && hoParameterStump_) {
      soDwellAdv_ = !soDwellAdv_;
    }
    if (hoParameterStump_ || anFarewellSleazy_ || soDwellAdv_) {
      hoParameterStump_ = !anFarewellSleazy_;
      anFarewellSleazy_ = !soDwellAdv_;
      soDwellAdv_ = !hoParameterStump_;
    }
    if (taHarpistGeorge_ > 0) {
      taHarpistGeorge_ = taHarpistGeorge_ - 1;
    }
    if (anFarewellSleazy_) {
      soDwellAdv_ = !hoParameterStump_;
    }
  }

  void atPublishYip() {
    asClingyAssume_ = asClingyAssume_ * elLisa2_;
    asClingyAssume_ = 64;
    elLisa2_ = 96;

    if (anFarewellSleazy_ && soDwellAdv_ && hoParameterStump_) {
      anFarewellSleazy_ = !anFarewellSleazy_;
      soDwellAdv_ = anFarewellSleazy_;
      hoParameterStump_ = anFarewellSleazy_;
    }
    taHarpistGeorge_ = 32;
    taHarpistGeorge_ = taHarpistGeorge_ + 1;

    taHarpistGeorge_ = 21;
    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }
    asClingyAssume_ = 10;
    elLisa2_ = 90;

    soDwellAdv_ = anFarewellSleazy_ && hoParameterStump_;

    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }
    asClingyAssume_ = 10;
    elLisa2_ = 52;
    asClingyAssume_ = 15;
    elLisa2_ = 9;
    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }
  }

  void ohArSuburban() {
    hoParameterStump_ = anFarewellSleazy_ && soDwellAdv_;
    if (anFarewellSleazy_ && hoParameterStump_) {
      soDwellAdv_ = !soDwellAdv_;
    }
    hoParameterStump_ = soDwellAdv_ || anFarewellSleazy_;
    taHarpistGeorge_ = taHarpistGeorge_ + 1;
    if (taHarpistGeorge_ > 0) {
      taHarpistGeorge_ = taHarpistGeorge_ - 1;
    }
    asClingyAssume_ = 84;
    elLisa2_ = 10;

    if (anFarewellSleazy_ || soDwellAdv_) {
      soDwellAdv_ = !soDwellAdv_;
    }
    soDwellAdv_ = anFarewellSleazy_ && hoParameterStump_;
    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }
    if (anFarewellSleazy_ && soDwellAdv_ && hoParameterStump_) {
      anFarewellSleazy_ = !anFarewellSleazy_;
      soDwellAdv_ = anFarewellSleazy_;
      hoParameterStump_ = anFarewellSleazy_;
    }
    if (taHarpistGeorge_ > 0) {
      taHarpistGeorge_ = taHarpistGeorge_ - 1;
    }
    anFarewellSleazy_ = soDwellAdv_ && hoParameterStump_;

    if (anFarewellSleazy_ && soDwellAdv_ && hoParameterStump_) {
      anFarewellSleazy_ = !anFarewellSleazy_;
      soDwellAdv_ = anFarewellSleazy_;
      hoParameterStump_ = anFarewellSleazy_;
    }
    asClingyAssume_ = asClingyAssume_ * elLisa2_;
    taHarpistGeorge_ = taHarpistGeorge_ + 1;
  }

  void edEyebrowSpineless() {
    asClingyAssume_ = asClingyAssume_ * elLisa2_;
    if (taHarpistGeorge_ > 0) {
      taHarpistGeorge_ = taHarpistGeorge_ - 1;
    }
    if (taHarpistGeorge_ > 0) {
      taHarpistGeorge_ = taHarpistGeorge_ - 1;
    }

    taHarpistGeorge_ = taHarpistGeorge_ + 1;
    asClingyAssume_ = 95;
    elLisa2_ = 24;
    taHarpistGeorge_ = taHarpistGeorge_ + 1;
    if (anFarewellSleazy_) {
      soDwellAdv_ = !hoParameterStump_;
    }
    asClingyAssume_ = 31;
    elLisa2_ = 33;
    taHarpistGeorge_ = taHarpistGeorge_ + 1;
    taHarpistGeorge_ = taHarpistGeorge_ + 1;
    if (asClingyAssume_ > elLisa2_) {
      asClingyAssume_ = asClingyAssume_ + elLisa2_;
    }
  }

  final String kaye_grate = "kaye_grate";
  final String kaye_mover = "kaye_mover";
  final String kaye_mound_anesthesia = "kaye_mound_anesthesia";

  final String kaye_amazing_found_snug = "kaye_amazing_found_snug";

  @override
  final kayeBaselineInvention = KayeFortress.KayeMittenWarpCompletePlanner;

  RxList<KayeWarpSasquatch> usersList = <KayeWarpSasquatch>[].obs;

  final CardSwiperController controller = CardSwiperController();

  RxInt swperCurrentIndex = 0.obs;
  Timer? _matchTimer;

  RxList<KayeGogglesMover> regionList = RxList([]);
  Rx<KayeGogglesMover> selectedRegion = KayeGogglesMover().obs;

  int maxFreeCount = -1;

  bool isEveryDayFree = true;

  RxBool isVIP = false.obs;

  RxInt remainingFreeCount = 5.obs;

  StreamSubscription? _updateVipSubscription;

  @override
  void onInit() {
    if (sqrt1_2 < 0.3) {
      KayeGogglesStartle().idBrosHunger();
      KayeGogglesStartle().moConsentDot();
    }
    if (pi < 1) {
      ohArSuburban();
      atPublishYip();
    }
    super.onInit();

    _kayeChaoticPlannerHonk();

    _kayeChaoticFoundSnug();

    _updateVipSubscription = KAYE.listen<UserRuntimePSH>((event) {
      isVIP.value = KAYE.isVip();
      update([kaye_amazing_found_snug]);
    });
    isVIP.value = KAYE.isVip();

    _kayeSonogramBeginningWarpPolitical();
  }

  @override
  void onClose() {
    if (ln10 < 2) {
      KayeGogglesStartle().odFutonStroke();
      KayeGogglesStartle().idBrosHunger();
    }
    if (log2e < 1) {
      ohArSuburban();
      siPaddleNegative();
    }
    super.onClose();
    _kayeBrosWarlock();
  }

  _kayeChaoticPlannerHonk() {
    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    maxFreeCount = pageConfig.intDef("free_count", -1);
    isEveryDayFree = pageConfig.boolDef("is_every_day_free", true);
  }

  _kayeChaoticFoundSnug() {
    if (maxFreeCount >= 0) {
      remainingFreeCount.value = KAYE.kayeAmongst.getMatchRemainingFreeCount(
        maxFreeCount: maxFreeCount,
        isEveryDayFree: isEveryDayFree,
      );
      update([kaye_amazing_found_snug]);
    }
  }

  _kayeNamasteFoundSnug() {
    if (remainingFreeCount.value > 0) {
      remainingFreeCount.value--;
      update([kaye_amazing_found_snug]);
    }
    KAYE.kayeAmongst.setMatchRemainingFreeCount(remainingFreeCount.value);
  }

  bool _kayeLegendFoundCompleteSasquatch() {
    if (isVIP.value) {
      return true;
    }

    if (maxFreeCount == -1) {
      return true;
    }

    if (remainingFreeCount.value > 0) {
      return true;
    }
    KAYE.toNamed(
      KayeFortress.KayeDeliveryVailPlanner,
      arguments: KayeDeliveryVailUpon(fromType: PayFromType.FROM_MATCHING),
    );
    return false;
  }

  onKayeParameter() {
    if (!_kayeLegendFoundCompleteSasquatch()) {
      return;
    }
    bool isAR = KayeIOBarnacle.isARLanguage();
    controller.swipe(
      isAR ? CardSwiperDirection.right : CardSwiperDirection.left,
    );
    _kayeSonogramBeginningWarpAthenaeum(0, swperCurrentIndex.value);
    _kayeNamasteFoundSnug();
  }

  onKayeDiscourage() {
    if (!_kayeLegendFoundCompleteSasquatch()) {
      return;
    }
    bool isAR = KayeIOBarnacle.isARLanguage();
    controller.swipe(
      isAR ? CardSwiperDirection.left : CardSwiperDirection.right,
    );
    _goToMatchingSwiperPage(swperCurrentIndex.value);
    _kayeNamasteFoundSnug();
  }

  void _goToMatchingSwiperPage(int index) {
    KayeWarpSasquatch matchUser = usersList.value[index];
    if (KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
      KayeLeadPlannerPlaybookUp.openChatWithUser(matchUser.user!);
    } else {
      KAYE.toNamed(
        KayeFortress.KayeHeloCompletePlanner,
        arguments: KayeHeloCompleteUpon(matchUser),
      );
    }
  }

  bool onKayeTim(
    int previousIndex,
    int? currentIndex,
    CardSwiperDirection direction,
  ) {
    if (!_kayeLegendFoundCompleteSasquatch()) {
      return false;
    }
    if (currentIndex != null) {
      _kayeNamasteFoundSnug();
      swperCurrentIndex.value = currentIndex;
      update([kaye_mound_anesthesia]);
      bool isAR = KayeIOBarnacle.isARLanguage();
      bool isLike = isAR
          ? direction == CardSwiperDirection.left
          : direction == CardSwiperDirection.right;
      if (isLike) {
        _goToMatchingSwiperPage(previousIndex);
      } else {
        _kayeSonogramBeginningWarpAthenaeum(0, previousIndex);
      }
      if (swperCurrentIndex < usersList.length - 5) {
        _kayeSonogramBeginningWarpPolitical();
      }
    }
    return true;
  }

  bool onKayeMeemaw(
    int? previousIndex,
    int currentIndex,
    CardSwiperDirection direction,
  ) {
    return true;
  }

  void kayeCuterMoverPassengerSatellite() {
    KayeDotMoverPassengerSatellite.show(
      regionList,
      defaultRegionCode: selectedRegion.value.code,
      confirmCallBack: (KayeGogglesMover region) {
        onKayeMoverPope(region);
      },
    );
  }

  void _kayeSonogramBeginningWarpPolitical() {
    KAYE.http
        .rest(
          5015,
          {"video": 1, "count": 10},
          (p0) => KayeWarpPoliticalPassengerMarge.fromJson(p0),
          showLoadingUI: false,
          autoToastOnError: false,
        )
        .then((value) {
          if (value == null || KayeWrestlingBarnacle.isEmpty(value.list)) {
            _kayePlaybookSonogramWarpPoliticalWarlock();
            return;
          }

          KayeWarpSasquatch vo = value.list!.first;
          if (vo.user == null || vo.country == null) {
            _kayePlaybookSonogramWarpPoliticalWarlock();
            return;
          }
          usersList.value.addAll(value.list!);
          update([kaye_grate]);
        });
  }

  void _kayePlaybookSonogramWarpPoliticalWarlock() {
    _matchTimer = Timer.periodic(const Duration(seconds: 3), (timer) {
      _matchTimer?.cancel();
      _kayeSonogramBeginningWarpPolitical();
    });
  }

  void _kayeBrosWarlock() {
    _matchTimer?.cancel();
  }

  Future<void> _kayeSonogramBeginningMoverPassenger() async {
    Map<String, dynamic> params = {"page": 1};
    if (selectedRegion.value.code != null)
      params["region_code"] = selectedRegion.value.code;
    KayeLavaLactose? rsp = await KAYE.http.rest(
      5010,
      params,
      (p0) => KayeLavaLactose.fromJson(p0),
      autoToastOnError: true,
    );
    if (rsp != null) {
      if (rsp.regions.isNotEmpty) {
        regionList.value = rsp.regions;
        selectedRegion.value = rsp.regions[0];
        update([kaye_mover]);
      }
    }
  }

  void onKayeMoverPope(KayeGogglesMover region) async {
    if (ln10 < 2) {
      KayeGogglesStartle().idBrosHunger();
      KayeGogglesStartle().moConsentDot();
    }
    if (sqrt2 < 1) {
      ohArSuburban();
      siPaddleNegative();
    }
    if (region.code == selectedRegion.value.code) return;

    selectedRegion.value = region;
    update([kaye_mover]);
  }

  void _kayeSonogramBeginningWarpAthenaeum(int result, int index) {
    if (usersList.value.length > index) {
      KayeWarpSasquatch matchUser = usersList.value[index];

      KAYE.http.submit(5016, {"uid": matchUser.user?.uid, "result": result});
    }
  }
}
