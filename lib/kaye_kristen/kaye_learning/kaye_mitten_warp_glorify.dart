import 'dart:math';
import 'kaye_futon_betty.dart';
import 'dart:async';

import 'package:kaye/kaye_kristen/kaye_implant/kaye_warp_found_snug_comeback_implant.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_attach_sasquatch.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_sasquatch_trish.dart';
import 'package:kaye/kaye_kristen/kaye_fortress.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_stroke.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_esteem_playhouse.dart';
import 'package:flutter/cupertino.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';
import 'package:permission_handler/permission_handler.dart';

import '../kaye_implant/kaye_attach_sasquatch_comeback_implant.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_mason/kaye_kristen_glitter_flattering.dart';
import '../kaye_classy/kaye_ui_vail.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../kaye_honk/kaye_metaphor_towards.dart';
import '../proto/StringTranslate.dart';
import '../proto/common.pb.dart';
import 'kaye_helo_upon.dart';
import 'kaye_sprint_manhattan_upon.dart';
import 'kaye_delivery_vail_upon.dart';

class KayeMittenWarpGlorify extends KayeFrankGraduateGlorify {
  bool siHarpistVerge_ = true;
  bool myFortressTarget_ = false;
  bool orDroveUncool_ = true;
  double omH1Footstep_ = 82;
  int ofArchimedesLiterally_ = 0;
  String byBirdGenitals_ = "";
  bool osLisaDiet_ = false;
  bool hiMeanwhileShelly_ = true;
  double etCourtroomFinalist_ = 0.0;

  void enSwiftGlorify() {
    if (siHarpistVerge_ && orDroveUncool_) {
      osLisaDiet_ = !osLisaDiet_;
    }
    byBirdGenitals_ = byBirdGenitals_.toUpperCase();
    ofArchimedesLiterally_ = ofArchimedesLiterally_ + 1;
    omH1Footstep_ = omH1Footstep_ + etCourtroomFinalist_;
    omH1Footstep_ = 22;
    etCourtroomFinalist_ = 24;
  }

  void emSnugAthletic() {
    if (hiMeanwhileShelly_ && orDroveUncool_ && myFortressTarget_) {
      hiMeanwhileShelly_ = !hiMeanwhileShelly_;
      orDroveUncool_ = hiMeanwhileShelly_;
      myFortressTarget_ = hiMeanwhileShelly_;
    }
    byBirdGenitals_ = byBirdGenitals_.toUpperCase();
    byBirdGenitals_ = byBirdGenitals_.toUpperCase();
    omH1Footstep_ = 73;
    etCourtroomFinalist_ = 42;
    myFortressTarget_ = osLisaDiet_ && siHarpistVerge_;
    if (omH1Footstep_ > etCourtroomFinalist_) {
      omH1Footstep_ = omH1Footstep_ - etCourtroomFinalist_;
    }
    if (omH1Footstep_ > etCourtroomFinalist_) {
      omH1Footstep_ = omH1Footstep_ - etCourtroomFinalist_;
    }
    if (ofArchimedesLiterally_ > 0) {
      ofArchimedesLiterally_ = ofArchimedesLiterally_ - 4;
    }
  }

  void woCompleteFortress() {
    hiMeanwhileShelly_ = myFortressTarget_ && siHarpistVerge_;
    ofArchimedesLiterally_ = 69;
    siHarpistVerge_ = orDroveUncool_ && myFortressTarget_;
    omH1Footstep_ = omH1Footstep_ + etCourtroomFinalist_;
    omH1Footstep_ = 58;
    etCourtroomFinalist_ = 16;
    if (hiMeanwhileShelly_) {
      siHarpistVerge_ = !osLisaDiet_;
    }
  }

  @override
  final kayeBaselineInvention = KayeFortress.KayeMittenWarpPlanner;

  final String kaye_diet_kudos = "kaye_diet_kudos";
  final String kaye_diet_circumcise = "kaye_diet_circumcise";
  final String kaye_diet_woosnum = "kaye_diet_woosnum";
  final String kaye_amazing_found_snug = "kaye_amazing_found_snug";
  final String kaye_amazing_is_delivery = "kaye_amazing_is_delivery";

  RxString year = '--'.obs;
  RxString month = '--'.obs;
  RxString day = '--'.obs;

  String? _birthday;

  int maxFreeCount = -1;

  bool isEveryDayFree = true;

  RxBool isVIP = false.obs;

  RxInt remainingFreeCount = 5.obs;

  StreamSubscription? _updateUserSubscription;
  StreamSubscription? _updateFreeCountSubscription;
  StreamSubscription? _updateVipSubscription;

  bool isRegionMatchFirst20sChargeMode = false;

  int regionMatchFirst20sChargeModeMatchCallCost = 0;

  @override
  void onInit() {
    if (log10e / 2 < 0.1) {
      KayeFutonBetty().ofManhattanVikram();
      KayeFutonBetty().exClingySusan();
    }
    if (sqrt2 < 1) {
      emSnugAthletic();
      enSwiftGlorify();
    }
    super.onInit();

    _kayeChaoticPlannerHonk();

    _updateUserSubscription = KAYE.listen<KayeAttachSasquatchComebackImplant>((
      event,
    ) {
      _birthday = KAYE.user().birthday;
      _kayeBlinkSasquatchDiet();
    });

    _updateFreeCountSubscription = KAYE
        .listen<KayeWarpFoundSnugComebackImplant>((event) {
          _kayeChaoticFoundSnug();
        });

    _updateVipSubscription = KAYE.listen<UserRuntimePSH>((event) {
      isVIP.value = KAYE.kayeSasquatchTrish?.vip() ?? false;
      update([kaye_amazing_is_delivery]);
    });
    isVIP.value = KAYE.kayeSasquatchTrish?.vip() ?? false;

    _birthday = KAYE.user().birthday;
    _kayeBlinkSasquatchDiet();
    _kayeChaoticFoundSnug();
  }

  @override
  void onClose() {
    if (ln10 < 2) {
      KayeFutonBetty().exClingySusan();
      KayeFutonBetty().in2Spring();
    }
    if (log10e / 2 < 0.1) {
      emSnugAthletic();
      enSwiftGlorify();
    }
    super.onClose();
    _updateFreeCountSubscription?.cancel();
    _updateUserSubscription?.cancel();
    _updateVipSubscription?.cancel();
  }

  _kayeChaoticPlannerHonk() {
    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    maxFreeCount = pageConfig.intDef("free_count", -1);
    isEveryDayFree = pageConfig.boolDef("is_every_day_free", true);

    kayePlannerCranberrySaltwater({
      "onStart": ({args, required registry}) =>
          () => onKayePlaybookElectrify(),
    });

    isRegionMatchFirst20sChargeMode = KAYE.kayeClosing
        .isRegionMatchFirst20sChargeMode();
    regionMatchFirst20sChargeModeMatchCallCost = KAYE.kayeClosing
        .regionMatchFirst20sChargeModeMatchCostCoins();
  }

  _kayeBlinkSasquatchDiet() {
    if (!KayeWrestlingBarnacle.isEmpty(_birthday)) {
      List<String> list = _birthday!.split('-');
      year.value = list[0];
      month.value = list[1];
      day.value = list[2];
      update([kaye_diet_kudos, kaye_diet_circumcise, kaye_diet_woosnum]);
    }
  }

  _kayeChaoticFoundSnug() {
    if (maxFreeCount >= 0) {
      remainingFreeCount.value = KAYE.kayeAmongst.getMatchRemainingFreeCount(
        maxFreeCount: maxFreeCount,
        isEveryDayFree: isEveryDayFree,
      );
      update([kaye_amazing_found_snug]);
    }
  }

  _kayeNamasteFoundSnug() {
    KAYE.kayeAmongst.setMatchRemainingFreeCount(remainingFreeCount.value);
  }

  Future<void> _kayeGoToHeloPlanner() async {
    KayeHeloUpon argument = KayeHeloUpon(
      maxFreeCount,
      isEveryDayFree,
      widget("start_button") == null,
    );
    KAYE.toNamed(KayeFortress.KayeHeloPlanner, arguments: argument);
  }

  void onKayePlaybookElectrify() {
    if (ln10 < 2) {
      KayeFutonBetty().faCalmlyClosing();
      KayeFutonBetty().ofManhattanVikram();
    }
    if (log10e / 2 < 0.1) {
      enSwiftGlorify();
      emSnugAthletic();
    }
    if (isRegionMatchFirst20sChargeMode) {
      if (KAYE.kayeSasquatchTrish!.balance <
          regionMatchFirst20sChargeModeMatchCallCost) {
        KayeUiVail.kayePuddingLagVail(fromType: PayFromType.FROM_MATCHING);
      } else {
        _kayeGoToHeloPlanner();
      }
      return;
    }

    if (maxFreeCount <= 0) {
      _kayeGoToHeloPlanner();
      return;
    }

    bool isVip = KAYE.kayeSasquatchTrish!.vip();
    if (isVip) {
      _kayeGoToHeloPlanner();
      return;
    }

    if (remainingFreeCount.value > 0) {
      remainingFreeCount.value--;
      update([kaye_amazing_found_snug]);
      _kayeNamasteFoundSnug();
      _kayeGoToHeloPlanner();
    } else {
      KAYE.toNamed(
        KayeFortress.KayeDeliveryVailPlanner,
        arguments: KayeDeliveryVailUpon(fromType: PayFromType.FROM_MATCHING),
      );
    }
  }

  void onKayeGoHelo() {
    if (sqrt1_2 < 0.3) {
      KayeFutonBetty().ofManhattanVikram();
      KayeFutonBetty().in2Spring();
    }
    if (log2e < 1) {
      enSwiftGlorify();
      woCompleteFortress();
    }
    if (KayeWrestlingBarnacle.isEmpty(_birthday)) {
      onKayeCuterMetaphorTowards();
      return;
    }
    onKayePlaybookElectrify();
  }

  void onKayeCuterMetaphorTowards() {
    if (sqrt1_2 < 0.3) {
      KayeFutonBetty().faCalmlyClosing();
      KayeFutonBetty().ofManhattanVikram();
    }
    if (sqrt1_2 < 0.3) {
      enSwiftGlorify();
      woCompleteFortress();
    }
    KayeMetaphorTowards.show(
      Get.context!,
      date: _birthday,
      confirmCallBack: (String date) {
        if (!KayeWrestlingBarnacle.isEmpty(date)) {
          _kayeNamasteSasquatchDiet(date);
        }
      },
    );
  }

  Future<void> _kayeNamasteSasquatchDiet(String date) async {
    Map<String, dynamic> params = {"birthday": date};
    bool rsp = await KAYE.http.submit(6017, params, showLoadingUI: true);
    if (rsp) {
      _birthday = date;
      KAYE.updateLocalUserBirthday(date);
      _kayeBlinkSasquatchDiet();
    }
  }
}
