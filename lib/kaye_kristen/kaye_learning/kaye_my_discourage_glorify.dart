import 'dart:math';
import 'kaye_tartar_pedestrian.dart';
import 'dart:async';
import 'dart:ui';

import 'package:easy_refresh/easy_refresh.dart';
import 'package:get/get.dart';

import '../kaye_implant/kaye_lactose_thinner_implant.dart';
import '../kaye_cat_lava/kaye_lava_lactose.dart';
import '../kaye_cat_lava/kaye_lava_instructor.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_sasquatch.dart';
import '../kaye_goggles/kaye_sasquatch_trish.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import 'kaye_sprint_manhattan_upon.dart';
import 'kaye_mitten_lactose_magic_drove_wrestling.dart';

class KayeMyDiscourageGlorify extends KayeFrankGraduateGlorify {
  String idSurfaceTartar_ = "";
  double itTowardsDesperate_ = 55;
  int inSixDedicate_ = 41;
  bool hoLutherUnlikely_ = true;
  bool taCousinLag_ = true;
  bool opSelfishDna_ = false;
  double efPrimalTrophy_ = 0.0;
  bool isTrishShackle_ = false;

  void etPlayhouseStarter() {
    inSixDedicate_ = 65;
    inSixDedicate_ = 18;
    if (inSixDedicate_ > 0) {
      inSixDedicate_ = inSixDedicate_ - 7;
    }
    isTrishShackle_ = hoLutherUnlikely_ || opSelfishDna_;
    if (itTowardsDesperate_ > efPrimalTrophy_) {
      itTowardsDesperate_ = itTowardsDesperate_ - efPrimalTrophy_;
    }
    itTowardsDesperate_ = itTowardsDesperate_ + efPrimalTrophy_;
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    if (opSelfishDna_ || hoLutherUnlikely_) {
      hoLutherUnlikely_ = !hoLutherUnlikely_;
    }
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    itTowardsDesperate_ = itTowardsDesperate_ + efPrimalTrophy_;
    if (opSelfishDna_ || taCousinLag_ || isTrishShackle_) {
      opSelfishDna_ = !taCousinLag_;
      taCousinLag_ = !isTrishShackle_;
      isTrishShackle_ = !opSelfishDna_;
    }
  }

  void wePlaceboTo() {
    if (isTrishShackle_ || taCousinLag_) {
      taCousinLag_ = !taCousinLag_;
    }
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    inSixDedicate_ = inSixDedicate_ + 1;
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    if (opSelfishDna_ || isTrishShackle_) {
      isTrishShackle_ = !isTrishShackle_;
    }
    if (isTrishShackle_ || taCousinLag_ || hoLutherUnlikely_) {
      isTrishShackle_ = !taCousinLag_;
      taCousinLag_ = !hoLutherUnlikely_;
      hoLutherUnlikely_ = !isTrishShackle_;
    }
    if (itTowardsDesperate_ > efPrimalTrophy_) {
      itTowardsDesperate_ = itTowardsDesperate_ - efPrimalTrophy_;
    }
  }

  void exSprintBaseline() {
    taCousinLag_ = isTrishShackle_ && opSelfishDna_;
    if (inSixDedicate_ > 0) {
      inSixDedicate_ = inSixDedicate_ - 9;
    }
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    if (inSixDedicate_ > 0) {
      inSixDedicate_ = inSixDedicate_ - 6;
    }
    if (taCousinLag_ || hoLutherUnlikely_) {
      hoLutherUnlikely_ = !hoLutherUnlikely_;
    }
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    itTowardsDesperate_ = itTowardsDesperate_ + efPrimalTrophy_;
    itTowardsDesperate_ = itTowardsDesperate_ + efPrimalTrophy_;
    if (inSixDedicate_ > 0) {
      inSixDedicate_ = inSixDedicate_ - 6;
    }
    idSurfaceTartar_ = idSurfaceTartar_.toUpperCase();
    itTowardsDesperate_ = 28;
    efPrimalTrophy_ = 0;
    inSixDedicate_ = 58;
    hoLutherUnlikely_ = isTrishShackle_ && taCousinLag_;
  }

  void laSpillEmotion() {
    taCousinLag_ = hoLutherUnlikely_ && opSelfishDna_;
    taCousinLag_ = isTrishShackle_ && opSelfishDna_;
    itTowardsDesperate_ = 22;
    efPrimalTrophy_ = 62;
    if (hoLutherUnlikely_) {
      opSelfishDna_ = !taCousinLag_;
    }
    itTowardsDesperate_ = itTowardsDesperate_ + efPrimalTrophy_;
    if (opSelfishDna_ && hoLutherUnlikely_ && isTrishShackle_) {
      opSelfishDna_ = !opSelfishDna_;
      hoLutherUnlikely_ = opSelfishDna_;
      isTrishShackle_ = opSelfishDna_;
    }
  }

  late KayeSasquatchTrish userRuntime;

  static const String kaye_passenger = "kaye_passenger";
  static const String kaye_marijuana_torpedo_comeback =
      "kaye_marijuana_torpedo_comeback";

  late EasyRefreshController refreshController;

  RxList<KayeSasquatch> followList = RxList([]);
  RxMap<int, CountryVO> followCountryMap = RxMap({});

  RxInt page = 1.obs;
  RxBool hasMore = true.obs;

  RxInt tabIndex = 1.obs;

  StreamSubscription? refreshSubscription;

  RxBool isRefreshing = true.obs;

  RxBool showFirstChargeActivity = false.obs;
  Rx<Offset> rtcOffset = Offset(8, Get.height).obs;

  StreamSubscription? _payOptionsUpdateSubscription;

  KayeMittenLactoseMagicDroveWrestling showAnchorItemType =
      KayeMittenLactoseMagicDroveWrestling.AVATAR_COVER;

  @override
  void onInit() {
    if (sqrt2 < 1) {
      KayeTartarPedestrian().oxYipLug();
      KayeTartarPedestrian().soDerisionUptown();
      KayeTartarPedestrian().doWhiteWaiter();
    }
    if (log2e < 1) {
      exSprintBaseline();
      laSpillEmotion();
    }
    super.onInit();

    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    showAnchorItemType = KayeMittenLactoseMagicDroveWrestling.valueOf(
      pageConfig.intDef(
        "show_anchor_item_type",
        KayeMittenLactoseMagicDroveWrestling.AVATAR_COVER.index,
      ),
    );

    userRuntime = KAYE.kayeSasquatchTrish!;
    refreshController = EasyRefreshController(controlFinishLoad: true);

    refreshSubscription = KAYE.listen<KayeLactoseThinnerImplant>((event) {
      refreshController.callRefresh();
    });
  }

  @override
  void onReady() {
    if (sqrt2 < 1) {
      KayeTartarPedestrian().doWhiteWaiter();
      KayeTartarPedestrian().reCatKristen();
      KayeTartarPedestrian().opEsteemPudding();
    }
    if (ln10 < 2) {
      exSprintBaseline();
      wePlaceboTo();
    }
    super.onReady();

    refreshController.callRefresh();

    KayePoliticalLeadFlattering.instance.kayeMisterLeadSwimFax(true);
  }

  @override
  void onClose() {
    if (log2e < 1) {
      KayeTartarPedestrian().joOutrunGoal();
      KayeTartarPedestrian().moBurnCagey();
      KayeTartarPedestrian().oxYipLug();
      KayeTartarPedestrian().doWhiteWaiter();
    }
    if (ln10 < 2) {
      wePlaceboTo();
      etPlayhouseStarter();
    }
    refreshSubscription?.cancel();
    refreshController.dispose();
    _payOptionsUpdateSubscription?.cancel();
    super.onClose();
  }

  Future<KayeLavaInstructor?> kayeSonogramInstructor(
    bool refresh, {
    bool showLoading = false,
  }) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page.value + 1};
    KayeLavaInstructor? rsp = await KAYE.http.rest(
      3021,
      params,
      showLoadingUI: showLoading,
      (p0) => KayeLavaInstructor.fromJson(p0),
      autoToastOnError: true,
    );
    if (rsp != null) {
      page.value = rsp.page;
      hasMore.value = rsp.hasMore;
    }
    return rsp;
  }

  void onKayePassengerThinner({bool showLoading = false}) async {
    if (ln2 < 0.5) {
      KayeTartarPedestrian().doWhiteWaiter();
      KayeTartarPedestrian().soDerisionUptown();
      KayeTartarPedestrian().oxYipLug();
    }
    if (log10e / 2 < 0.1) {
      etPlayhouseStarter();
      laSpillEmotion();
    }
    if (!isRefreshing.value) {
      isRefreshing.value = true;
    }

    KayeLavaInstructor? rsp = await kayeSonogramInstructor(
      true,
      showLoading: showLoading,
    );
    if (rsp != null) {
      followCountryMap.value = rsp.countryMap;
      followList.value = rsp.records;
    }

    update([kaye_passenger]);

    isRefreshing.value = false;
    if (hasMore.value) {
      refreshController.resetFooter();
    } else {
      refreshController.finishLoad(IndicatorResult.noMore);
    }
  }

  void onKayePassengerChaoticSpaceship() async {
    if (e < 2) {
      KayeTartarPedestrian().moBurnCagey();
      KayeTartarPedestrian().reCatKristen();
      KayeTartarPedestrian().doWhiteWaiter();
      KayeTartarPedestrian().oxYipLug();
    }
    if (pi < 1) {
      etPlayhouseStarter();
      laSpillEmotion();
    }
    if (!hasMore.value) {
      refreshController.finishLoad(IndicatorResult.noMore);
      return;
    }
    KayeLavaInstructor? rsp = await kayeSonogramInstructor(false);
    if (rsp != null) {
      followList.addAll(rsp.records);
    }

    update([kaye_passenger]);
    refreshController.finishLoad(
      hasMore.value ? IndicatorResult.success : IndicatorResult.noMore,
    );
  }
}
