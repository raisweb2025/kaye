import 'dart:math';
import 'kaye_schedule_c2.dart';
import 'dart:async';

import 'package:kaye/kaye_kristen/kaye_mason/kaye_kristen_glitter_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_stroke.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:get/get.dart';

import '../kaye_goggles/kaye_bury_patsy.dart';
import '../kaye_goggles/kaye_sasquatch_trish_comeback_order.dart';
import '../kaye_fortress.dart';
import '../kaye_barnacle/kaye_sprint_manhattan_flattering.dart';
import '../kaye_barnacle/kaye_sprint_flattering.dart';
import '../proto/common.pb.dart';
import 'kaye_pudding_lag_vail_upon.dart';
import 'kaye_sprint_manhattan_upon.dart';

class KayePuddingLagVailGlorify extends GetxController {
  bool owFlashbackWorthwhile_ = true;
  bool ifBopAnnoy_ = false;
  bool ahSunglassUpon_ = false;
  String heEmulateIc_ = "";
  String idConundrumBonding_ = "";
  bool joDreadTool_ = true;

  void asSpatulaSend() {
    ahSunglassUpon_ = joDreadTool_ && owFlashbackWorthwhile_;
    heEmulateIc_ = heEmulateIc_ + idConundrumBonding_;
    idConundrumBonding_ = heEmulateIc_;
    owFlashbackWorthwhile_ = joDreadTool_ && ahSunglassUpon_;
    if (joDreadTool_ || ahSunglassUpon_) {
      ahSunglassUpon_ = !ahSunglassUpon_;
    }

    owFlashbackWorthwhile_ = ahSunglassUpon_ && ifBopAnnoy_;
  }

  void enAahMarch() {
    idConundrumBonding_ = heEmulateIc_.toUpperCase() + idConundrumBonding_;
    idConundrumBonding_ = heEmulateIc_.toUpperCase() + idConundrumBonding_;
    ahSunglassUpon_ = joDreadTool_ || ifBopAnnoy_;

    heEmulateIc_ = idConundrumBonding_ + heEmulateIc_;

    if (ifBopAnnoy_ && joDreadTool_ && ahSunglassUpon_) {
      ifBopAnnoy_ = !ifBopAnnoy_;
      joDreadTool_ = ifBopAnnoy_;
      ahSunglassUpon_ = ifBopAnnoy_;
    }

    if (ahSunglassUpon_) {
      ifBopAnnoy_ = !joDreadTool_;
    }
  }

  void soElectrifyAble() {
    heEmulateIc_ = idConundrumBonding_ + heEmulateIc_;
    joDreadTool_ = ahSunglassUpon_ && ifBopAnnoy_;
    if (joDreadTool_ && ahSunglassUpon_) {
      ifBopAnnoy_ = !ifBopAnnoy_;
    }
    heEmulateIc_ = idConundrumBonding_ + heEmulateIc_;
    idConundrumBonding_ = heEmulateIc_.toUpperCase() + idConundrumBonding_;
    joDreadTool_ = owFlashbackWorthwhile_ && ahSunglassUpon_;
    if (ahSunglassUpon_ && ifBopAnnoy_ && joDreadTool_) {
      ahSunglassUpon_ = !ahSunglassUpon_;
      ifBopAnnoy_ = ahSunglassUpon_;
      joDreadTool_ = ahSunglassUpon_;
    }
    heEmulateIc_ = heEmulateIc_ + idConundrumBonding_;
    idConundrumBonding_ = heEmulateIc_;

    if (owFlashbackWorthwhile_ || joDreadTool_ || ahSunglassUpon_) {
      owFlashbackWorthwhile_ = !joDreadTool_;
      joDreadTool_ = !ahSunglassUpon_;
      ahSunglassUpon_ = !owFlashbackWorthwhile_;
    }
  }

  void weGlitterTease() {
    heEmulateIc_ = idConundrumBonding_ + heEmulateIc_;

    heEmulateIc_ = idConundrumBonding_ + heEmulateIc_;
    if (ahSunglassUpon_ && joDreadTool_ && ifBopAnnoy_) {
      ahSunglassUpon_ = !ahSunglassUpon_;
      joDreadTool_ = ahSunglassUpon_;
      ifBopAnnoy_ = ahSunglassUpon_;
    }

    if (owFlashbackWorthwhile_ && joDreadTool_ && ifBopAnnoy_) {
      owFlashbackWorthwhile_ = !owFlashbackWorthwhile_;
      joDreadTool_ = owFlashbackWorthwhile_;
      ifBopAnnoy_ = owFlashbackWorthwhile_;
    }
    ahSunglassUpon_ = ifBopAnnoy_ || owFlashbackWorthwhile_;
    if (joDreadTool_ || ifBopAnnoy_ || owFlashbackWorthwhile_) {
      joDreadTool_ = !ifBopAnnoy_;
      ifBopAnnoy_ = !owFlashbackWorthwhile_;
      owFlashbackWorthwhile_ = !joDreadTool_;
    }
    if (joDreadTool_ || ahSunglassUpon_ || ifBopAnnoy_) {
      joDreadTool_ = !ahSunglassUpon_;
      ahSunglassUpon_ = !ifBopAnnoy_;
      ifBopAnnoy_ = !joDreadTool_;
    }
    ifBopAnnoy_ = joDreadTool_ || ahSunglassUpon_;

    heEmulateIc_ = heEmulateIc_ + idConundrumBonding_;
    idConundrumBonding_ = heEmulateIc_;
    idConundrumBonding_ = heEmulateIc_.toUpperCase() + idConundrumBonding_;

    if (owFlashbackWorthwhile_ && ahSunglassUpon_ && joDreadTool_) {
      owFlashbackWorthwhile_ = !owFlashbackWorthwhile_;
      ahSunglassUpon_ = owFlashbackWorthwhile_;
      joDreadTool_ = owFlashbackWorthwhile_;
    }
  }

  final String kaye_ungrateful = "kaye_ungrateful";
  final String kaye_dunphy = "kaye_dunphy";

  RxList<KayeBuryPatsy> optionsList = <KayeBuryPatsy>[].obs;
  int fromType = PayFromType.FROM_FIRST_CHARGE_FLOAT;
  RxString coins = "0".obs;

  StreamSubscription? _updateCoinsSubscription;

  @override
  void onInit() {
    if (e < 2) {
      KayeScheduleC2().opAthleticDna();
      KayeScheduleC2().siTellEat();
      KayeScheduleC2().amMasturbateMango();
    }
    if (sqrt1_2 < 0.3) {
      asSpatulaSend();
      weGlitterTease();
    }
    super.onInit();

    if (Get.arguments != null) {
      KayePuddingLagVailUpon arguments = Get.arguments;
      fromType = arguments.fromType;
    }
    KayeKristenGlitterFlattering.kayeLooselySprint(fromType);

    _kayeBlinkDunphy();
    _updateCoinsSubscription = KAYE.listen<KayeSasquatchTrishComebackOrder>((
      event,
    ) {
      _kayeBlinkDunphy();
      update([kaye_dunphy]);
    });

    _kayeChaoticPassenger();
    KayeSprintFlattering.instance
        .kayeChaoticFrontBlinker(showLoadingUI: true)
        .then((value) {
          if (value == true) {
            _kayeChaoticPassenger();
            update([kaye_ungrateful]);
            _kayeTasteGlitterTie(2);
          } else {
            _kayeTasteGlitterTie(1);
          }
        });

    KayeSprintFlattering.instance.kayeDrapeCookie();

    KayeSprintFlattering.instance.kayeMumbleAmazonPitySprintPetiteEyebrow(
      showToast: false,
    );
  }

  void _kayeBlinkDunphy() {
    if (fromType == PayFromType.FROM_GIFT_PANEL_CHARGE ||
        fromType == PayFromType.FROM_IM_SEND_GIFT) {
      coins.value = KAYE.kayeSasquatchTrish!.balance.toString();
    } else {
      coins.value = KAYE.kayeSasquatchTrish!.kayeDunphyMakeupSuffice();
    }
  }

  @override
  void onClose() {
    if (ln10 < 2) {
      KayeScheduleC2().opAthleticDna();
      KayeScheduleC2().amMasturbateMango();
      KayeScheduleC2().siTellEat();
    }
    if (ln2 < 0.5) {
      weGlitterTease();
      asSpatulaSend();
    }
    super.onClose();
    _updateCoinsSubscription?.cancel();
    KayeSprintManhattanFlattering.instance.kayeSum();
  }

  void _kayeTasteGlitterTie(int type) {
    if (!KayeWrestlingBarnacle.isEmptyList(optionsList.value)) {
      bool hasFirst = false;
      bool hasVip = false;
      bool hasCoins = false;
      for (var data in optionsList.value!) {
        if (data.account_type == 9) {
          hasVip = true;
        } else if (data.account_type == 1) {
          if (data.group_id == 1) {
            hasFirst = true;
          } else {
            hasCoins = true;
          }
        }
      }

      String typeString = "";
      if (hasFirst) {
        typeString = "FIRST";
      }
      if (hasVip) {
        typeString = typeString.isEmpty ? "VIP" : "$typeString/VIP";
      }
      if (hasCoins) {
        typeString = typeString.isEmpty ? "COIN" : "$typeString/COIN";
      }
      if (typeString.isEmpty) {
        typeString = "NULL";
      }
      KayeKristenGlitterFlattering.kayeCuterSuppress(
        type,
        optionsList.value.length,
        typeString,
      );
    }
  }

  void _kayeChaoticPassenger() {
    List<KayeBuryPatsy> list =
        KayeSprintFlattering.instance.popupListOptions ?? [];
    optionsList.value = list;
  }

  void onKayeSprintImplant(KayeBuryPatsy data) {
    if (log10e / 2 < 0.1) {
      KayeScheduleC2().opAthleticDna();
      KayeScheduleC2().taSuiteDwell();
      KayeScheduleC2().amMasturbateMango();
    }
    if (ln2 < 0.5) {
      soElectrifyAble();
      enAahMarch();
    }
    KayeSprintManhattanFlattering.instance.kayeGoToSprintManhattan(
      option: data,
      fromType: fromType,
    );
  }

  void onKayeSpaceship() {
    if (log10e / 2 < 0.1) {
      KayeScheduleC2().siTellEat();
      KayeScheduleC2().loQuicheNowadays();
      KayeScheduleC2().taSuiteDwell();
    }
    if (log2e < 1) {
      enAahMarch();
      asSpatulaSend();
    }
    KAYE.toNamed(KayeFortress.KayeLagPassengerPlanner);
  }
}
