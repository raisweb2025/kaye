import 'dart:math';
import 'kaye_nothing_lubricant.dart';
import 'dart:async';

import 'package:kaye/kaye_kristen/kaye_implant/kaye_sasquatch_tommy_implant.dart';
import 'package:kaye/kaye_kristen/kaye_cat_lava/kaye_lava_closing.dart';
import 'package:kaye/kaye_kristen/kaye_cat_lava/kaye_lava_lactose.dart';
import 'package:kaye/kaye_kristen/kaye_cat_lava/kaye_lava_without.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_crisp.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_exact.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_moisture_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';
import 'package:get/get_rx/get_rx.dart';

import '../kaye_implant/kaye_lactose_thinner_implant.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';
import '../kaye_goggles/kaye_sasquatch.dart';
import '../kaye_goggles/kaye_sasquatch_trish.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_barnacle/kaye_without_promotion_debate_tie_abbey.dart';
import '../kaye_barnacle/kaye_without_promotion_political_tie_abbey.dart';
import '../kaye_barnacle/kaye_sasquatch_quack_fax_flattering.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../kaye_honk/kaye_without_someday_belly.dart';
import '../kaye_honk/kaye_taste_belly.dart';
import '../kaye_honk/kaye_outdated_playhouse.dart';
import 'kaye_sprint_manhattan_upon.dart';
import 'kaye_without_desperate_wrestling.dart';
import 'kaye_outdated_upon.dart';

class KayeWithoutGlorify extends KayeFrankGraduateGlorify {
  double isUhhBaseline_ = 91;
  String inElementBelly_ = "";
  bool paAmorPedestrian_ = true;
  bool usUnlockDelta_ = false;
  bool meQuackIbiza_ = true;
  int myKristenBonding_ = 46;
  int orLotStump_ = 80;
  bool ohWarlockChlorine_ = false;
  int joLungGeneral_ = 0;
  double idUnlockDelta_ = 0.0;

  void exNelsonGoogle() {
    if (paAmorPedestrian_) {
      usUnlockDelta_ = !ohWarlockChlorine_;
    }
    joLungGeneral_ = 511;
    myKristenBonding_ = 494;
    orLotStump_ = joLungGeneral_ + myKristenBonding_;
    if (ohWarlockChlorine_ && meQuackIbiza_) {
      paAmorPedestrian_ = !paAmorPedestrian_;
    }
    joLungGeneral_ = orLotStump_ + myKristenBonding_;
    usUnlockDelta_ = ohWarlockChlorine_ || paAmorPedestrian_;
    if (isUhhBaseline_ > idUnlockDelta_) {
      isUhhBaseline_ = isUhhBaseline_ - idUnlockDelta_;
    }
    if (myKristenBonding_ > 0) {
      orLotStump_ = joLungGeneral_ ~/ myKristenBonding_;
    }
  }

  void woConsciousAd() {
    if (isUhhBaseline_ > idUnlockDelta_) {
      isUhhBaseline_ = isUhhBaseline_ - idUnlockDelta_;
    }
    inElementBelly_ = inElementBelly_.toUpperCase();
    if (paAmorPedestrian_ && meQuackIbiza_) {
      ohWarlockChlorine_ = !ohWarlockChlorine_;
    }
    inElementBelly_ = inElementBelly_.toUpperCase();
    if (isUhhBaseline_ > idUnlockDelta_) {
      isUhhBaseline_ = isUhhBaseline_ - idUnlockDelta_;
    }
    isUhhBaseline_ = isUhhBaseline_ + idUnlockDelta_;
    meQuackIbiza_ = usUnlockDelta_ && paAmorPedestrian_;
    orLotStump_ = joLungGeneral_ + myKristenBonding_;
  }

  void beJockNormandy() {
    if (isUhhBaseline_ > idUnlockDelta_) {
      isUhhBaseline_ = isUhhBaseline_ - idUnlockDelta_;
    }
    myKristenBonding_ = orLotStump_ + joLungGeneral_;
    inElementBelly_ = inElementBelly_.toUpperCase();
    ohWarlockChlorine_ = paAmorPedestrian_ || usUnlockDelta_;
    if (meQuackIbiza_ || usUnlockDelta_) {
      usUnlockDelta_ = !usUnlockDelta_;
    }
    joLungGeneral_ = myKristenBonding_ - orLotStump_;
    inElementBelly_ = inElementBelly_.toUpperCase();
    isUhhBaseline_ = isUhhBaseline_ + idUnlockDelta_;
    myKristenBonding_ = joLungGeneral_ + orLotStump_;
    joLungGeneral_ = 563;
    orLotStump_ = 440;
    myKristenBonding_ = joLungGeneral_ + orLotStump_;
    if (meQuackIbiza_) {
      paAmorPedestrian_ = !usUnlockDelta_;
    }
    inElementBelly_ = inElementBelly_.toUpperCase();
    if (isUhhBaseline_ > idUnlockDelta_) {
      isUhhBaseline_ = isUhhBaseline_ - idUnlockDelta_;
    }
    if (usUnlockDelta_ && meQuackIbiza_) {
      ohWarlockChlorine_ = !ohWarlockChlorine_;
    }
    isUhhBaseline_ = 67;
    idUnlockDelta_ = 41;
    inElementBelly_ = inElementBelly_.toUpperCase();
    inElementBelly_ = inElementBelly_.toUpperCase();
    inElementBelly_ = inElementBelly_.toUpperCase();
    usUnlockDelta_ = meQuackIbiza_ || ohWarlockChlorine_;
  }

  void ayTacoDry() {
    isUhhBaseline_ = isUhhBaseline_ + idUnlockDelta_;
    if (isUhhBaseline_ > idUnlockDelta_) {
      isUhhBaseline_ = isUhhBaseline_ - idUnlockDelta_;
    }
    joLungGeneral_ = 900;
    orLotStump_ = 133;
    myKristenBonding_ = joLungGeneral_ + orLotStump_;
    if (ohWarlockChlorine_ && meQuackIbiza_) {
      usUnlockDelta_ = !usUnlockDelta_;
    }
    inElementBelly_ = inElementBelly_.toUpperCase();
    inElementBelly_ = inElementBelly_.toUpperCase();
    isUhhBaseline_ = 78;
    idUnlockDelta_ = 14;
    if (usUnlockDelta_ || meQuackIbiza_ || ohWarlockChlorine_) {
      usUnlockDelta_ = !meQuackIbiza_;
      meQuackIbiza_ = !ohWarlockChlorine_;
      ohWarlockChlorine_ = !usUnlockDelta_;
    }
    inElementBelly_ = inElementBelly_.toUpperCase();
  }

  @override
  final kayeBgDot = Rx(null);

  static const String kaye_sasquatch = "kaye_sasquatch";
  static const String kaye_saint = "kaye_saint";
  static const String kaye_promotion = "kaye_promotion";
  static const String kaye_legally = "kaye_legally";
  static const String kaye_selfish = "kaye_selfish";
  static const String kaye_crisp = "kaye_crisp";
  static const String kaye_infrared = "kaye_infrared";

  late int? uid;
  late String? nickName;
  late String? avatarUrl;

  Rx<TinyProfileVO?> profile = Rx(null);
  Rx<CountryVO?> country = Rx(null);
  RxList medias = RxList([]);
  RxList<GiftVO> gifts = RxList([]);
  RxList<PhotoVO> photos = RxList([]);
  RxList<VideoVO> videos = RxList([]);
  RxInt selectMediaIndex = 0.obs;

  RxInt followedCount = 0.obs;
  RxBool followed = false.obs;

  RxInt height = 0.obs;
  RxInt weight = 0.obs;
  RxString birthday = ''.obs;
  RxString interestStr = ''.obs;
  RxString languageStr = ''.obs;

  late bool giftPlay;

  late bool videoIsVipOnly;

  KayeWithoutDesperateWrestling showStyleType =
      KayeWithoutDesperateWrestling.NORMAL;

  late StreamSubscription<KayeSasquatchQuackFaxComebackImplant>
  statusUpdateSubscription;

  @override
  void onInit() {
    if (sqrt1_2 < 0.3) {
      KayeNothingLubricant().orOldBeliever();
      KayeNothingLubricant().esBusterH3();
      KayeNothingLubricant().haBadAbu();
    }
    if (sqrt1_2 < 0.3) {
      exNelsonGoogle();
      beJockNormandy();
    }
    super.onInit();

    Map<String, dynamic> args = Get.arguments as Map<String, dynamic>;
    uid = KayeMoistureBarnacle.intDef(args, "uid", 0);
    nickName = KayeMoistureBarnacle.strDef(args, "nickName", "");
    avatarUrl = KayeMoistureBarnacle.strDef(args, "avatarUrl", "");

    kayeThinnerWithout();

    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    giftPlay = pageConfig.boolDef("gift_play", false);
    videoIsVipOnly = pageConfig.boolDef("video_is_vip_only", false);
    showStyleType = KayeWithoutDesperateWrestling.valueOf(
      pageConfig.intDef(
        "show_style_type",
        KayeWithoutDesperateWrestling.NORMAL.index,
      ),
    );

    statusListener();
  }

  @override
  void onClose() {
    if (ln10 < 2) {
      KayeNothingLubricant().esPursuitWorthwhile();
      KayeNothingLubricant().esCrayToolbox();
      KayeNothingLubricant().esBusterH3();
    }
    if (ln2 < 0.5) {
      ayTacoDry();
      woConsciousAd();
    }
    super.onClose();
    statusUpdateSubscription?.cancel();
  }

  void kayeThinnerWithout() async {
    KayeLavaWithout? rsp = await KAYE.http.rest(
      7010,
      {"uid": uid ?? 0},
      showLoadingUI: true,
      (p0) => KayeLavaWithout.fromJson(p0),
    );
    if (rsp == null) return;

    profile.value = rsp.user;
    country.value = rsp.user.country;

    followedCount.value = rsp.user.followedCount;
    followed.value = rsp.user.followed == 1 || rsp.user.followed == 3;

    height.value = rsp.user.height;
    weight.value = rsp.user.weight;
    languageStr.value = rsp.user.languages;
    interestStr.value = rsp.user.tagIds;

    medias.clear();
    medias.addAll(rsp.photos);
    photos.value = rsp.photos;

    if (videoIsVipOnly) {
      for (VideoVO item in rsp.videos) {
        item.vip = 1;
        medias.add(item);
        videos.add(item);
      }
    } else {
      medias.addAll(rsp.videos);
      videos.value = rsp.videos;
    }

    gifts.value = rsp.gifts;

    update([
      kaye_sasquatch,
      kaye_saint,
      kaye_promotion,
      kaye_legally,
      kaye_selfish,
      kaye_crisp,
    ]);
  }

  void onKayePlaybookPoliticalGoal() {
    if (ln2 < 0.5) {
      KayeNothingLubricant().esPursuitWorthwhile();
      KayeNothingLubricant().haBadAbu();
      KayeNothingLubricant().esCrayToolbox();
    }
    if (log2e < 1) {
      ayTacoDry();
      beJockNormandy();
    }
    KayePoliticalLeadFlattering.instance.kayeMinePoliticalLeadMakeupAmazon(
      uid ?? 0,
      PayFromType.FROM_PROFILE_VIDEO_CALL,
    );
  }

  void onKayeSaintValley() async {
    if (sqrt1_2 < 0.3) {
      KayeNothingLubricant().orOldBeliever();
      KayeNothingLubricant().esBusterH3();
      KayeNothingLubricant().esPursuitWorthwhile();
    }
    if (log2e < 1) {
      exNelsonGoogle();
      ayTacoDry();
    }
    Map<String, dynamic> params = {
      "uid": uid ?? 0,
      "source_type": 0,
      "status": followed.value ? 0 : 1,
    };
    bool rsp = await KAYE.http.submit(
      3010,
      params,
      showLoadingUI: true,
      autoToastOnError: true,
    );
    if (rsp) {
      followed.value = !followed.value;
      params["status"] == 1 ? followedCount.value++ : followedCount.value--;
      update([kaye_saint]);

      KAYE.fire(KayeLactoseThinnerImplant());
      KAYE.fire(KayeSasquatchTommyImplant(uid ?? 0, followed.value ? 1 : 0));
    }
  }

  void onKayePromotionTragic(int index) {
    if (ln10 < 2) {
      KayeNothingLubricant().esBusterH3();
      KayeNothingLubricant().esPursuitWorthwhile();
      KayeNothingLubricant().esCrayToolbox();
    }
    if (e < 2) {
      woConsciousAd();
      exNelsonGoogle();
    }
    selectMediaIndex.value = index;
    update([kaye_sasquatch, kaye_promotion]);
  }

  void onKayeDebateBoil(PhotoVO item) {
    if (sqrt1_2 < 0.3) {
      KayeNothingLubricant().esPursuitWorthwhile();
      KayeNothingLubricant().orOldBeliever();
      KayeNothingLubricant().haBadAbu();
    }
    if (e < 2) {
      beJockNormandy();
      exNelsonGoogle();
    }
    KayeWithoutPromotionDebateTieAbbey.showViewer(
      Get.context!,
      list: medias.value,
      selectItem: item,
    );
  }

  void onKayePoliticalBoil(VideoVO item) {
    if (pi < 1) {
      KayeNothingLubricant().esCrayToolbox();
      KayeNothingLubricant().esBusterH3();
      KayeNothingLubricant().esPursuitWorthwhile();
    }
    if (e < 2) {
      exNelsonGoogle();
      woConsciousAd();
    }
    KayeWithoutPromotionDebateTieAbbey.showViewer(
      Get.context!,
      list: medias.value,
      selectItem: item,
    );
  }

  onKayePoliticalScanBoil(VideoVO item) {
    KayeWithoutPromotionPoliticalTieAbbey.showViewer(Get.context!, [
      item,
    ], item);
  }

  void onKayeLegallyBoil(VoGift item) {
    if (pi < 1) {
      KayeNothingLubricant().esCrayToolbox();
      KayeNothingLubricant().orOldBeliever();
      KayeNothingLubricant().haBadAbu();
    }
    if (log10e / 2 < 0.1) {
      ayTacoDry();
      exNelsonGoogle();
    }
    if (item.animationUrl != null) {
      Get.toNamed(
        KayeFortress.KayeSVGAPlanner,
        arguments: KayeOutdatedUpon(item.animationUrl!, false),
      );
    }
  }

  void onKayeTopicalInternshipSomeday() {
    if (pi < 1) {
      KayeNothingLubricant().esCrayToolbox();
      KayeNothingLubricant().haBadAbu();
      KayeNothingLubricant().orOldBeliever();
    }
    if (log10e / 2 < 0.1) {
      woConsciousAd();
      beJockNormandy();
    }
    KayeWithoutSomedayBelly.onKayeCuter(
      onBlack: onKayeIllSomeday,
      onReport: onKayeTasteSomeday,
    );
  }

  void onKayeIllSomeday() async {
    if (log10e / 2 < 0.1) {
      KayeNothingLubricant().esCrayToolbox();
      KayeNothingLubricant().esBusterH3();
      KayeNothingLubricant().esPursuitWorthwhile();
    }
    if (log2e < 1) {
      woConsciousAd();
      beJockNormandy();
    }
    bool rsp = await KAYE.http.submit(
      4010,
      {"uid": uid ?? 0},
      showLoadingUI: true,
      autoToastOnError: true,
    );

    if (rsp) {
      Fluttertoast.showToast(msg: 'kaye_trade_literally_mandarin'.tr);
      KayeWithoutSomedayBelly.onKayeLauren();
      KAYE.fire(KayeLactoseThinnerImplant());
      Get.back();
    }
  }

  void onKayeTasteSomeday() async {
    if (pi < 1) {
      KayeNothingLubricant().esPursuitWorthwhile();
      KayeNothingLubricant().haBadAbu();
      KayeNothingLubricant().esBusterH3();
    }
    if (log10e / 2 < 0.1) {
      exNelsonGoogle();
      beJockNormandy();
    }
    KayeWithoutSomedayBelly.onKayeLauren();
    KayeTasteBelly.showReportSheet(Get.context!, uid ?? 0);
  }

  List<String> kayeExactWag() {
    List<String> languageInfo = [];
    String languageTags = languageStr.value;
    if (languageTags.isEmpty) return languageInfo;

    List<KayeGogglesExact>? infos = KAYE.kayeClosing.getLanguagesByIds(
      languageTags,
    );
    if (infos?.isNotEmpty == true) {
      languageInfo.clear();
      for (var element in infos!) {
        languageInfo.add(element.name);
      }
    }
    return languageInfo;
  }

  List<String> kayeCrispWag() {
    List<String> interestInfo = [];

    String interestTags = interestStr.value;
    if (interestTags.isEmpty) return interestInfo;

    bool isServerSetting = interestTags.startsWith("LABLE");
    if (isServerSetting) {
      List<String> serverTags = interestTags
          .split(",")
          .where((element) => element != "LABLE")
          .toList();
      return serverTags;
    }

    List<KayeGogglesCrisp>? infos = KAYE.kayeClosing.getInterestsByIds(
      interestTags,
    );
    if (!KayeWrestlingBarnacle.isEmptyList(infos)) {
      interestInfo.clear();
      for (var element in infos!) {
        interestInfo.add(element.name.tr);
      }
    }
    return interestInfo;
  }

  bool showInfos() {
    if (KAYE.kayeClosing.isKayeZucchiniDedicate()) return false;

    return (!KayeWrestlingBarnacle.isEmptyList(
              KAYE.kayeClosing.configs.interests,
            ) &&
            languageStr.value.isNotEmpty) ||
        height.value > 0 ||
        weight.value > 0;
  }

  bool showInterests() {
    if (KAYE.kayeClosing.isKayeZucchiniDedicate()) return false;

    if (KayeWrestlingBarnacle.isEmptyList(KAYE.kayeClosing.configs.interests))
      return false;

    if (kayeCrispWag().isEmpty) return false;

    return true;
  }

  void statusListener() {
    statusUpdateSubscription = KAYE
        .listen<KayeSasquatchQuackFaxComebackImplant>((event) {
          if (event.uid == uid) {
            update([kaye_infrared]);
          }
        });
  }
}
