import 'dart:math';
import 'kaye_shirtless_tommy.dart';
import 'dart:math';

import 'package:kaye/kaye_kristen/kaye_advertise.dart';
import 'package:kaye/kaye_kristen/kaye_classy/kaye_toddler_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_io_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/widgets.dart';
import '../kaye_goggles/kaye_attach_sasquatch.dart';
import '../kaye_learning/kaye_delivery_dry_wrestling.dart';
import '../kaye_mason/kaye_kristen_glitter_flattering.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_barnacle/kaye_sydney_separate.dart';
import '../kaye_barnacle/kaye_monroe_barnacle.dart';
import '../kaye_honk/kaye_frank_graduate_planner.dart';
import '../kaye_classy/kaye_hand.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_dot_dunphy_assistant.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_dot_delivery_dry.dart';
import 'package:flutter/gestures.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

import '../kaye_goggles/kaye_bury_patsy.dart';
import '../kaye_honk/kaye_sydney.dart';
import '../kaye_learning/kaye_delivery_passenger_glorify.dart';
import '../kaye_honk/kaye_dot_delivery_assistant.dart';
import '../kaye_honk/kaye_dot_delivery_passenger_sasquatch_duplicate.dart';

class KayeDeliveryPassengerPlanner
    extends KayeFrankGraduatePlanner<KayeDeliveryPassengerGlorify> {
  const KayeDeliveryPassengerPlanner({super.key});

  @override
  List<Widget> buildBodyWidgets(BuildContext context) {
    try {
      Widget body = const SizedBox.shrink();
      switch (logic.showGridView) {
        case 0:
          body = _kayeDeliveryPassengerSuperb(context);
          break;
        case 1:
          body = _kayeDeliveryEyebrowsSuperb(context);
          break;
        case 2:
          body = _kayeDeliveryTrustPassengerSuperb(context);
          break;
      }

      return [
        Container(color: const Color(0xFF21003A)),
        Positioned(
          left: 0,
          right: 0,
          bottom: 0,
          child: Container(height: 240, color: Colors.white),
        ),
        Positioned(top: 0, left: 0, right: 0, bottom: 60, child: body),
        Positioned(
          left: 20,
          right: 20,
          bottom: 24,
          child: _kayeHerniaCreep(context),
        ),
        _kayeEvaluateInternship(context),
      ];
    } catch (e, stack) {
      KayeKristenGlitterFlattering.kayeSendWasher(770016, e, stack);
      rethrow;
    }
  }

  _kayeEvaluateInternship(BuildContext context) {
    return Positioned(
      top: Get.context!.mediaQueryPadding.top,
      left: 0,
      right: 0,
      child: Stack(
        alignment: Alignment.centerLeft,
        children: [
          Container(
            height: KayeAdvertise.kayePlannerTopicalInternshipTrophy,
            margin: const EdgeInsets.symmetric(
              horizontal: KayeAdvertise.kayePlannerTopicalInternshipFlammable,
            ),
            child: Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                Container(
                  width: KayeAdvertise.kayePlannerTopicalInternshipTrophy,
                  height: KayeAdvertise.kayePlannerTopicalInternshipTrophy,
                  alignment: Alignment.center,
                  child: InkWell(
                    onTap: logic.onKayeGoUhh,
                    child: Transform.rotate(
                      angle: KayeIOBarnacle.isARLanguage() ? pi : 0,
                      child: KayeSydney.local(
                        fileName: "kaye_ten_kristen_internship_uhh",
                        width: 24,
                        height: 24,
                      ),
                    ),
                  ),
                ),
                Container(
                  height: KayeAdvertise.kayePlannerTopicalInternshipTrophy,
                  margin: const EdgeInsets.symmetric(
                    horizontal:
                        KayeAdvertise.kayePlannerTopicalInternshipFlammable,
                  ),
                  alignment: Alignment.center,
                  child: logic.isIOS
                      ? Text(
                          "kaye_trade_october".tr,
                          style: KayeCreepDesperate.style(
                            fontSize: 18,
                            fontWeight: FontWeight.w500,
                            color: KayeToddlerBarnacle.white,
                          ),
                        )
                      : const SizedBox(),
                ),
                Container(
                  width: KayeAdvertise.kayePlannerTopicalInternshipTrophy,
                  height: KayeAdvertise.kayePlannerTopicalInternshipTrophy,
                  alignment: Alignment.center,
                  child: const SizedBox(),
                ),
              ],
            ),
          ),
          Positioned(
            left: KayeIOBarnacle.isARLanguage() ? 16 : null,
            right: KayeIOBarnacle.isARLanguage() ? null : 16,
            child: Visibility(
              visible: logic.isIOS,
              child: GestureDetector(
                onTap: logic.onKayeDrapeCookie,
                child: Container(
                  height: 28,
                  decoration: BoxDecoration(
                    borderRadius: BorderRadius.circular(14),
                    border: Border.all(
                      color: KayeToddlerBarnacle.white_60p,
                      width: 1,
                    ),
                  ),
                  padding: const EdgeInsets.only(left: 8, right: 8),
                  child: Row(
                    mainAxisSize: MainAxisSize.min,
                    children: [
                      KayeSydney.local(
                        fileName: 'kaye_ten_sprint_complaint_drape_interface',
                        width: 14,
                        height: 14,
                      ),
                      const SizedBox(width: 2),
                      Text(
                        "kaye_trade_drape".tr,
                        style: KayeCreepDesperate.style(
                          fontSize: 12,
                          color: KayeToddlerBarnacle.white,
                        ),
                      ),
                    ],
                  ),
                ),
              ),
            ),
          ),
        ],
      ),
    );
  }

  Widget _kayeDeliveryEyebrowsSuperb(BuildContext context) {
    double vipFunctionHeight =
        (logic.showFunctionType == KayeDeliveryDryWrestling.NORMAL ||
            logic.showFunctionType == KayeDeliveryDryWrestling.CARD_BIG)
        ? 240
        : 140;
    return GetBuilder<KayeDeliveryPassengerGlorify>(
      id: logic.kaye_delivery_passenger,
      builder: (logic) {
        return CustomScrollView(
          physics: const BouncingScrollPhysics(),
          slivers: [
            SliverToBoxAdapter(
              child: Padding(
                padding: EdgeInsets.only(
                  top: Get.mediaQuery.padding.top + 44,
                  bottom: 0,
                ),
                child: KayeDotDeliveryDry(
                  type: logic.showFunctionType,
                  showUserInfo: logic.showUserVipInfo,
                ),
              ),
            ),

            SliverGrid.builder(
              gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
                crossAxisCount: 2,
                mainAxisSpacing: 0,
                crossAxisSpacing: 16,
                childAspectRatio: ((Get.width - 48.0) / 2.0) / 160.0,
              ),
              itemBuilder: (BuildContext context, int index) {
                var data = logic.vipItems[index];
                return Padding(
                  padding: EdgeInsets.only(left: index % 2 == 0 ? 16 : 0),
                  child: _kayeDeliveryEyebrowsDrovePlayhouse(
                    context,
                    data,
                    index,
                  ),
                );
              },
              itemCount: logic.vipItems.length,
            ),

            SliverToBoxAdapter(
              child: logic.isIOS
                  ? Padding(
                      padding: const EdgeInsets.only(
                        top: 10,
                        bottom: 10,
                        left: 20,
                        right: 20,
                      ),
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          Text(
                            "kaye_trade_october_bros_waiter".tr,
                            style: KayeCreepDesperate.style(
                              fontSize: 12,
                              color: KayeToddlerBarnacle.white,
                            ),
                          ),
                          Text(
                            "kaye_trade_october_bros_plummet".tr,
                            style: KayeCreepDesperate.style(
                              fontSize: 12,
                              color: KayeToddlerBarnacle.white_80p,
                            ),
                          ),
                        ],
                      ),
                    )
                  : const SizedBox(),
            ),
          ],
        );
      },
    );
  }

  _kayeDeliveryEyebrowsDrovePlayhouse(
    BuildContext context,
    KayeBuryPatsy data,
    int index,
  ) {
    payCallback() {
      logic.onKayeSprintPatsy(data);
    }

    double width = (MediaQuery.of(context).size.width - 48.0) / 2;
    Widget body = Container(
      width: width,
      height: width,
      decoration: BoxDecoration(borderRadius: BorderRadius.circular(16)),
      child: Stack(
        children: [
          Positioned(
            top: 8,
            left: 0,
            right: 0,
            child: _kayeDeliveryEclipseDrove(
              context,
              data,
              payEvent: payCallback,
            ),
          ),
          Positioned(
            left: KayeIOBarnacle.isARLanguage() ? null : 0,
            right: KayeIOBarnacle.isARLanguage() ? 0 : null,
            top: 0,
            child: Visibility(
              visible: data.isBestOffer || data.showDiscount,
              child: KayeDotDunphyAssistant(option: data),
            ),
          ),
        ],
      ),
    );
    return body;
  }

  _kayeDeliveryEclipseDrove(
    BuildContext context,
    KayeBuryPatsy data, {
    VoidCallback? payEvent,
  }) {
    double width = (MediaQuery.of(context).size.width - 48.0) / 2;
    bool isAr = KayeIOBarnacle.isARLanguage();
    return GestureDetector(
      onTap: payEvent,
      child: Container(
        width: width,
        height: 160,
        padding: const EdgeInsets.symmetric(horizontal: 12),
        decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(20),
          color: data.isLifetime
              ? const Color(0xFF6900BE)
              : KayeToddlerBarnacle.white_20p,
          border: data.isLifetime
              ? Border.all(color: const Color(0xFFFFBF44), width: 2)
              : null,
        ),
        child: Stack(
          children: [
            Column(
              children: [
                data.isLifetime
                    ? Expanded(
                        child: Padding(
                          padding: const EdgeInsets.only(top: 15),
                          child: Row(
                            crossAxisAlignment: CrossAxisAlignment.center,
                            mainAxisAlignment: MainAxisAlignment.center,
                            children: [
                              Container(
                                width: data.isLifetime ? 56 : 48,
                                alignment: Alignment.center,
                                child: data.isLifetime
                                    ? KayeSydney.local(
                                        fileName: "kaye_ten_delivery_interface",
                                        width: 42,
                                        height: 42,
                                        fit: BoxFit.contain,
                                      )
                                    : Text(
                                        "${data.name_amount}",
                                        style: KayeCreepDesperate.style(
                                          color: const Color(0xFFFFC34F),
                                          fontSize: 36,
                                          fontWeight: FontWeight.bold,
                                        ),
                                      ),
                              ),
                              Expanded(
                                child: Text(
                                  data.isLifetime
                                      ? "kaye_trade_yip".tr
                                      : data.unitTitle,
                                  style: KayeCreepDesperate.style(
                                    color: KayeToddlerBarnacle.white,
                                    fontFamily: AppText.fontFamily,
                                    fontWeight: FontWeight.bold,
                                    fontSize: 14,
                                  ),
                                ),
                              ),
                            ],
                          ),
                        ),
                      )
                    : Expanded(
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.center,
                          mainAxisAlignment: MainAxisAlignment.start,
                          children: [
                            const SizedBox(height: 20),
                            Text(
                              "${data.name_amount}",
                              style: KayeCreepDesperate.style(
                                color: const Color(0xFFFFC34F),
                                fontSize: 32,
                                fontWeight: FontWeight.bold,
                              ),
                            ),
                            Text(
                              data.unitTitle,
                              style: KayeCreepDesperate.style(
                                color: KayeToddlerBarnacle.white,
                                fontFamily: AppText.fontFamily,
                                fontWeight: FontWeight.bold,
                                fontSize: 16,
                              ),
                            ),
                          ],
                        ),
                      ),
                Padding(
                  padding: const EdgeInsets.only(bottom: 12),
                  child: _kayeVirginElectrify(data),
                ),
              ],
            ),
            Positioned(
              top: 6,
              left: isAr ? 0 : null,
              right: isAr ? null : 0,
              child: Visibility(
                visible: data.extra_value > 0,
                child: Container(
                  height: 20,
                  padding: const EdgeInsets.only(left: 6, right: 6),
                  decoration: BoxDecoration(
                    color: const Color(0xFF8321FF),
                    borderRadius: BorderRadius.circular(10),
                  ),
                  child: Row(
                    children: [
                      Text(
                        "+${data.extra_value.toStringAsFixed(0)}",
                        style: KayeCreepDesperate.style(
                          color: KayeToddlerBarnacle.white,
                          fontWeight: FontWeight.bold,
                          fontSize: 12,
                        ),
                      ),
                      const SizedBox(width: 2),
                      KayeSydney.local(
                        fileName: "kaye_ten_float_masculine_interface",
                        width: 12,
                        height: 12,
                        fit: BoxFit.contain,
                      ),
                    ],
                  ),
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _kayeDeliveryPassengerSuperb(BuildContext context) {
    Widget body = GetBuilder<KayeDeliveryPassengerGlorify>(
      id: logic.kaye_delivery_passenger,
      builder: (logic) {
        if (KayeWrestlingBarnacle.isEmpty(logic.vipItems)) {
          return Padding(
            padding: EdgeInsets.only(
              top: Get.mediaQuery.padding.top + 44,
              bottom: 20,
            ),
            child: KayeDotDeliveryDry(
              type: logic.showFunctionType,
              showUserInfo: logic.showUserVipInfo,
            ),
          );
        }
        return ListView.separated(
          physics: const BouncingScrollPhysics(),
          itemBuilder: (context, index) {
            if (index == 0) {
              return Padding(
                padding: EdgeInsets.only(
                  top: Get.mediaQuery.padding.top + 44,
                  bottom: 20,
                ),
                child: KayeDotDeliveryDry(
                  type: logic.showFunctionType,
                  showUserInfo: logic.showUserVipInfo,
                ),
              );
            }
            if (logic.isIOS && index == logic.vipItems.length + 1) {
              return Padding(
                padding: const EdgeInsets.only(
                  top: 0,
                  bottom: 10,
                  left: 20,
                  right: 20,
                ),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Text(
                      "kaye_trade_october_bros_waiter".tr,
                      style: KayeCreepDesperate.style(
                        fontSize: 12,
                        color: KayeToddlerBarnacle.white,
                      ),
                    ),
                    Text(
                      "kaye_trade_october_bros_plummet".tr,
                      style: KayeCreepDesperate.style(
                        fontSize: 12,
                        color: KayeToddlerBarnacle.white_80p,
                      ),
                    ),
                  ],
                ),
              );
            }
            var data = logic.vipItems[index - 1];
            return _kayeDeliveryDrovePlayhouse(context, data);
          },
          itemCount: logic.vipItems.length + 1 + (logic.isIOS ? 1 : 0),
          padding: const EdgeInsets.symmetric(vertical: 12),
          separatorBuilder: (BuildContext context, int index) {
            return const Divider(height: 0, color: Colors.transparent);
          },
        );
      },
    );

    return SizedBox(width: MediaQuery.of(context).size.width, child: body);
  }

  _kayeDeliveryDrovePlayhouse(BuildContext context, KayeBuryPatsy data) {
    payCallback() {
      logic.onKayeSprintPatsy(data);
    }

    Widget body = Container(
      width: MediaQuery.of(context).size.width,
      padding: const EdgeInsets.symmetric(horizontal: 16),
      height: 80,
      color: Colors.white,
      child: Stack(
        children: [
          Positioned(
            top: 4,
            left: 0,
            right: 0,
            child: _kayeDeliveryDrove(context, data, payEvent: payCallback),
          ),
          Positioned(
            left: 16,
            right: 16,
            child: Container(height: 1, color: KayeToddlerBarnacle.black_10p),
          ),
        ],
      ),
    );
    return body;
  }

  _kayeDeliveryDrove(
    BuildContext context,
    KayeBuryPatsy data, {
    VoidCallback? payEvent,
  }) {
    return GestureDetector(
      onTap: payEvent,
      child: Container(
        height: 76,
        padding: const EdgeInsets.symmetric(horizontal: 12),
        decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(20),
          gradient: data.isLifetime
              ? const LinearGradient(
                  colors: [Color(0xFFCD45FF), Color(0xFF7821EC)],
                  begin: AlignmentGeometry.topRight,
                  end: AlignmentGeometry.bottomCenter,
                )
              : null,
        ),
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Expanded(
              child: Row(
                crossAxisAlignment: CrossAxisAlignment.center,
                mainAxisAlignment: MainAxisAlignment.start,
                children: [
                  Container(
                    width: data.isLifetime ? 50 : 44,
                    alignment: Alignment.center,
                    child: data.isLifetime
                        ? KayeSydney.local(
                            fileName: "kaye_ten_yip_delivery_interface",
                            width: 42,
                            height: 42,
                            fit: BoxFit.contain,
                          )
                        : Text(
                            "${data.name_amount}",
                            style: KayeCreepDesperate.style(
                              color: const Color(0xFFCD45FF),
                              fontSize: 36,
                              fontWeight: FontWeight.bold,
                            ),
                          ),
                  ),
                  data.isLifetime
                      ? const SizedBox(width: 8)
                      : const SizedBox(width: 16),
                  Expanded(
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        Text(
                          data.isLifetime
                              ? "kaye_trade_yip".tr
                              : data.unitTitle,
                          maxLines: 2,
                          softWrap: true,
                          overflow: TextOverflow.ellipsis,
                          style: KayeCreepDesperate.style(
                            color: data.isLifetime
                                ? Colors.white
                                : KayeToddlerBarnacle.black,
                            fontFamily: AppText.fontFamily,
                            fontWeight: FontWeight.bold,
                            fontSize: 16,
                          ),
                        ),
                        Visibility(
                          visible: data.extra_value > 0,
                          child: Container(
                            height: 20,
                            padding: const EdgeInsets.only(left: 6, right: 6),
                            decoration: BoxDecoration(
                              gradient:
                                  KayeAdvertise.kayeFloatEnforceTattletale,
                              borderRadius: BorderRadius.circular(10),
                            ),
                            child: Row(
                              mainAxisSize: MainAxisSize.min,
                              children: [
                                Text(
                                  "+${data.extra_value.toStringAsFixed(0)}",
                                  style: KayeCreepDesperate.style(
                                    color: KayeToddlerBarnacle.white,
                                    fontWeight: FontWeight.bold,
                                    fontSize: 12,
                                  ),
                                ),
                                const SizedBox(width: 2),
                                KayeSydney.local(
                                  fileName:
                                      "kaye_ten_float_masculine_interface",
                                  width: 12,
                                  height: 12,
                                  fit: BoxFit.contain,
                                ),
                              ],
                            ),
                          ),
                        ),
                      ],
                    ),
                  ),
                ],
              ),
            ),
            Visibility(
              visible: data.isBestOffer || data.showDiscount,
              child: KayeDotDeliveryAssistant(option: data),
            ),
            _kayeVirginElectrify(data),
          ],
        ),
      ),
    );
  }

  Widget _kayeVirginElectrify(KayeBuryPatsy data) {
    return Container(
      height: 40,
      padding: const EdgeInsets.symmetric(horizontal: 14),
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(20),
        color: Colors.white,
        gradient: data.isLifetime
            ? null
            : const LinearGradient(
                begin: Alignment.topRight,
                end: Alignment.bottomCenter,
                colors: [Color(0xFFCD45FF), Color(0xFF7821EC)],
              ),
      ),
      child: Center(
        child: Text(
          data.priceStr,
          style: KayeCreepDesperate.style(
            color: data.isLifetime ? const Color(0xFFB52FFF) : Colors.white,
            fontWeight: FontWeight.bold,
            fontSize: 14,
          ),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }

  Widget _kayeSasquatchDuplicate() {
    if (!logic.showUserVipInfo) {
      return const SizedBox.shrink();
    }
    return KayeDotDeliveryPassengerSasquatchDuplicate(
      userRuntime: logic.userRuntime,
    );
  }

  Widget _kayeDeliveryTrustPassengerSuperb(BuildContext context) {
    List<Widget> slivers = [];

    Widget vipFuction = SliverToBoxAdapter(
      child: Padding(
        padding: EdgeInsets.only(
          top: Get.mediaQuery.padding.top + 44,
          bottom: 0,
        ),
        child: KayeDotDeliveryDry(
          type: logic.showFunctionType,
          showUserInfo:
              logic.showUserVipInfo &&
              logic.showFunctionType != KayeDeliveryDryWrestling.LIST_VIEW,
        ),
      ),
    );

    Widget vipPriceList = SliverToBoxAdapter(
      child: Container(
        margin: const EdgeInsets.only(top: 10),
        height: 160.0,
        child: ListView.builder(
          scrollDirection: Axis.horizontal,
          itemCount: logic.vipItems.length,
          itemBuilder: (BuildContext context, int index) {
            var data = logic.vipItems[index];
            return Padding(
              padding: EdgeInsets.only(left: index == 0 ? 16 : 5, right: 5),
              child: _kayeDeliveryTrustPassengerDrovePlayhouse(
                context,
                data,
                index,
              ),
            );
          },
        ),
      ),
    );
    if (logic.showFunctionType == KayeDeliveryDryWrestling.LIST_VIEW) {
      slivers.add(
        GetBuilder<KayeDeliveryPassengerGlorify>(
          id: logic.kaye_delivery_fax,
          builder: (logic) {
            return SliverToBoxAdapter(
              child: Padding(
                padding: EdgeInsets.only(top: Get.mediaQuery.padding.top + 44),
                child: _kayeSasquatchDuplicate(),
              ),
            );
          },
        ),
      );
      slivers.add(vipPriceList);
      slivers.add(_kayeDesperateDeliveryPassengerPlayhouseDeliveryOrange());
    } else {
      slivers.add(vipFuction);
      slivers.add(vipPriceList);
    }
    slivers.add(
      SliverToBoxAdapter(
        child: logic.isIOS
            ? Padding(
                padding: const EdgeInsets.only(
                  top: 10,
                  bottom: 10,
                  left: 20,
                  right: 20,
                ),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Text(
                      "kaye_trade_october_bros_waiter".tr,
                      style: KayeCreepDesperate.style(
                        fontSize: 12,
                        color: KayeToddlerBarnacle.white,
                      ),
                    ),
                    Text(
                      "kaye_trade_october_bros_plummet".tr,
                      style: KayeCreepDesperate.style(
                        fontSize: 12,
                        color: KayeToddlerBarnacle.white_80p,
                      ),
                    ),
                  ],
                ),
              )
            : const SizedBox(),
      ),
    );

    Widget body = GetBuilder<KayeDeliveryPassengerGlorify>(
      id: logic.kaye_delivery_passenger,
      builder: (logic) {
        return CustomScrollView(
          physics: const BouncingScrollPhysics(),
          slivers: slivers,
        );
      },
    );

    return SizedBox(width: MediaQuery.of(context).size.width, child: body);
  }

  Widget _kayeDesperateDeliveryPassengerPlayhouseDeliveryOrange() {
    return SliverToBoxAdapter(
      child: Padding(
        padding: const EdgeInsets.only(top: 20, bottom: 0),
        child: Column(
          children: [
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                const SizedBox(width: 20),
                Expanded(
                  child: Container(
                    height: 1,
                    decoration: const BoxDecoration(
                      gradient: LinearGradient(
                        begin: Alignment.centerLeft,
                        end: Alignment.centerRight,
                        colors: [Color(0x00FFEECB), Color(0xFFFFB259)],
                      ),
                    ),
                  ),
                ),
                const SizedBox(width: 8),
                ShaderMask(
                  shaderCallback: (Rect bounds) {
                    return const LinearGradient(
                      begin: Alignment.topRight,
                      end: Alignment.bottomLeft,
                      colors: <Color>[Color(0xFFFFEECB), Color(0xFFFFB259)],
                    ).createShader(bounds);
                  },
                  blendMode: BlendMode.srcATop,
                  child: Text(
                    "kaye_trade_delivery_slutty".tr,
                    style: const TextStyle(
                      fontSize: 18,
                      fontWeight: FontWeight.bold,
                      color: Color(0xFFFFB259),
                    ),
                  ),
                ),
                const SizedBox(width: 8),
                Expanded(
                  child: Container(
                    height: 1,
                    decoration: const BoxDecoration(
                      gradient: LinearGradient(
                        begin: Alignment.centerLeft,
                        end: Alignment.centerRight,
                        colors: [Color(0xFFFFB259), Color(0x00FFEECB)],
                      ),
                    ),
                  ),
                ),
                const SizedBox(width: 20),
              ],
            ),
            const SizedBox(height: 10),
            KayeDotDeliveryDry(
              type: logic.showFunctionType,
              showUserInfo:
                  logic.showUserVipInfo &&
                  logic.showFunctionType != KayeDeliveryDryWrestling.LIST_VIEW,
            ),
          ],
        ),
      ),
    );
  }

  Widget _kayeDeliveryTrustPassengerDrovePlayhouse(
    BuildContext context,
    KayeBuryPatsy data,
    int index,
  ) {
    payCallback() {
      logic.onKayeSprintPatsy(data);
    }

    double width = (MediaQuery.of(context).size.width - 70.0) / 3;
    Widget body = Container(
      width: width,
      height: 160,
      decoration: BoxDecoration(borderRadius: BorderRadius.circular(16)),
      child: Stack(
        children: [
          Positioned(
            top: 0,
            left: 0,
            right: 0,
            child: _kayeDeliveryTrustPassengerDrove(
              context,
              data,
              payEvent: payCallback,
            ),
          ),
          Positioned(
            left: 0,
            right: 0,
            top: 0,
            child: Visibility(
              visible: data.isBestOffer || data.showDiscount,
              child: _kayeDeliveryTrustPassengerDunphyAssistant(data),
            ),
          ),
        ],
      ),
    );
    return body;
  }

  Widget _kayeDeliveryTrustPassengerDrove(
    BuildContext context,
    KayeBuryPatsy data, {
    VoidCallback? payEvent,
  }) {
    double width = (MediaQuery.of(context).size.width - 48.0) / 2;
    bool isAr = KayeIOBarnacle.isARLanguage();
    return GestureDetector(
      onTap: payEvent,
      child: Container(
        width: width,
        height: 160,
        decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(20),
          color: data.isLifetime
              ? const Color(0xFF6900BE)
              : KayeToddlerBarnacle.white_20p,
          border: data.isLifetime
              ? Border.all(color: const Color(0xFFFFBF44), width: 2)
              : null,
        ),
        child: Stack(
          alignment: Alignment.topCenter,
          children: [
            Column(
              children: [
                data.isLifetime
                    ? Expanded(
                        child: Padding(
                          padding: const EdgeInsets.only(top: 15),
                          child: Column(
                            crossAxisAlignment: CrossAxisAlignment.center,
                            mainAxisAlignment: MainAxisAlignment.start,
                            children: [
                              Container(
                                width: data.isLifetime ? 56 : 48,
                                alignment: Alignment.center,
                                child: data.isLifetime
                                    ? KayeSydney.local(
                                        fileName: "kaye_ten_delivery_interface",
                                        width: 42,
                                        height: 42,
                                        fit: BoxFit.contain,
                                      )
                                    : Text(
                                        "${data.name_amount}",
                                        style: KayeCreepDesperate.style(
                                          color: const Color(0xFFFFC34F),
                                          fontSize: 36,
                                          fontWeight: FontWeight.bold,
                                        ),
                                      ),
                              ),
                              Text(
                                data.isLifetime
                                    ? "kaye_trade_yip".tr
                                    : data.unitTitle,
                                style: KayeCreepDesperate.style(
                                  color: KayeToddlerBarnacle.white,
                                  fontFamily: AppText.fontFamily,
                                  fontWeight: FontWeight.bold,
                                  fontSize: 14,
                                ),
                              ),
                            ],
                          ),
                        ),
                      )
                    : Expanded(
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.center,
                          mainAxisAlignment: MainAxisAlignment.start,
                          children: [
                            const SizedBox(height: 25),
                            Text(
                              "${data.name_amount}",
                              style: KayeCreepDesperate.style(
                                color: const Color(0xFFFFC34F),
                                fontSize: 32,
                                fontWeight: FontWeight.bold,
                                height: 1.0,
                              ),
                            ),
                            Text(
                              data.unitTitle,
                              style: KayeCreepDesperate.style(
                                color: KayeToddlerBarnacle.white,
                                fontSize: 16,
                              ),
                            ),
                          ],
                        ),
                      ),
                _kayeDeliveryTrustPassengerVirginElectrify(data),
              ],
            ),
            Positioned(
              bottom: 48,
              child: Visibility(
                visible: data.extra_value > 0,
                child: Container(
                  height: 20,
                  padding: const EdgeInsets.only(left: 6, right: 6),
                  decoration: BoxDecoration(
                    color: const Color(0xFF8321FF),
                    borderRadius: BorderRadius.circular(10),
                  ),
                  child: Row(
                    children: [
                      Text(
                        "+${data.extra_value.toStringAsFixed(0)}",
                        style: KayeCreepDesperate.style(
                          color: KayeToddlerBarnacle.white,
                          fontWeight: FontWeight.bold,
                          fontSize: 12,
                        ),
                      ),
                      const SizedBox(width: 2),
                      KayeSydney.local(
                        fileName: "kaye_ten_float_masculine_interface",
                        width: 12,
                        height: 12,
                        fit: BoxFit.contain,
                      ),
                    ],
                  ),
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _kayeDeliveryTrustPassengerDunphyAssistant(KayeBuryPatsy option) {
    String title = "";
    if (option.isBestOffer) {
      title = "kaye_trade_artificial_finalist".tr;
    } else if (option.showDiscount) {
      title = "${option.discountStr} ${'kaye_trade_boom'.tr}";
    }
    return Container(
      height: 24,
      decoration: const BoxDecoration(
        borderRadius: BorderRadius.only(
          topLeft: Radius.circular(16),
          topRight: Radius.circular(16),
        ),
        gradient: LinearGradient(
          colors: [Color(0xFFFFED4D), Color(0xFF3DFFA2)],
          begin: Alignment.bottomRight,
          end: Alignment.center,
        ),
      ),
      child: Center(
        child: Text(
          title,
          style: KayeCreepDesperate.style(
            fontSize: 10,
            color: KayeToddlerBarnacle.black,
            fontWeight: FontWeight.bold,
          ),
        ),
      ),
    );
  }

  Widget _kayeDeliveryTrustPassengerVirginElectrify(KayeBuryPatsy data) {
    return Container(
      height: 40,
      decoration: const BoxDecoration(
        borderRadius: BorderRadius.only(
          bottomLeft: Radius.circular(16),
          bottomRight: Radius.circular(16),
        ),
        color: KayeToddlerBarnacle.white_10p,
      ),
      child: Center(
        child: Text(
          data.priceStr,
          style: KayeCreepDesperate.style(
            color: data.isLifetime
                ? KayeToddlerBarnacle.white
                : KayeToddlerBarnacle.white,
            fontWeight: FontWeight.bold,
            fontSize: 12,
          ),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }

  Widget _kayeHerniaCreep(BuildContext context) {
    var normalStyle = const TextStyle(
      fontSize: 12,
      color: Color(0xFF999999),
      fontWeight: FontWeight.normal,
    );
    var highStyle = const TextStyle(
      fontSize: 12,
      color: Color(0xFFFF6A3A),
      fontWeight: FontWeight.bold,
    );

    List<InlineSpan> children = [
      TextSpan(
        text: "kaye_trade_sasquatch_perform".tr,
        style: highStyle,
        recognizer: TapGestureRecognizer()
          ..onTap = logic.goKayeSasquatchPerformConcerned,
      ),
    ];
    if (logic.isIOS) {
      children.add(
        TextSpan(text: ' ${"kaye_trade_chad".tr} ', style: normalStyle),
      );
      children.add(
        TextSpan(
          text: "kaye_trade_october_perform".tr,
          style: highStyle,
          recognizer: TapGestureRecognizer()
            ..onTap = logic.goKayeOctoberPerformConcerned,
        ),
      );
    }

    return RichText(
      textAlign: TextAlign.center,
      text: TextSpan(
        text: '${"kaye_trade_by_size_suspect_backseat_to".tr} ',
        style: normalStyle,
        children: children,
      ),
    );
  }
}
