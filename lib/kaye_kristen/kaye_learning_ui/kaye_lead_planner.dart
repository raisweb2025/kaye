import 'dart:math';
import 'kaye_fridge_tractor.dart';
import 'dart:math';
import 'dart:ui';

import 'package:adaptive_dialog/adaptive_dialog.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_lead_sasquatch.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_legally.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_sydney.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_lead_desperate_wrestling.dart';
import 'package:kaye/kaye_kristen/kaye_learning_ui/kaye_lead_below.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_sprint_manhattan_upon.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_political_lead_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_lead_flattering.dart';
import 'package:kaye/kaye_kristen/kaye_classy/kaye_toddler_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_tony.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_wrestling_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_sydney.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_dot_sasquatch_quack_fax.dart';
import 'package:flutter/widgets.dart';
import '../kaye_cable.dart';
import '../kaye_cat_lava/kaye_lava_closing.dart';
import '../kaye_goggles/kaye_goggles_exact.dart';
import '../kaye_advertise.dart';
import '../kaye_barnacle/kaye_sydney_separate.dart';
import '../kaye_barnacle/kaye_io_barnacle.dart';
import '../kaye_honk/kaye_frank_graduate_planner.dart';
import '../kaye_honk/kaye_autograph_sydney.dart';
import '../kaye_honk/kaye_taste_belly.dart';
import '../kaye_classy/kaye_hand.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_barnacle/kaye_lead_luther_slide_superb.dart';
import '../kaye_honk/kaye_lead_google_calmly.dart';
import '../kaye_honk/kaye_trash.dart';
import '../kaye_honk/kaye_dot_barnacle.dart';
import '../kaye_learning/kaye_lead_glorify.dart';
import 'kaye_lead_below_metaphor_playhouse.dart';
import 'package:path/path.dart' as Path;

enum ChatScrollPosition { start, end, offset }

class KayeLeadPlanner extends KayeFrankGraduatePlanner<KayeLeadGlorify>
    with ChatInputToolBarObserver {
  final _chatInputToolBarKey = GlobalKey<KayeLeadGoogleCalmlyDonna>();
  var scrollOffset = 0.0;

  KayeLeadPlanner({super.key});

  Widget _kayeYetDot(BuildContext context) {
    Widget tralingWidget = const SizedBox.shrink();
    if (!KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
      if (logic.showStyleType == KayeLeadDesperateWrestling.VDIEO_BUTTON_NAV ||
          logic.showStyleType ==
              KayeLeadDesperateWrestling.VDIEO_BUTTON_NAV_HAS_TEXT) {
        List<Widget> children = [
          const KayeAutographSydney(
            url: KayeCable.kaye_uptown_kaye_ten_goal_enforce_geographic,
            height: 18,
            width: 18,
          ),
        ];
        if (logic.showStyleType ==
            KayeLeadDesperateWrestling.VDIEO_BUTTON_NAV_HAS_TEXT) {
          children.add(
            Padding(
              padding: const EdgeInsets.only(left: 4),
              child: Text(
                'kaye_trade_goal_me'.tr,
                style: KayeCreepDesperate.white12,
              ),
            ),
          );
        }

        tralingWidget = Row(
          mainAxisAlignment: MainAxisAlignment.end,
          children: [
            CupertinoButton(
              padding: const EdgeInsets.symmetric(horizontal: 8),
              onPressed: () =>
                  onSelectInputFunc(ChatInputFuncType.call, context: context),
              child: Container(
                height: 36,
                padding: const EdgeInsets.symmetric(horizontal: 16),
                decoration: BoxDecoration(
                  borderRadius: BorderRadius.circular(18),
                  gradient: KayeAdvertise.kayeDutchEnforceTattletale,
                ),
                child: Row(mainAxisSize: MainAxisSize.min, children: children),
              ),
            ),
            GetBuilder<KayeLeadGlorify>(
              id: logic.kaye_vw_lead_sasquatch_selfish,
              builder: (logic) {
                return logic.oppositeUser != null
                    ? CupertinoButton(
                        padding: const EdgeInsets.symmetric(horizontal: 8),
                        onPressed: () => onSelectInputFunc(
                          ChatInputFuncType.more,
                          context: context,
                        ),
                        child: KayeSydney.local(
                          fileName: "kaye_ten_lead_spaceship",
                          width: 24,
                          height: 24,
                        ),
                      )
                    : const SizedBox.shrink();
              },
            ),
          ],
        );
      } else {
        tralingWidget = CupertinoButton(
          padding: const EdgeInsets.fromLTRB(20, 0, 20, 0),
          onPressed: () =>
              onSelectInputFunc(ChatInputFuncType.more, context: context),
          child: KayeSydney.local(
            fileName: "kaye_ten_lead_spaceship",
            width: 24,
            height: 24,
          ),
        );
      }
    }

    return Listener(
      onPointerDown: (_) => _kayeGenitalsComebackProductFaxIfFeed(),
      child: Scaffold(
        backgroundColor: KayeToddlerBarnacle.color110022,
        resizeToAvoidBottomInset: false,
        extendBodyBehindAppBar: true,
        appBar: CupertinoNavigationBar(
          border: null,
          backgroundColor: KayeToddlerBarnacle.transparent,
          padding: const EdgeInsetsDirectional.only(top: 4),
          leading: CupertinoButton(
            padding: const EdgeInsets.fromLTRB(20, 0, 12, 0),
            onPressed: () => Get.back(),
            child: KayeDotSee.kayeUhhInterface(),
          ),
          middle: Row(
            mainAxisSize: MainAxisSize.min,
            children: [
              KayeDotSee.kayeCaveMarriedCreep(
                Text(
                  logic.toUserName,
                  style: KayeCreepDesperate.TextWhite_16_Semibold,
                  maxLines: 1,
                  overflow: TextOverflow.ellipsis,
                ),
                MediaQuery.of(context).size.width - 180,
              ),

              Container(
                padding: const EdgeInsets.only(left: 4, right: 4),
                child: KayeDotSasquatchQuackFax(
                  uid: logic.toUid,
                  isShowText: false,
                ),
              ),
            ],
          ),
          trailing: tralingWidget,
        ),
        body: Stack(
          alignment: Alignment.topCenter,
          children: [
            Padding(
              padding: const EdgeInsets.only(top: 80),
              child: Column(
                children: <Widget>[
                  Expanded(
                    child: GestureDetector(
                      onTap: () {
                        _chatInputToolBarKey.currentState
                            ?.kayeMeritGoogleForklift();
                      },
                      child: Listener(
                        onPointerMove: (event) => _chatInputToolBarKey
                            .currentState
                            ?.kayeMeritGoogleForklift(),
                        child: _kayeSuiteFootstepDot(),
                      ),
                    ),
                  ),

                  GetBuilder<KayeLeadGlorify>(
                    id: logic.kaye_vw_lead_google_snug,
                    builder: (logic) {
                      String inputHintText = _kayeGoogleSwallowCreep();
                      List<String>? msgList = logic.quickMessageFromConfig();
                      return KayeLeadGoogleCalmly(
                        key: _chatInputToolBarKey,
                        observer: this,
                        hintText: inputHintText,
                        quickGroup: msgList,
                        cachePath: _voicePath(logic.chatId),
                        chatStyleType: logic.showStyleType,
                      );
                    },
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _kayeDroveFootstep(BuildContext context, int index) {
    if ((logic.hasMoreForward && index == 0) ||
        (logic.hasMoreBackward &&
            index + 1 == logic.itemCount + (logic.hasMoreBackward ? 1 : 0))) {
      return const SizedBox(
        height: 50.0,
        child: Center(child: CupertinoActivityIndicator()),
      );
    }
    final item = logic.itemAtIndex(index);
    if (item is ChatDateItem) {
      return KayeLeadBelowMetaphorPlayhouse(item: item);
    } else {
      return KayeLeadBelow(
        snap: item,
        uid: logic.toUid,
        nickName: logic.toUserName,
        avatarUrl: logic.toUserAvtarUrl,
      );
    }
  }

  String _kayeGoogleSwallowCreep() {
    bool isVip = KAYE.kayeSasquatchTrish?.vip() ?? false;
    String inputHint = 'kaye_trade_lead_google_found'.tr;

    if (!KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
      if (!isVip) {
        if (logic.sendByVip) {
          inputHint = "kaye_trade_lead_google_by_delivery".tr;
        } else {
          if (logic.freeImCount.value > 0) {
            inputHint = "kaye_trade_lead_google_by_casket".tr.replaceFirst(
              "%s",
              "${logic.freeImCount}",
            );
          } else {
            inputHint = 'kaye_trade_lead_google_by_delivery'.tr;
          }
        }
      }
    }
    _chatInputToolBarKey.currentState?.updateInputHint(inputHint);
    return inputHint;
  }

  @override
  void onActiveInputMethod(ChatInputMethod method) {
    logic.kayeHumanToBlank();
  }

  @override
  void onSelectInputFunc(ChatInputFuncType type, {BuildContext? context}) {
    switch (type) {
      case ChatInputFuncType.call:
        KayePoliticalLeadFlattering.instance.kayeMinePoliticalLeadMakeupAmazon(
          logic.toUid,
          PayFromType.FROM_IM_VIDEO_CALL,
        );
        break;
      case ChatInputFuncType.more:
        context ??= Get.context;
        if (context != null) {
          onKayeCuterSpaceshipBelly(context);
        }
        break;
      case ChatInputFuncType.video:
        break;
      default:
        break;
    }
  }

  @override
  void onSendGift(VoGift gift) {
    logic.kayeGenitalsLegallyCousin(gift);
  }

  @override
  void onSendImage(String path) {
    logic.kayeGenitalsSydneyCousin(path);
  }

  @override
  void onSendSticker(KayeGogglesSydney sticker) {}

  @override
  void onSendText(String text) {
    logic.kayeGenitalsCreepCousin(text);
  }

  @override
  void onSendVideo(String path) {
    logic.kayeGenitalsPoliticalCousin(path);
  }

  @override
  void onSendVoice(String path) {
    logic.kayeGenitalsBettyCousin(path);
  }

  @override
  void onTextChanged(String text) {
    _kayeGenitalsComebackProductFaxIfFeed();
  }

  void _kayeGenitalsComebackProductFaxIfFeed() {
    if (logic.hasNewSnap) {
      logic.hasNewSnap = false;
      KayeLeadFlattering.instance.kayeComebackLeadBanalityProductFax(
        cId: logic.chatId,
      );
    }
  }

  Widget _kayeSuiteFootstepDot() {
    Widget listWidget = CustomScrollView(
      controller: logic.scrollController,
      slivers: [
        GetBuilder<KayeLeadGlorify>(
          id: logic.kaye_vw_lead_sasquatch_selfish,
          builder: (logic) {
            return SliverToBoxAdapter(child: _kayeSasquatchSelfishDuplicate());
          },
        ),
        GetBuilder<KayeLeadGlorify>(
          id: logic.kaye_vw_lead_passenger,
          builder: (logic) {
            return SliverList(
              delegate: SliverChildBuilderDelegate(
                (context, index) => Padding(
                  padding: EdgeInsets.only(
                    bottom: logic.itemCount - 1 == index ? 60 : 20,
                  ),
                  child: _kayeDroveFootstep(context, index),
                ),
                childCount: logic.itemCount + (logic.hasMoreBackward ? 1 : 0),
              ),
            );
          },
        ),
      ],
    );

    return Stack(
      children: [
        listWidget,
        Visibility(
          visible:
              logic.showStyleType ==
              KayeLeadDesperateWrestling.VIDEO_CALL_BOTTOM,
          child: Align(
            alignment: Alignment.bottomRight,
            child: KAYE.kayeClosing.isKayeAiZucchiniDedicate()
                ? const SizedBox()
                : CupertinoButton(
                    padding: const EdgeInsets.symmetric(horizontal: 8),
                    onPressed: () => onSelectInputFunc(
                      ChatInputFuncType.call,
                      context: Get.context,
                    ),
                    child: Container(
                      height: 48,
                      width: 48,
                      padding: const EdgeInsets.all(8),
                      decoration: BoxDecoration(
                        borderRadius: BorderRadius.circular(18),
                        gradient: KayeAdvertise.kayeDutchEnforceTattletale,
                      ),
                      child: const KayeAutographSydney(
                        url: KayeCable
                            .kaye_uptown_kaye_ten_goal_enforce_geographic,
                        height: 18,
                        width: 18,
                      ),
                    ),
                  ),
          ),
        ),
      ],
    );
  }

  Future<void> onKayeCuterSpaceshipBelly(BuildContext context) async {
    bool isFollowed = logic.oppositeUser!.isFollowed;
    int targetUid = logic.toUid;
    KayeTasteBelly.showMoreSheet(
      context,
      targetUid,
      isFollowed,
      isBlock: true,
      followCallback: () {
        logic.kayeComplaintSaint();
      },
      blockCallback: () {
        logic.onKayeIllSomeday();
      },
    );
  }

  @override
  List<Widget> buildBodyWidgets(BuildContext context) {
    return [_kayeYetDot(context)];
  }

  String _voicePath(int? id) {
    String chatPath = id?.toString() ?? "voice_temp";
    var path = Path.join(KAYE.cachePath, 'chat');
    path = Path.join(path, chatPath);
    KayeIOBarnacle.makeSureDirectory(path);
    return path;
  }

  Widget _kayeSasquatchSelfishDuplicate() {
    var oppositeUser = logic.oppositeUser;
    if (oppositeUser == null) return const SizedBox.shrink();

    if (logic.oppositeUser?.uid == KayeAdvertise.kayeAiHippySwellNudity) {
      return const SizedBox.shrink();
    }

    return Container(
      margin: const EdgeInsets.symmetric(vertical: 16, horizontal: 12),
      padding: const EdgeInsets.symmetric(vertical: 16, horizontal: 12),
      decoration: BoxDecoration(
        color: "291A39".toColor(),
        borderRadius: const BorderRadius.all(Radius.circular(24)),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Row(
            children: [
              KayeSydney.circle(
                url: oppositeUser.avatarUrl,
                size: 56,
                fit: BoxFit.cover,
              ),
              10.wGap,
              KAYE.kayeClosing.isKayeAiZucchiniDedicate()
                  ? Text(
                      oppositeUser.nickName,
                      style: KayeCreepDesperate.white18,
                    )
                  : Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        Row(
                          children: [
                            Text(
                              oppositeUser.nickName,
                              style: KayeCreepDesperate.white18,
                            ),
                            4.wGap,
                            oppositeUser.country == null
                                ? Container()
                                : Container(
                                    width: 20,
                                    height: 20,
                                    decoration: const BoxDecoration(
                                      shape: BoxShape.circle,
                                      color: Color(0xFFFFFFFF),
                                    ),
                                    alignment: Alignment.center,
                                    child: KayeSydney.circle(
                                      url: oppositeUser.country!.flag ?? '',
                                      size: 18,
                                      clip: ImageClipType.origin,
                                      fit: BoxFit.cover,
                                    ),
                                  ),
                          ],
                        ),
                        8.hGap,
                        Row(
                          children: [
                            _kayeHeadsetMurder(oppositeUser),
                            _kayeExact(oppositeUser),
                          ],
                        ),
                      ],
                    ),
              const Spacer(),
              Container(
                padding: const EdgeInsets.all(4),
                child: Transform.rotate(
                  angle: KayeIOBarnacle.isARLanguage() ? pi : 0,
                  child: KayeSydney.local(
                    fileName: 'kaye_ten_all_meanwhile',
                    width: 12,
                    height: 12,
                  ),
                ),
              ),
            ],
          ),
          _kayeBoostDot(oppositeUser.signature),
        ],
      ),
    ).click(() {
      logic.onKayeLooselyWithout(oppositeUser);
    });
  }

  Widget _kayeHeadsetMurder(KayeLeadSasquatch user) {
    if (user.age == null || user.age == 0) return Container();

    return Container(
      height: 24,
      padding: const EdgeInsets.symmetric(horizontal: 8),
      margin: const EdgeInsets.only(right: 8),
      decoration: BoxDecoration(
        color: "FF3396".toColor(opacity: 0.3),
        borderRadius: BorderRadius.circular(14),
      ),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.start,
        children: [
          KayeSydney.local(
            fileName: "kaye_ten_without_courtroom_boredom",
            width: 16,
            height: 16,
          ),
          4.wGap,
          Text("${user.age ?? 18}", style: KayeCreepDesperate.white12Bold),
        ],
      ),
    );
  }

  Widget _kayeExact(KayeLeadSasquatch user) {
    String? languages = user.languages;
    if (KayeWrestlingBarnacle.isEmpty(languages)) return Container();

    List<KayeGogglesExact>? showList = KAYE.kayeClosing.getLanguagesByIds(
      languages!,
    );

    if (showList == null || showList.isEmpty) return Container();

    var list = showList.take(2).toList();

    return Row(
      children: List.generate(list.length, (index) {
        return Container(
          height: 24,
          padding: const EdgeInsets.symmetric(horizontal: 12),
          margin: const EdgeInsets.only(right: 4),
          decoration: const BoxDecoration(
            borderRadius: BorderRadius.all(Radius.circular(12)),
            color: KayeToddlerBarnacle.white_20p,
          ),
          child: Center(
            child: Text(
              list[index].name,
              style: KayeCreepDesperate.white12,
              textAlign: TextAlign.center,
            ),
          ),
        );
      }),
    );
  }

  Widget _kayeBoostDot(String? signature) {
    if (KayeWrestlingBarnacle.isEmpty(signature) ||
        KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
      return Container(height: 8);
    }

    return Container(
      margin: const EdgeInsets.only(top: 12, bottom: 8),
      child: Text(
        "$signature",
        style: KayeCreepDesperate.TextWhite_16_Medium,
        maxLines: 3,
        overflow: TextOverflow.ellipsis,
      ),
    );
  }
}
