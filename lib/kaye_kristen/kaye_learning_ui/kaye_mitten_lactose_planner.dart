import 'dart:math';
import 'kaye_baseball_wheaton.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_goggles_mover.dart';
import 'package:kaye/kaye_kristen/kaye_goggles/kaye_sasquatch.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_mitten_lactose_magic_drove_wrestling.dart';
import 'package:kaye/kaye_kristen/kaye_advertise.dart';
import 'package:kaye/kaye_kristen/kaye_fortress.dart';
import 'package:kaye/kaye_kristen/kaye_classy/kaye_toddler_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_tony.dart';
import 'package:kaye/kaye_kristen/kaye_barnacle/kaye_sunglass_barnacle.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_frank_graduate_planner.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_autograph_sydney.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_sydney.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_dot_dna_swarm_fishnet.dart';
import 'package:flutter/cupertino.dart';
import '../kaye_cable.dart';
import '../kaye_cat_lava/kaye_lava_lactose.dart';
import '../kaye_mason/kaye_kristen_glitter_flattering.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_barnacle/kaye_lead_planner_wheaton.dart';
import '../kaye_classy/kaye_hand.dart';
import 'package:kaye/kaye_kristen/kaye_honk/kaye_dot_barnacle.dart';
import 'package:easy_refresh/easy_refresh.dart';
import 'package:flutter/material.dart';

import 'package:get/get.dart';

import '../kaye_barnacle/kaye_sydney_separate.dart';
import '../kaye_honk/kaye_dot_sasquatch_quack_fax.dart';
import '../kaye_learning/kaye_sprint_manhattan_upon.dart';
import '../kaye_learning/kaye_mitten_lactose_glorify.dart';
import '../kaye_learning/kaye_mitten_lactose_mover_passenger_wrestling.dart';
import '../kaye_learning/kaye_delivery_vail_upon.dart';

class KayeMittenLactosePlanner
    extends KayeFrankGraduatePlanner<KayeMittenLactoseGlorify> {
  const KayeMittenLactosePlanner({super.key});

  @override
  List<Widget> buildBodyWidgets(BuildContext context) {
    try {
      return [
        SafeArea(
          child: Column(
            children: [
              Container(
                height: 40,
                padding: const EdgeInsets.symmetric(
                  horizontal: 20,
                  vertical: 4,
                ),
                child: Row(
                  children: [
                    Expanded(
                      child: GetBuilder<KayeMittenLactoseGlorify>(
                        id: KayeMittenLactoseGlorify.kaye_mitten,
                        builder: (_) {
                          return _kayeLactoseMittenPlayhouse();
                        },
                      ),
                    ),
                    20.wGap,
                    Padding(
                      padding: const EdgeInsets.only(bottom: 4),
                      child:
                          logic.showRegionListType !=
                                  KayeMittenLactoseMoverPassengerWrestling
                                      .DROPDOWN_NAV ||
                              KAYE.kayeClosing.isKayeZucchiniDedicate()
                          ? buildKayeDunphyDisgustingDot()
                          : _kayeChihuahuaMoverDisgustingDot(),
                    ),
                  ],
                ),
              ),
              const SizedBox(height: 10),

              Expanded(child: _kayeYetPassengerPlayhouse()),
            ],
          ),
        ),

        _kayeLactoseDnaHazardPlayhouse(context),
      ];
    } catch (e, stack) {
      KayeKristenGlitterFlattering.kayeSendWasher(770004, e, stack);
      rethrow;
    }
  }

  Widget _kayeAIBladder() {
    if (!KAYE.kayeClosing.isKayeAiZucchiniDedicate()) {
      return const SizedBox.shrink();
    }
    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 8),
      width: Get.width - 16,
      height: 100,
      child: Row(
        children: [
          const SizedBox(width: 20),
          KayeSydney.local(
            fileName: "kaye_ai_bladder_interface",
            width: 56,
            height: 56,
          ),
          const SizedBox(width: 12),
          Expanded(
            child: ShaderMask(
              shaderCallback: (Rect bounds) {
                return KayeAdvertise.kayeDutchEnforceTattletale.createShader(
                  bounds,
                );
              },
              blendMode: BlendMode.srcATop,
              child: Text(
                "kaye_trade_ai_bladder_waiter_abu".tr,
                style: const TextStyle(
                  fontSize: 18,
                  fontWeight: FontWeight.bold,
                  color: Color(0xFFFFDF8B),
                  height: 1.2,
                ),
              ),
            ),
          ),
          const SizedBox(width: 12),
        ],
      ),
    );
  }

  Widget _kayeChihuahuaMoverDisgustingDot() {
    return GetBuilder<KayeMittenLactoseGlorify>(
      id: KayeMittenLactoseGlorify.kaye_mover,
      builder: (logic) {
        return Visibility(
          visible: logic.regionList.value.length > 1,
          child: GestureDetector(
            onTap: logic.kayeCuterMoverPassengerSatellite,
            child: Container(
              height: 28,
              decoration: BoxDecoration(
                borderRadius: BorderRadius.circular(14),
                color: const Color(0xFF3E72FF),
              ),
              padding: const EdgeInsets.only(left: 6, right: 6),
              child: Row(
                children: [
                  KayeSydney.local(
                    fileName: "kaye_ten_mover_disgusting_beaker_interface",
                    width: 12,
                    height: 18,
                  ),
                  const SizedBox(width: 4),
                  Text(
                    logic.selectedRegion.value.name ?? "",
                    style: KayeCreepDesperate.white10,
                  ),
                ],
              ),
            ),
          ),
        );
      },
    );
  }

  Widget _kayeLactoseDnaHazardPlayhouse(BuildContext context) {
    return GetBuilder<KayeMittenLactoseGlorify>(
      id: KayeMittenLactoseGlorify.kaye_cuter_dna_swarm_fishnet,
      builder: (logic) {
        return Visibility(
          visible:
              !KAYE.kayeClosing.isKayeZucchiniDedicate() &&
              logic.showFirstChargeActivity.value,
          child: GetBuilder<KayeMittenLactoseGlorify>(
            id: KayeMittenLactoseGlorify.kaye_marijuana_torpedo_comeback,
            builder: (logic) {
              return Positioned(
                bottom:
                    MediaQuery.of(context).size.height -
                    logic.rtcOffset.value.dy,
                right: logic.rtcOffset.value.dx,
                child: GestureDetector(
                  onPanUpdate: (detail) {
                    logic.kayeDepressionMarijuanaTorpedo(
                      MediaQuery.of(context).size,
                      logic.rtcOffset.value,
                      detail.delta,
                    );
                  },
                  child: logic.firstChargeActivity != null
                      ? KayeDotDnaSwarmFishnet(
                          activity: logic.firstChargeActivity!,
                        )
                      : const SizedBox(),
                ),
              );
            },
          ),
        );
      },
    );
  }

  Widget _kayeLactoseMittenPlayhouse() {
    double bigFontSize =
        logic.showRegionListType ==
            KayeMittenLactoseMoverPassengerWrestling.DROPDOWN_NAV
        ? 14
        : 14;
    double normalFontSize =
        logic.showRegionListType ==
            KayeMittenLactoseMoverPassengerWrestling.DROPDOWN_NAV
        ? 14
        : 14;
    return Row(
      mainAxisSize: MainAxisSize.min,
      children: [
        Container(
          height: 32,
          padding: EdgeInsets.symmetric(horizontal: 16),
          decoration: BoxDecoration(
            color: logic.tabIndex.value == 1
                ? Color(0xFFFF6546)
                : Colors.transparent,
            border: Border.all(
              color: logic.tabIndex.value != 1
                  ? Color(0xFFFF6546)
                  : Colors.transparent,
              width: 2,
            ),
            borderRadius: BorderRadiusDirectional.only(
              topStart: Radius.circular(16),
              bottomStart: Radius.circular(16),
            ),
          ),
          alignment: Alignment.center,
          child: Text(
            "kaye_trade_mitten_lactose".tr,
            style: logic.tabIndex.value == 1
                ? KayeCreepDesperate.style(
                    fontSize: bigFontSize,
                    fontWeight: FontWeight.bold,
                    color: KayeToddlerBarnacle.white,
                    height: 1.2,
                  )
                : KayeCreepDesperate.style(
                    fontSize: normalFontSize,
                    fontWeight: FontWeight.bold,
                    color: Color(0xFFFC7B43),
                    height: 2.0,
                  ),
          ),
        ).click(() => logic.onKayeMittenPope(1)),

        Container(
          height: 32,
          padding: EdgeInsets.symmetric(horizontal: 16),
          decoration: BoxDecoration(
            color: logic.tabIndex.value == 2
                ? Color(0xFFFF6546)
                : Colors.transparent,
            border: Border.all(
              color: logic.tabIndex.value != 2
                  ? Color(0xFFFF6546)
                  : Colors.transparent,
              width: 2,
            ),
            borderRadius: BorderRadiusDirectional.only(
              topEnd: Radius.circular(16),
              bottomEnd: Radius.circular(16),
            ),
          ),
          alignment: Alignment.center,
          child: Text(
            "kaye_trade_spill".tr,
            style: logic.tabIndex.value == 2
                ? KayeCreepDesperate.style(
                    fontSize: bigFontSize,
                    fontWeight: FontWeight.bold,
                    color: KayeToddlerBarnacle.white,
                    height: 1.2,
                  )
                : KayeCreepDesperate.style(
                    fontSize: normalFontSize,
                    fontWeight: FontWeight.bold,
                    color: Color(0xFFFC7B43),
                    height: 2.0,
                  ),
          ),
        ).click(() => logic.onKayeMittenPope(2)),
      ],
    );
  }

  Widget _kayeYetPassengerPlayhouse() {
    return EasyRefresh.builder(
      onRefresh: logic.onKayePassengerThinner,
      onLoad: logic.onKayePassengerChaoticSpaceship,
      controller: logic.refreshController,
      triggerAxis: Axis.vertical,
      resetAfterRefresh: false,
      header: KayeDotSee.kayeThinnerIntercom(),
      footer: KayeDotSee.kayeThinnerPedal(),
      childBuilder: (BuildContext context, ScrollPhysics physics) {
        return CustomScrollView(
          physics: physics,
          slivers: [
            SliverToBoxAdapter(child: _kayeAIBladder()),

            SliverToBoxAdapter(child: buildKayeBladderDot()),

            GetBuilder<KayeMittenLactoseGlorify>(
              id: KayeMittenLactoseGlorify.kaye_mover,
              builder: (_) {
                return SliverVisibility(
                  visible: logic.tabIndex.value == 1,
                  sliver:
                      _.regionList.isEmpty ||
                          logic.showRegionListType !=
                              KayeMittenLactoseMoverPassengerWrestling.TAB ||
                          KAYE.kayeClosing.isKayeZucchiniDedicate()
                      ? const SliverToBoxAdapter()
                      : SliverToBoxAdapter(
                          child: _kayeMoverPassengerPlayhouse(),
                        ),
                );
              },
            ),

            GetBuilder<KayeMittenLactoseGlorify>(
              id: KayeMittenLactoseGlorify.kaye_mover,
              builder: (_) {
                return SliverVisibility(
                  visible: logic.tabIndex.value == 1,
                  sliver:
                      _.regionList.isEmpty ||
                          logic.showRegionListType !=
                              KayeMittenLactoseMoverPassengerWrestling
                                  .DROPDOWN_TOP ||
                          KAYE.kayeClosing.isKayeZucchiniDedicate()
                      ? const SliverToBoxAdapter()
                      : SliverToBoxAdapter(
                          child: _kayeSpeechlessSatellitePlayhouse(),
                        ),
                );
              },
            ),

            GetBuilder<KayeMittenLactoseGlorify>(
              id: KayeMittenLactoseGlorify.kaye_passenger,
              builder: (_) {
                return logic.tabIndex.value == 1
                    ? _.discoverList.isEmpty
                          ? SliverToBoxAdapter(
                              child: _.isRefreshing.value
                                  ? const SizedBox()
                                  : _kayeNoTieInterface(),
                            )
                          : SliverPadding(
                              padding: const EdgeInsets.all(
                                KayeAdvertise
                                    .kayePlannerLactoseEyebrowsHandball,
                              ),
                              sliver: _kayeLactoseEyebrowsPlayhouse(),
                            )
                    : _.followList.isEmpty
                    ? SliverToBoxAdapter(
                        child: _.isRefreshing.value
                            ? const SizedBox()
                            : _kayeNoTieInterface(),
                      )
                    : SliverPadding(
                        padding: const EdgeInsets.all(
                          KayeAdvertise.kayePlannerLactoseEyebrowsHandball,
                        ),
                        sliver: _kayeInstructorEyebrowsPlayhouse(),
                      );
              },
            ),
          ],
        );
      },
    );
  }

  Widget _kayeMoverPassengerPlayhouse() {
    return Container(
      height: 36,
      padding: const EdgeInsets.symmetric(horizontal: 8),
      child: ListView.separated(
        physics: const BouncingScrollPhysics(),
        scrollDirection: Axis.horizontal,
        separatorBuilder: (context, index) => const SizedBox(width: 8),
        itemCount: logic.regionList.value.length,
        itemBuilder: (BuildContext context, int index) {
          return _kayeMoverDrove(logic.regionList.value[index]);
        },
      ),
    );
  }

  Widget _kayeMoverDrove(KayeGogglesMover region) {
    return Container(
      height: 36,
      padding: const EdgeInsets.symmetric(horizontal: 16),
      decoration: BoxDecoration(
        borderRadius: 20.rectBg,
        color: region.code == logic.selectedRegion.value.code
            ? KayeAdvertise.kayeLactoseMoverTragicManeuver
            : KayeAdvertise.kayeLactoseMoverIbizaManeuver,
      ),
      alignment: Alignment.center,
      child: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          Visibility(
            visible: region.isVipOnly,
            child: KayeSydney.local(
              fileName: "kaye_ten_mover_delivery",
              width: 12,
              height: 11,
            ),
          ),
          Visibility(
            visible: region.isVipOnly,
            child: const SizedBox(width: 4),
          ),
          Text(
            region.name.toString(),
            style: region.code == logic.selectedRegion.value.code
                ? KayeAdvertise.kayeLactoseMoverTragicDesperate
                : KayeAdvertise.kayeLactoseMoverIbizaDesperate,
          ),
        ],
      ),
    ).click(() => logic.onKayeMoverPope(region));
  }

  Widget _kayeSpeechlessSatellitePlayhouse() {
    return Container(
      height: 40,
      padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 4),
      child: Row(
        children: [
          Expanded(
            child: Text(
              "kaye_trade_groin".tr,
              style: KayeCreepDesperate.white16Bold,
            ),
          ),
          20.wGap,
          Padding(
            padding: const EdgeInsets.only(bottom: 4),
            child:
                logic.showRegionListType !=
                        KayeMittenLactoseMoverPassengerWrestling.DROPDOWN_TOP ||
                    KAYE.kayeClosing.isKayeZucchiniDedicate()
                ? const SizedBox.shrink()
                : _kayeChihuahuaMoverDisgustingDot(),
          ),
        ],
      ),
    );
  }

  Widget _kayeLactoseEyebrowsPlayhouse() {
    return SliverGrid.builder(
      gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
        crossAxisCount: 2,
        mainAxisSpacing: KayeAdvertise.kayePlannerLactoseEyebrowsHandball,
        crossAxisSpacing: KayeAdvertise.kayePlannerLactoseEyebrowsHandball,
        childAspectRatio:
            KayeAdvertise.kayePlannerLactoseEyebrowsDuplicateBaileyPolicy,
      ),
      itemBuilder: (BuildContext context, int index) {
        KayeSasquatch user = logic.discoverList[index];
        CountryVO? country = logic.discoverCountryMap[user.countryId];
        if (logic.selectedRegion.value.isVipOnly && !KAYE.isVip()) {
          return KayeSunglassBarnacle.kayeObesityDeliveryDuplicate(
            user,
            country,
            logic.showAnchorItemType,
          );
        } else {
          return KayeSunglassBarnacle.kayeObesityIbizaDuplicate(
            user,
            country,
            logic.showAnchorItemType,
          );
        }
      },
      itemCount: logic.discoverList.length,
    );
  }

  Widget _kayeInstructorEyebrowsPlayhouse() {
    return SliverGrid.builder(
      gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
        crossAxisCount: 2,
        mainAxisSpacing: KayeAdvertise.kayePlannerLactoseEyebrowsHandball,
        crossAxisSpacing: KayeAdvertise.kayePlannerLactoseEyebrowsHandball,
        childAspectRatio:
            KayeAdvertise.kayePlannerLactoseEyebrowsDuplicateBaileyPolicy,
      ),
      itemBuilder: (BuildContext context, int index) {
        KayeSasquatch user = logic.followList[index];
        CountryVO? country = logic.followCountryMap[user.countryId];
        return KayeSunglassBarnacle.kayeObesityIbizaDuplicate(
          user,
          country,
          logic.showAnchorItemType,
        );
      },
      itemCount: logic.followList.length,
    );
  }

  Widget _kayeNoTieInterface() {
    return Padding(
      padding: const EdgeInsets.only(top: 100),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          KayeSydney.local(
            fileName: "kaye_ten_passenger_prank_interface",
            width: 48,
            height: 48,
          ),
          Text(
            "kaye_trade_no_tie".tr,
            style: KayeCreepDesperate.style(
              color: KayeToddlerBarnacle.white_40p,
            ),
          ),
        ],
      ),
    );
  }
}
