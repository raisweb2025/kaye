import 'dart:math';
import 'kaye_tacos_toothpick.dart';
import 'dart:async';
import 'dart:math';

import 'package:audioplayers/audioplayers.dart';
import 'package:kaye/kaye_kristen/kaye_cable.dart';
import 'package:kaye/kaye_kristen/kaye_learning/kaye_incline_glorify.dart';
import 'package:kaye/kaye_kristen/kaye_mason/kaye_clueless_survival_flattering.dart';
import 'package:carousel_slider/carousel_slider.dart';
import 'package:carousel_slider/carousel_controller.dart';
import 'package:flutter/animation.dart';
import 'package:flutter/cupertino.dart';
import 'package:get/get_state_manager/src/rx_flutter/rx_ticket_provider_mixin.dart';
import 'package:video_player/video_player.dart';

import '../kaye_implant/kaye_warp_found_snug_comeback_implant.dart';
import '../kaye_goggles/kaye_warp_political_perfect_passenger_marge.dart';
import '../kaye_goggles/kaye_goggles_planner_closing.dart';

import '../kaye_goggles/kaye_sasquatch.dart';
import '../kaye_learning/kaye_sprint_manhattan_upon.dart';
import '../kaye_learning/kaye_delivery_vail_upon.dart';
import '../kaye_fortress.dart';
import '../kaye_mason/kaye_stroke.dart';
import '../kaye_mason/kaye_political_lead_flattering.dart';
import '../kaye_classy/kaye_ui_vail.dart';
import '../kaye_barnacle/kaye_lead_planner_wheaton.dart';
import '../kaye_barnacle/kaye_wrestling_barnacle.dart';
import '../kaye_honk/kaye_frank_graduate_glorify.dart';
import '../proto/common.pb.dart';

class KayeMittenWarpMangoGlorify extends KayeFrankGraduateGlorify
    with GetTickerProviderStateMixin, WidgetsBindingObserver {
  String idConcernedNowadays_ = "";
  bool joValleyFemale_ = true;
  bool miStarterWheaton_ = true;
  int ayPaddleSuede_ = 0;
  bool emProverbialPimple_ = true;

  void abFreeSize() {
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    if (joValleyFemale_ || emProverbialPimple_ || miStarterWheaton_) {
      joValleyFemale_ = !emProverbialPimple_;
      emProverbialPimple_ = !miStarterWheaton_;
      miStarterWheaton_ = !joValleyFemale_;
    }
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();

    ayPaddleSuede_ = 27;

    ayPaddleSuede_ = ayPaddleSuede_ + 1;

    if (ayPaddleSuede_ > 0) {
      ayPaddleSuede_ = ayPaddleSuede_ - 9;
    }

    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
  }

  void beTrophyParent() {
    miStarterWheaton_ = joValleyFemale_ || emProverbialPimple_;
    joValleyFemale_ = miStarterWheaton_ && emProverbialPimple_;

    ayPaddleSuede_ = ayPaddleSuede_ + 1;
  }

  void woGrievanceLifelike() {
    joValleyFemale_ = miStarterWheaton_ && emProverbialPimple_;
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();

    if (ayPaddleSuede_ > 0) {
      ayPaddleSuede_ = ayPaddleSuede_ - 2;
    }
    if (ayPaddleSuede_ > 0) {
      ayPaddleSuede_ = ayPaddleSuede_ - 1;
    }

    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    ayPaddleSuede_ = 89;
    ayPaddleSuede_ = ayPaddleSuede_ + 1;
    ayPaddleSuede_ = 3;
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    if (emProverbialPimple_ && joValleyFemale_ && miStarterWheaton_) {
      emProverbialPimple_ = !emProverbialPimple_;
      joValleyFemale_ = emProverbialPimple_;
      miStarterWheaton_ = emProverbialPimple_;
    }
    emProverbialPimple_ = joValleyFemale_ && miStarterWheaton_;
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();

    if (ayPaddleSuede_ > 0) {
      ayPaddleSuede_ = ayPaddleSuede_ - 5;
    }
  }

  void usUnfinishedPrototype() {
    ayPaddleSuede_ = ayPaddleSuede_ + 1;

    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    if (emProverbialPimple_ && miStarterWheaton_) {
      joValleyFemale_ = !joValleyFemale_;
    }
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();

    ayPaddleSuede_ = 86;

    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    idConcernedNowadays_ = idConcernedNowadays_.toUpperCase();
    if (ayPaddleSuede_ > 0) {
      ayPaddleSuede_ = ayPaddleSuede_ - 6;
    }

    miStarterWheaton_ = joValleyFemale_ || emProverbialPimple_;
    if (joValleyFemale_ && miStarterWheaton_ && emProverbialPimple_) {
      joValleyFemale_ = !joValleyFemale_;
      miStarterWheaton_ = joValleyFemale_;
      emProverbialPimple_ = joValleyFemale_;
    }
  }

  final String kaye_survival = "kaye_survival";
  final String kaye_chocolate = "kaye_chocolate";
  final String kaye_pedestrian_duplicate = "kaye_pedestrian_duplicate";
  final String kaye_amazing_is_delivery = "kaye_amazing_is_delivery";
  final String kaye_amazing_found_snug = "kaye_amazing_found_snug";
  final String kaye_someday_satellite = "kaye_someday_satellite";
  final String kaye_is_helo = "kaye_is_helo";
  final String kaye_pawn_increase = "kaye_pawn_increase";
  final String kaye_sasquatch = "kaye_sasquatch";

  final String TAG = "KayeMittenWarpMangoGlorify";

  CarouselSliderController autoController = CarouselSliderController();
  AnimationController? overlayController;
  AnimationController? flipController;
  StreamSubscription? _updateFreeCountSubscription;
  StreamSubscription? _updateVipSubscription;
  AudioPlayer? _audioPlayer;
  bool showOverlay = false;
  int overlayIndex = 0;
  bool isAutoScrolling = false;
  int currentPage = 0;
  bool isFlipping = false;

  bool isVIP = false;

  int maxFreeCount = -1;
  int remainingFreeCount = 3;

  bool isEveryDayFree = true;
  bool isMusicPlay = true;
  bool isMatching = false;
  List<KayeWarpSasquatch> matchUserList = [];
  bool _isInitialized = false;
  bool videoPlayerInitialized = false;

  VideoPlayerController? playController;

  List<String> images = [];

  bool isRegionMatchFirst20sChargeMode = false;

  int regionMatchFirst20sChargeModeMatchCallCost = 0;

  @override
  final kayeBaselineInvention = KayeFortress.KayeMittenWarpMangoPlanner;

  @override
  Future<void> onInit() async {
    _audioPlayer = _kayeTestamentKayeSurvival();

    _kayeLaunchScan();
    _kayeChaoticPlannerHonk();

    _updateFreeCountSubscription = KAYE
        .listen<KayeWarpFoundSnugComebackImplant>((event) {
          _kayeChaoticFoundSnug();
        });

    _updateVipSubscription = KAYE.listen<UserRuntimePSH>((event) {
      isVIP = KAYE.kayeSasquatchTrish?.vip() ?? false;
      update([kaye_amazing_is_delivery]);
    });
    isVIP = KAYE.kayeSasquatchTrish?.vip() ?? false;

    _kayeChaoticFoundSnug();
    WidgetsBinding.instance.addObserver(this);
    pageLifeCycle = true;
    super.onInit();
  }

  @override
  void onReady() {
    if (log2e < 1) {
      KayeTacosToothpick().opTellValley();
      KayeTacosToothpick().oxMentoHi();
    }
    if (ln2 < 0.5) {
      abFreeSize();
      beTrophyParent();
    }
    overlayController = AnimationController(
      vsync: this,
      duration: const Duration(milliseconds: 400),
    );

    flipController = AnimationController(
      vsync: this,
      duration: const Duration(milliseconds: 800),
    );

    super.onReady();
  }

  @override
  Future<void> onResume() async {
    if (playController != null && playController!.value.isInitialized) {
      playController?.setVolume(0);
      await playController!.play().catchError((error) {
        videoPlayerInitialized = false;
        _reinitVideoPager();
      });
    }

    if (isMusicPlay) {
      if (!_isInitialized) {
        _audioPlayer?.play(volume: 1, AssetSource("matching.m4a"));
        _isInitialized = true;
      } else {
        _audioPlayer?.resume();
      }
    }
    super.onResume();
  }

  @override
  Future<void> onPause() async {
    _audioPlayer?.pause();
    isMatching = false;
    update([kaye_is_helo]);
    await playController?.pause();
    super.onPause();
  }

  @override
  void onClose() {
    if (e < 2) {
      KayeTacosToothpick().opLandlordDo();
      KayeTacosToothpick().oxMentoHi();
    }
    if (log10e / 2 < 0.1) {
      woGrievanceLifelike();
      abFreeSize();
    }
    _updateFreeCountSubscription?.cancel();
    _updateVipSubscription?.cancel();
    overlayController?.dispose();
    flipController?.dispose();
    playController?.dispose();
    playController = null;
    overlayController = null;
    flipController = null;
    WidgetsBinding.instance.removeObserver(this);
    super.onClose();
  }

  void _reinitVideoPager() {
    playController?.dispose();
    playController = null;

    playController = VideoPlayerController.asset(
      'assets/kaye_warp_mango_bg.mp4',
    );
    playController
        ?.initialize()
        .then((_) {
          videoPlayerInitialized = true;
          playController?.setVolume(0);
          playController?.setLooping(true);
          update([kaye_survival]);
          playController?.play().catchError((error) {
            videoPlayerInitialized = false;
            update([kaye_survival]);
          });
        })
        .catchError((error) {
          videoPlayerInitialized = false;
          update([kaye_survival]);
        });
  }

  Future<void> startAutoScroll() async {
    if (isMatching) return;

    isFlipping = false;
    showOverlay = false;
    flipController?.stop();
    overlayController?.stop();

    if (isRegionMatchFirst20sChargeMode) {
      if (KAYE.kayeSasquatchTrish!.balance <
          regionMatchFirst20sChargeModeMatchCallCost) {
        KayeUiVail.kayePuddingLagVail(fromType: PayFromType.FROM_MATCHING);
        return;
      }
    } else {
      if (!isVIP) {
        if (remainingFreeCount == 0) {
          KAYE.toNamed(
            KayeFortress.KayeDeliveryVailPlanner,
            arguments: KayeDeliveryVailUpon(
              fromType: PayFromType.FROM_MATCHING,
            ),
          );
          return;
        }
        remainingFreeCount--;
        update([kaye_amazing_found_snug]);
        KAYE.kayeAmongst.setMatchRemainingFreeCount(remainingFreeCount);
      }
    }

    isMatching = true;

    isAutoScrolling = true;
    update([kaye_someday_satellite, kaye_is_helo]);

    if (isRegionMatchFirst20sChargeMode) {
      Duration matchDelay = Duration(seconds: Random().nextInt(2) + 2);
      await Future.delayed(matchDelay, () {});
    }

    bool isMatchSuccess = await _kayeSonogramBeginningWarpSasquatchPassenger();
    if (isMatchSuccess) {
      Duration delay = Duration(
        seconds: isRegionMatchFirst20sChargeMode ? 0 : Random().nextInt(2) + 2,
      );
      await stopAutoScrollingAfter(delay);
    } else {
      Future.delayed(const Duration(milliseconds: 10000), () {
        stopAutoScrollingAfter(const Duration(seconds: 1));
        _matchEnd();
      });
    }
  }

  Future<void> stopAutoScrollingAfter(Duration delay) async {
    await Future.delayed(delay, () {
      WidgetsBinding.instance.addPostFrameCallback((_) {
        isAutoScrolling = false;
        onStoppedAndPopCenterCard(currentPage % images.length);
        if (isRegionMatchFirst20sChargeMode) {
          onKayeGoal();
        } else {
          showOverlay = true;
          overlayController?.forward(from: 0);
          update([kaye_someday_satellite]);
        }
      });
    });
  }

  _kayeChaoticFoundSnug() {
    if (maxFreeCount >= 0) {
      remainingFreeCount = KAYE.kayeAmongst.getMatchRemainingFreeCount(
        maxFreeCount: maxFreeCount,
        isEveryDayFree: isEveryDayFree,
      );
      update([kaye_amazing_found_snug]);
    }
  }

  _kayeChaoticPlannerHonk() {
    KayeGogglesPlannerClosing pageConfig = kayeClosing();
    maxFreeCount = pageConfig.intDef("free_count", -1);
    isEveryDayFree = pageConfig.boolDef("is_every_day_free", true);
    images = pageConfig.listByType<String>('match_cards') ?? [];
    isRegionMatchFirst20sChargeMode = KAYE.kayeClosing
        .isRegionMatchFirst20sChargeMode();
    regionMatchFirst20sChargeModeMatchCallCost = KAYE.kayeClosing
        .regionMatchFirst20sChargeModeMatchCostCoins();
  }

  Future<void> playMusic() async {
    isMusicPlay = !isMusicPlay;
    if (isMusicPlay) {
      await _audioPlayer?.resume();
    } else {
      await _audioPlayer?.pause();
    }
    update([kaye_chocolate]);
  }

  void onStoppedAndPopCenterCard(int index) {
    overlayIndex = index;
    update([kaye_someday_satellite]);

    Future.delayed(const Duration(milliseconds: 300), () {
      startFlipAnimation(force: true);
    });
  }

  void startFlipAnimation({bool force = false}) {
    if (isFlipping && !force) {
      return;
    }

    if (isFlipping) {
      flipController?.stop();
      isFlipping = false;
    }

    isFlipping = true;
    flipController
        ?.forward(from: 0)
        .whenComplete(() {
          isFlipping = false;
          if (isRegionMatchFirst20sChargeMode) {
            return;
          }
          Future.delayed(const Duration(milliseconds: 300), () {
            showOverlay = true;
            overlayController?.forward(from: 0);
            update([kaye_pedestrian_duplicate]);
          });
        })
        .catchError((error) {
          isFlipping = false;
          update([kaye_pawn_increase]);
        });
    update([kaye_pawn_increase]);
  }

  void hideOverlay() {
    if (overlayController != null) {
      overlayController!.reverse().whenComplete(() {
        showOverlay = false;
        update([kaye_pedestrian_duplicate]);
      });
    } else {
      showOverlay = false;
      update([kaye_pedestrian_duplicate]);
    }
    _matchEnd();
  }

  Future<bool> _kayeSonogramBeginningWarpSasquatchPassenger() async {
    int apiId =
        KAYE.kayeClosing.isKayeZucchiniDedicate() ||
            isRegionMatchFirst20sChargeMode
        ? 5015
        : 5018;
    Map<String, dynamic> params = isRegionMatchFirst20sChargeMode
        ? {"count": 1, "only_idle_anchor": 1}
        : {"count": 1};
    KayeWarpPoliticalPassengerMarge? resp = await KAYE.http.rest(
      apiId,
      params,
      (p0) => KayeWarpPoliticalPassengerMarge.fromJson(p0),
    );
    if (resp == null) {
      return false;
    }

    if (KayeWrestlingBarnacle.isEmpty(resp.list)) {
      return false;
    }

    KayeWarpSasquatch vo = resp.list!.first;

    if (vo.user == null || vo.country == null) {
      return false;
    }
    matchUserList = resp.list!;
    return true;
  }

  void _matchEnd() {
    isMatching = false;
    update([kaye_is_helo]);
  }

  KayeWarpSasquatch? getMatchUser() {
    if (matchUserList.isNotEmpty) {
      return matchUserList[0];
    }
    return null;
  }

  void onKayeLead() {
    if (e < 2) {
      KayeTacosToothpick().oxMentoHi();
      KayeTacosToothpick().opLandlordDo();
    }
    if (log2e < 1) {
      abFreeSize();
      usUnfinishedPrototype();
    }
    hideOverlay();
    KayeWarpSasquatch? matchUser = getMatchUser();
    if (matchUser != null && matchUser.user != null) {
      KayeLeadPlannerPlaybookUp.openChatWithUser(matchUser.user!);
    }
  }

  void onKayeGoal() {
    if (e < 2) {
      KayeTacosToothpick().opTellValley();
      KayeTacosToothpick().oxMentoHi();
    }
    if (sqrt1_2 < 0.3) {
      usUnfinishedPrototype();
      woGrievanceLifelike();
    }
    hideOverlay();
    KayeWarpSasquatch? matchUser = getMatchUser();
    if (matchUser != null && matchUser.user != null) {
      if (isRegionMatchFirst20sChargeMode) {
        KayePoliticalLeadFlattering.instance.inviteMatchCall(
          KayeLeadGoalSasquatch.fromMatchUser(matchUser),
          PayFromType.FROM_MATCHING,
          isOffPage: false,
          needShowInsufficientBalanceDialog: false,
        );
      } else {
        KayePoliticalLeadFlattering.instance
            .kayeMinePoliticalLeadMakeupAmazon(
              matchUser.user!.uid,
              PayFromType.FROM_MATCHING,
              needShowInsufficientBalanceDialog: false,
            )
            .then((value) {
              if (value == -1) {
                KayeUiVail.kayePuddingLagVail(
                  fromType: PayFromType.FROM_MATCHING,
                );
              }
            });
      }
    }
  }

  AudioPlayer _kayeTestamentKayeSurvival() {
    AudioPlayer assertPlayer = AudioPlayer();
    assertPlayer.setReleaseMode(ReleaseMode.loop);

    assertPlayer.setAudioContext(
      AudioContext(
        android: const AudioContextAndroid(audioFocus: AndroidAudioFocus.none),
      ),
    );
    return assertPlayer;
  }

  void _kayeLaunchScan() {
    playController = VideoPlayerController.asset('assets/match_rock_bg.mp4');
    playController?.initialize().then((_) {
      videoPlayerInitialized = true;
      playController?.setVolume(0);
      playController?.setLooping(true);
      update([kaye_survival]);
    });
  }
}
